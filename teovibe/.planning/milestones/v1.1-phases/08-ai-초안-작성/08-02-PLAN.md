---
phase: 08-ai-초안-작성
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - teovibe/app/frontend/controllers/ai_draft_controller.js
  - teovibe/app/views/admin/posts/_form.html.erb
autonomous: false
requirements:
  - AIDR-01
  - AIDR-02
  - AIDR-03

must_haves:
  truths:
    - "Admin 게시글 작성/수정 폼 상단에 AI 초안 패널이 표시된다"
    - "주제 입력 후 '개요 생성' 버튼 클릭 시 로딩 스피너가 표시되고 개요 textarea에 결과가 나타난다"
    - "Admin이 개요 textarea를 직접 수정할 수 있다"
    - "'본문 생성' 버튼 클릭 시 개요 기반 본문이 rhino-editor에 삽입된다"
    - "에러 발생 시 에러 메시지가 화면에 표시된다"
    - "API 호출 중 버튼이 비활성화되어 중복 요청이 방지된다"
  artifacts:
    - path: "teovibe/app/frontend/controllers/ai_draft_controller.js"
      provides: "Stimulus Controller: 개요 생성, 본문 생성, rhino-editor 삽입, 로딩 상태"
      exports: ["default (Stimulus Controller)"]
    - path: "teovibe/app/views/admin/posts/_form.html.erb"
      provides: "AI 초안 패널 UI (주제 입력, 개요 textarea, 버튼, 에러 영역)"
  key_links:
    - from: "teovibe/app/views/admin/posts/_form.html.erb"
      to: "teovibe/app/frontend/controllers/ai_draft_controller.js"
      via: "data-controller='ai-draft', data-action 어트리뷰트"
      pattern: "ai-draft"
    - from: "teovibe/app/frontend/controllers/ai_draft_controller.js"
      to: "/admin/ai_draft/outline + /admin/ai_draft/body"
      via: "fetch POST with CSRF token"
      pattern: "fetch.*ai_draft"
    - from: "teovibe/app/frontend/controllers/ai_draft_controller.js"
      to: "rhino-editor DOM element"
      via: "rhinoEditor.editor.commands.setContent + updateInputElementValue()"
      pattern: "updateInputElementValue"
---

<objective>
AI 초안 작성 프론트엔드를 구현한다: Stimulus Controller(ai_draft_controller.js)로 개요 생성 + 개요 수정 + 본문 생성 + rhino-editor 삽입 흐름을 구현하고, Admin 게시글 작성/수정 폼에 AI 초안 패널 UI를 추가한다.

Purpose: Admin이 실제로 주제 입력 → 개요 확인/수정 → 본문 삽입까지 브라우저에서 조작할 수 있게 한다.
Output: Admin 게시글 폼에 완성된 AI 초안 UI + 사람이 직접 검증하는 체크포인트
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-ai-초안-작성/08-RESEARCH.md
@.planning/phases/08-ai-초안-작성/08-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 라우트 (outline, body 엔드포인트) -->
<!-- bin/rails routes | grep ai_draft 결과 예시: -->
<!-- outline_admin_ai_draft POST /admin/ai_draft/outline(.:format) admin/ai_drafts#outline -->
<!-- body_admin_ai_draft    POST /admin/ai_draft/body(.:format)    admin/ai_drafts#body    -->

From teovibe/app/views/admin/posts/_form.html.erb (현재 상태):
```erb
<%= form_with(model: [:admin, post], class: "space-y-4") do |f| %>
  <!-- ... 폼 필드들 ... -->
  <div>
    <label class="block text-sm font-bold mb-1">본문</label>
    <%= f.hidden_field :body, id: f.field_id(:body), value: f.object.body.try(:to_trix_html) || f.object.body %>
    <rhino-editor
      input="<%= f.field_id(:body) %>"
      data-blob-url-template="<%= rails_service_blob_url(":signed_id", ":filename") %>"
      data-direct-upload-url="<%= rails_direct_uploads_url %>"
      class="w-full min-h-[200px] rounded-2xl border border-gray-300"
    ></rhino-editor>
  </div>
  <!-- ... SEO 필드들 ... -->
<% end %>
```

From teovibe/app/frontend/controllers/search_autocomplete_controller.js (fetch 패턴):
```javascript
fetch(`${this.urlValue}?q=${encodeURIComponent(query)}`)
  .then(response => response.json())
  .then(data => { ... })
```

From 08-RESEARCH.md (rhino-editor 삽입 API):
```javascript
const rhinoEditor = document.querySelector("rhino-editor")
if (rhinoEditor && rhinoEditor.editor) {
  rhinoEditor.editor.commands.setContent(htmlContent, false)
  rhinoEditor.updateInputElementValue()
}
```

Rails URL helpers (Plan 01 라우트 기반):
- outline_admin_ai_draft_path → POST /admin/ai_draft/outline
- body_admin_ai_draft_path → POST /admin/ai_draft/body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stimulus ai_draft_controller.js 구현</name>
  <files>teovibe/app/frontend/controllers/ai_draft_controller.js</files>
  <action>
    `teovibe/app/frontend/controllers/ai_draft_controller.js` 생성:

    ```javascript
    import { Controller } from "@hotwired/stimulus"

    export default class extends Controller {
      static targets = ["topic", "outlinePanel", "outline", "generateBodyBtn", "loading", "error"]
      static values = { outlineUrl: String, bodyUrl: String }

      // AIDR-01: 개요 생성
      async generateOutline() {
        const topic = this.topicTarget.value.trim()
        if (!topic) {
          this.showError("주제를 입력해주세요.")
          return
        }

        this.clearError()
        this.setLoading(true, "outline")

        try {
          const response = await fetch(this.outlineUrlValue, {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
              "X-CSRF-Token": document.querySelector("meta[name='csrf-token']").content
            },
            body: JSON.stringify({ topic })
          })

          const data = await response.json()

          if (!response.ok) {
            this.showError(data.error || "개요 생성에 실패했습니다.")
            return
          }

          // AIDR-02: 개요 패널 표시 및 수정 가능 상태
          this.outlineTarget.value = data.outline
          this.outlinePanelTarget.classList.remove("hidden")
        } catch (e) {
          this.showError("네트워크 오류가 발생했습니다.")
        } finally {
          this.setLoading(false, "outline")
        }
      }

      // AIDR-02 + AIDR-03: 본문 생성 후 rhino-editor 삽입
      async generateBody() {
        const outline = this.outlineTarget.value.trim()
        if (!outline) {
          this.showError("개요를 먼저 생성해주세요.")
          return
        }

        this.clearError()
        this.setLoading(true, "body")

        try {
          const response = await fetch(this.bodyUrlValue, {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
              "X-CSRF-Token": document.querySelector("meta[name='csrf-token']").content
            },
            body: JSON.stringify({ outline })
          })

          const data = await response.json()

          if (!response.ok) {
            this.showError(data.error || "본문 생성에 실패했습니다.")
            return
          }

          // AIDR-03: rhino-editor에 삽입 (updateInputElementValue 필수)
          const rhinoEditor = document.querySelector("rhino-editor")
          if (rhinoEditor && rhinoEditor.editor) {
            rhinoEditor.editor.commands.setContent(data.body_html, false)
            rhinoEditor.updateInputElementValue()
          } else {
            this.showError("에디터를 찾을 수 없습니다. 페이지를 새로고침 후 다시 시도해주세요.")
          }
        } catch (e) {
          this.showError("네트워크 오류가 발생했습니다.")
        } finally {
          this.setLoading(false, "body")
        }
      }

      setLoading(isLoading, type) {
        if (this.hasLoadingTarget) {
          this.loadingTarget.classList.toggle("hidden", !isLoading)
        }
        // 버튼 비활성화로 중복 요청 방지
        const buttons = this.element.querySelectorAll("button[data-action]")
        buttons.forEach(btn => { btn.disabled = isLoading })
      }

      showError(message) {
        if (this.hasErrorTarget) {
          this.errorTarget.textContent = message
          this.errorTarget.classList.remove("hidden")
        }
      }

      clearError() {
        if (this.hasErrorTarget) {
          this.errorTarget.textContent = ""
          this.errorTarget.classList.add("hidden")
        }
      }
    }
    ```

    Stimulus index.js에 등록: `teovibe/app/frontend/controllers/index.js`에
    `import AiDraftController from "./ai_draft_controller"` 및
    `application.register("ai-draft", AiDraftController)` 추가.
    기존 컨트롤러 등록 패턴 확인 후 동일 방식으로 추가.
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && node -e "require('fs').readFileSync('app/frontend/controllers/ai_draft_controller.js', 'utf8')" | node -e "process.stdin.resume(); process.stdin.setEncoding('utf8'); let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{ ['generateOutline','generateBody','updateInputElementValue','X-CSRF-Token','outlineUrl','bodyUrl'].forEach(k=>{ if(!d.includes(k)) throw new Error('Missing: '+k) }); console.log('OK') })"</automated>
  </verify>
  <done>ai_draft_controller.js 생성, generateOutline/generateBody/setLoading/showError 메서드 구현, index.js에 등록 완료</done>
</task>

<task type="auto">
  <name>Task 2: Admin 게시글 폼에 AI 초안 패널 UI 추가</name>
  <files>teovibe/app/views/admin/posts/_form.html.erb</files>
  <action>
    `teovibe/app/views/admin/posts/_form.html.erb` 상단 (form 태그 바로 아래, 에러 표시 div 아래)에 AI 초안 패널 추가.

    Rails URL helpers:
    - outline URL: `outline_admin_ai_draft_path` (Plan 01 라우트)
    - body URL: `body_admin_ai_draft_path` (Plan 01 라우트)

    추가할 UI 블록 (제목 div 위, 에러 div 아래에 삽입):

    ```erb
    <%# AI 초안 작성 패널 %>
    <div class="bg-gray-50 rounded-2xl border border-gray-200 p-5"
         data-controller="ai-draft"
         data-ai-draft-outline-url-value="<%= outline_admin_ai_draft_path %>"
         data-ai-draft-body-url-value="<%= body_admin_ai_draft_path %>">
      <h3 class="text-sm font-black text-tv-black mb-3">AI 초안 작성</h3>

      <%# AIDR-01: 주제 입력 %>
      <div class="flex gap-2 mb-3">
        <input type="text"
               placeholder="주제 또는 키워드를 입력하세요 (예: Rails 성능 최적화 팁)"
               class="flex-1 px-3 py-2 text-sm rounded-xl border border-gray-300 focus:outline-none"
               data-ai-draft-target="topic" />
        <button type="button"
                class="px-4 py-2 text-sm font-bold bg-tv-black text-white rounded-xl hover:bg-gray-800 disabled:opacity-50"
                data-action="click->ai-draft#generateOutline">
          개요 생성
        </button>
      </div>

      <%# 로딩 인디케이터 %>
      <div class="hidden text-sm text-tv-gray mb-2" data-ai-draft-target="loading">
        생성 중... (10-30초 소요)
      </div>

      <%# 에러 메시지 %>
      <div class="hidden text-sm text-red-600 mb-2" data-ai-draft-target="error"></div>

      <%# AIDR-02: 개요 표시 + 수정 영역 %>
      <div class="hidden" data-ai-draft-target="outlinePanel">
        <label class="block text-xs font-bold text-tv-gray mb-1">생성된 개요 (수정 가능)</label>
        <textarea rows="8"
                  class="w-full px-3 py-2 text-sm rounded-xl border border-gray-300 focus:outline-none resize-y mb-2"
                  data-ai-draft-target="outline"
                  placeholder="개요가 여기에 표시됩니다"></textarea>
        <button type="button"
                class="px-4 py-2 text-sm font-bold bg-tv-gold text-tv-black rounded-xl hover:bg-yellow-400 disabled:opacity-50"
                data-action="click->ai-draft#generateBody">
          본문 생성 후 에디터에 삽입
        </button>
      </div>
    </div>
    ```

    파일 현재 상태를 Read 툴로 확인 후 정확한 위치에 삽입. 기존 코드는 절대 삭제하지 않는다.
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && grep -c "ai-draft" app/views/admin/posts/_form.html.erb</automated>
  </verify>
  <done>_form.html.erb에 data-controller="ai-draft" 패널이 추가되고, outline URL/body URL이 Rails URL helpers로 올바르게 참조된다</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    AI 초안 작성 전체 기능 (백엔드 API + 프론트엔드 Stimulus + Admin 폼 UI).
    ANTHROPIC_API_KEY가 .env에 설정되어 있어야 실제 API 호출이 동작한다.
  </what-built>
  <how-to-verify>
    1. `cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bin/rails server` 실행
    2. http://localhost:3000/admin/posts/new 접속 (Admin 계정으로 로그인)
    3. 폼 상단에 "AI 초안 작성" 패널이 표시되는지 확인
    4. 주제 입력란에 "Rails 성능 최적화 팁" 입력 후 "개요 생성" 버튼 클릭
    5. "생성 중..." 로딩 텍스트가 표시되고, 완료 후 개요 textarea에 H2 섹션 목록이 나타나는지 확인
    6. 개요 textarea 내용을 직접 수정 가능한지 확인
    7. "본문 생성 후 에디터에 삽입" 버튼 클릭
    8. rhino-editor에 H2/H3 구조의 본문이 삽입되는지 확인
    9. "작성" 버튼으로 저장 후 게시글 본문이 올바르게 저장되었는지 확인

    ANTHROPIC_API_KEY 미설정 시: 에러 메시지가 화면에 표시되면 정상 동작 (API 키 설정 필요 안내)
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견된 이슈 설명</resume-signal>
</task>

</tasks>

<verification>
1. `bin/rails routes | grep ai_draft` → outline, body 경로 두 개 노출
2. `bin/rails test` → 전체 테스트 통과
3. Admin 폼에서 AI 초안 패널이 표시되고 기능 동작 확인 (checkpoint)
</verification>

<success_criteria>
1. Admin 게시글 작성 폼에 AI 초안 패널 UI가 표시된다
2. 주제 입력 → 개요 생성 → textarea에 H2 목록 표시 → 수정 가능
3. 본문 생성 클릭 → rhino-editor에 HTML 삽입 → 폼 저장 시 본문 유지
4. 에러 발생 시 화면에 메시지 표시
5. API 호출 중 버튼 비활성화
</success_criteria>

<output>
완료 후 `.planning/phases/08-ai-초안-작성/08-02-SUMMARY.md` 생성
</output>
