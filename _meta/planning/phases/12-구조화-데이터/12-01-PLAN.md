---
phase: 12-구조화-데이터
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/app/views/posts/show.html.erb
  - teovibe/app/views/pages/home.html.erb
  - teovibe/test/integration/json_ld_test.rb
autonomous: true
requirements: [STRD-01, STRD-02, STRD-03]

must_haves:
  truths:
    - "게시글 상세 페이지 HTML에 Article JSON-LD가 script[type=application/ld+json] 태그로 출력된다"
    - "게시글 상세 페이지 HTML에 BreadcrumbList JSON-LD가 홈 > 카테고리 > 게시글 3단계로 출력된다"
    - "홈페이지 HTML에 WebSite + Organization JSON-LD가 각각 출력된다"
    - "JSON-LD 출력에 <script> 등 특수문자가 Unicode 이스케이프되어 있다"
  artifacts:
    - path: "teovibe/app/views/posts/show.html.erb"
      provides: "Article + BreadcrumbList JSON-LD content_for :head 블록"
      contains: "article_json_ld"
    - path: "teovibe/app/views/pages/home.html.erb"
      provides: "WebSite + Organization JSON-LD content_for :head 블록"
      contains: "website_json_ld"
    - path: "teovibe/test/integration/json_ld_test.rb"
      provides: "JSON-LD 출력 통합 테스트"
      min_lines: 30
  key_links:
    - from: "teovibe/app/views/posts/show.html.erb"
      to: "teovibe/app/helpers/seo_helper.rb"
      via: "article_json_ld(@post) + breadcrumb_json_ld(items)"
      pattern: "article_json_ld.*@post"
    - from: "teovibe/app/views/pages/home.html.erb"
      to: "teovibe/app/helpers/seo_helper.rb"
      via: "website_json_ld + organization_json_ld"
      pattern: "website_json_ld"
---

<objective>
게시글 상세 페이지에 Article + BreadcrumbList JSON-LD를, 홈페이지에 WebSite + Organization JSON-LD를 출력한다.

Purpose: Google Rich Results 자격 확보 및 사이트 신뢰도 구조화 데이터 제공
Output: 뷰 2개 수정, 통합 테스트 1개 생성
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- seo_helper.rb 헬퍼 메서드 시그니처 (Phase 9에서 패치 완료) -->

From teovibe/app/helpers/seo_helper.rb:
```ruby
# post = Post 객체 (title, created_at, updated_at, user.nickname 필요)
def article_json_ld(post) -> String (html_safe JSON)

# items = [{name: String, url: String|nil}, ...] 배열
def breadcrumb_json_ld(items) -> String (html_safe JSON)

# 파라미터 없음 (root_url 헬퍼 사용)
def website_json_ld -> String (html_safe JSON)

# 파라미터 없음 (root_url 헬퍼 사용)
def organization_json_ld -> String (html_safe JSON)
```

From teovibe/app/views/posts/show.html.erb (현재 상태):
- content_for :head 블록 없음 — 새로 추가 필요
- 기존 breadcrumb: render "shared/breadcrumb" (line 2-6)에서 동일한 계층 데이터 사용

From teovibe/app/views/pages/home.html.erb (현재 상태):
```erb
<div id="landing-root"></div>
<% content_for :head do %>
  <%= vite_javascript_tag 'landing.jsx' %>
<% end %>
```
- 이미 content_for :head 블록 존재 — 기존 블록에 JSON-LD 추가

From teovibe/app/views/layouts/application.html.erb:
- line 21: `<%= yield :head %>` — content_for :head가 이 위치에 렌더링됨
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 게시글 상세 Article + BreadcrumbList JSON-LD 배선</name>
  <files>teovibe/app/views/posts/show.html.erb, teovibe/test/integration/json_ld_test.rb</files>
  <behavior>
    - Test 1: GET /posts/:slug 응답 body에 script[type="application/ld+json"] 태그가 포함되고, 파싱하면 @type=Article인 JSON 객체가 있다
    - Test 2: Article JSON-LD에 headline, datePublished, author.name 필드가 존재한다
    - Test 3: GET /posts/:slug 응답에 @type=BreadcrumbList인 JSON-LD가 포함되고, itemListElement가 3개(홈, 카테고리, 게시글)이다
    - Test 4: BreadcrumbList의 첫 항목 name이 "홈"이고, 두 번째 항목이 카테고리명이다
  </behavior>
  <action>
    1. teovibe/test/integration/json_ld_test.rb 생성:
       - ActionDispatch::IntegrationTest 상속
       - fixtures :posts, :users, :categories, :action_text_rich_texts (기존 픽스처 활용)
       - GET posts/show 요청 후 response.body에서 script[type="application/ld+json"] 태그를 Nokogiri로 파싱
       - 각 JSON-LD 블록을 JSON.parse하여 @type 확인
       - Article: headline, datePublished, author 존재 검증
       - BreadcrumbList: itemListElement 3개, position/name 검증

    2. teovibe/app/views/posts/show.html.erb 상단에 content_for :head 블록 추가:
       ```erb
       <%# 구조화 데이터 JSON-LD (Article + BreadcrumbList) %>
       <% content_for :head do %>
         <script type="application/ld+json"><%= article_json_ld(@post) %></script>
         <script type="application/ld+json"><%= breadcrumb_json_ld([
           { name: "홈", url: root_url },
           { name: @post.category_name, url: category_posts_url(category_slug: @post.category&.slug) },
           { name: @post.title }
         ]) %></script>
       <% end %>
       ```
       기존 article 태그 이전에 배치. category_posts_url (절대 URL) 사용.
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test test/integration/json_ld_test.rb -v 2>&1 | tail -20</automated>
  </verify>
  <done>게시글 상세 페이지에 Article + BreadcrumbList JSON-LD가 출력되고, 통합 테스트 4개 PASS</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 홈페이지 WebSite + Organization JSON-LD 배선</name>
  <files>teovibe/app/views/pages/home.html.erb, teovibe/test/integration/json_ld_test.rb</files>
  <behavior>
    - Test 5: GET / 응답 body에 @type=WebSite인 JSON-LD가 포함되고, name과 url 필드가 있다
    - Test 6: GET / 응답 body에 @type=Organization인 JSON-LD가 포함되고, name과 url 필드가 있다
  </behavior>
  <action>
    1. teovibe/test/integration/json_ld_test.rb에 홈페이지 테스트 추가:
       - GET root_path 요청 후 script[type="application/ld+json"] 파싱
       - WebSite: @type, name, url 검증
       - Organization: @type, name, url 검증

    2. teovibe/app/views/pages/home.html.erb의 기존 content_for :head 블록에 JSON-LD 추가:
       ```erb
       <% content_for :head do %>
         <%= vite_javascript_tag 'landing.jsx' %>
         <script type="application/ld+json"><%= website_json_ld %></script>
         <script type="application/ld+json"><%= organization_json_ld %></script>
       <% end %>
       ```
       기존 vite_javascript_tag 유지하면서 JSON-LD 태그 추가.
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test test/integration/json_ld_test.rb -v 2>&1 | tail -20</automated>
  </verify>
  <done>홈페이지에 WebSite + Organization JSON-LD가 출력되고, 통합 테스트 6개 전부 PASS</done>
</task>

</tasks>

<verification>
1. 전체 통합 테스트 통과: `cd teovibe && bin/rails test test/integration/json_ld_test.rb`
2. 기존 seo_helper 단위 테스트 회귀 없음: `cd teovibe && bin/rails test test/helpers/seo_helper_test.rb`
3. 전체 테스트 스위트 회귀 확인: `cd teovibe && bin/rails test` (기존 AI 관련 실패 2건은 pre-existing)
</verification>

<success_criteria>
- 게시글 상세 페이지 HTML에 Article + BreadcrumbList JSON-LD가 script 태그로 출력
- BreadcrumbList가 홈 > 카테고리 > 게시글 3단계 구조
- 홈페이지 HTML에 WebSite + Organization JSON-LD가 각각 출력
- 모든 JSON-LD가 JSON.parse로 유효하게 파싱됨
- 특수문자가 Unicode 이스케이프 처리됨 (safe_json_ld 래퍼 경유)
</success_criteria>

<output>
After completion, create `.planning/phases/12-구조화-데이터/12-01-SUMMARY.md`
</output>
