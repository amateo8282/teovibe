---
phase: 09-xss-보안-패치
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/app/helpers/seo_helper.rb
  - teovibe/test/helpers/seo_helper_test.rb
autonomous: true
requirements:
  - SEC-01

must_haves:
  truths:
    - "게시글 제목에 <script> 태그를 포함해도 JSON-LD 출력에서 이스케이프 처리되어 브라우저가 실행하지 않는다"
    - "Brakeman 정적 분석에서 seo_helper.rb 관련 경고가 0건이다"
    - "이스케이프 후에도 JSON-LD 출력이 유효한 JSON으로 파싱된다"
  artifacts:
    - path: "teovibe/app/helpers/seo_helper.rb"
      provides: "safe_json_ld private 래퍼 메서드 + 8개 public 메서드 XSS 패치"
      contains: "safe_json_ld"
    - path: "teovibe/test/helpers/seo_helper_test.rb"
      provides: "XSS 이스케이프 및 JSON 유효성 검증 테스트"
      min_lines: 30
  key_links:
    - from: "teovibe/app/helpers/seo_helper.rb"
      to: "safe_json_ld"
      via: "모든 public JSON-LD 메서드가 safe_json_ld를 호출"
      pattern: "safe_json_ld\\("
---

<objective>
seo_helper.rb의 8개 JSON-LD 헬퍼 메서드에서 `.to_json.html_safe` XSS 취약 패턴을 `safe_json_ld` 래퍼로 교체한다.

Purpose: Phase 12(구조화 데이터)에서 JSON-LD 헬퍼를 뷰에서 안전하게 사용하기 위한 선행 보안 패치
Output: XSS 이스케이프된 JSON-LD 헬퍼 + 단위 테스트 + Brakeman 0경고
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-xss-보안-패치/09-RESEARCH.md

<interfaces>
<!-- seo_helper.rb 현재 구조: 8개 public 메서드 + 1개 private 메서드 -->

From teovibe/app/helpers/seo_helper.rb:
```ruby
module SeoHelper
  def article_json_ld(post)        # post.title, post.user.nickname 사용자 입력 포함
  def organization_json_ld         # 정적 데이터만 사용하나 일관성 위해 패치 대상
  def website_json_ld              # 정적 데이터 + search_url
  def software_application_json_ld(skill_pack)  # skill_pack.title, description 사용자 입력 포함
  def item_list_json_ld(items, name:)           # items.title, name 사용자 입력 포함
  def profile_page_json_ld(user)   # user.nickname, user.bio 사용자 입력 포함
  def faq_json_ld(items)           # items[:question], items[:answer] 사용자 입력 포함
  def breadcrumb_json_ld(items)    # items[:name] 사용자 입력 포함

  private
  def organization_json_ld_hash    # Hash 반환 (직렬화하지 않음) — 패치 불필요
end
```

From teovibe/test/fixtures/posts.yml:
```yaml
blog_post:
  title: 테스트 블로그 게시글
  slug: test-blog-post
  status: 1
  category: blog
  user: one
```

From teovibe/test/fixtures/users.yml:
```yaml
one:
  nickname: 테스트유저1
  role: 0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SeoHelper XSS 이스케이프 테스트 작성 및 safe_json_ld 패치 구현</name>
  <files>teovibe/test/helpers/seo_helper_test.rb, teovibe/app/helpers/seo_helper.rb</files>
  <behavior>
    - article_json_ld: 제목에 `</script><script>alert(1)</script>` 포함 시 raw `</script>` 미출현 + `\u003c` 포함
    - article_json_ld: 제목에 `Tom & Jerry` 포함 시 `\u0026` 포함
    - article_json_ld: 이스케이프 후 결과가 JSON.parse로 유효한 Hash 반환
    - organization_json_ld: raw `</script>` 미출현 + JSON.parse 유효
    - profile_page_json_ld: 닉네임에 `<img onerror=alert(1)>` 포함 시 `\u003c` 포함
    - item_list_json_ld: name 파라미터에 XSS 페이로드 포함 시 이스케이프
    - breadcrumb_json_ld: items[:name]에 XSS 페이로드 포함 시 이스케이프
    - 모든 public 메서드(8개)가 `.to_json.html_safe` 대신 `safe_json_ld` 호출
  </behavior>
  <action>
    TDD RED: `teovibe/test/helpers/seo_helper_test.rb` 신규 생성. ActionView::TestCase 상속. 테스트에서 post, user 등 객체는 OpenStruct으로 mock하여 DB 의존성 제거. root_url, search_url 등 URL 헬퍼는 `setup`에서 stub.

    테스트 케이스 목록:
    1. `test "article_json_ld escapes script injection in title"` — `</script><script>alert(1)</script>` 주입
    2. `test "article_json_ld escapes ampersand"` — `Tom & Jerry`
    3. `test "article_json_ld returns valid JSON"` — JSON.parse 성공
    4. `test "organization_json_ld escapes and returns valid JSON"`
    5. `test "profile_page_json_ld escapes nickname"` — `<img onerror=alert(1)>` 주입
    6. `test "item_list_json_ld escapes name parameter"` — XSS 페이로드
    7. `test "breadcrumb_json_ld escapes item names"` — XSS 페이로드
    8. `test "no raw html_safe without safe_json_ld in seo_helper"` — `grep` 또는 소스코드 검사로 `.to_json.html_safe` 패턴 0건 확인

    `bin/rails test test/helpers/seo_helper_test.rb` 실행 — 전부 FAIL 확인 (RED).

    TDD GREEN: `teovibe/app/helpers/seo_helper.rb` 수정.
    1. private 영역에 `safe_json_ld(data)` 메서드 추가:
       ```ruby
       def safe_json_ld(data)
         data.to_json
             .gsub('<', '\u003c')
             .gsub('>', '\u003e')
             .gsub('&', '\u0026')
             .gsub('/', '\u002f')
             .html_safe
       end
       ```
    2. 8개 public 메서드 모두 `}.to_json.html_safe` → `safe_json_ld({...})` 로 교체.
       - `article_json_ld`: hash 전체를 `safe_json_ld()`로 감싸기
       - `organization_json_ld`: `safe_json_ld(organization_json_ld_hash)`
       - `website_json_ld`: hash 전체를 `safe_json_ld()`로 감싸기
       - `software_application_json_ld`: hash 전체를 `safe_json_ld()`로 감싸기
       - `item_list_json_ld`: hash 전체를 `safe_json_ld()`로 감싸기
       - `profile_page_json_ld`: hash 전체를 `safe_json_ld()`로 감싸기
       - `faq_json_ld`: hash 전체를 `safe_json_ld()`로 감싸기
       - `breadcrumb_json_ld`: hash 전체를 `safe_json_ld()`로 감싸기
    3. `grep -n "\.to_json\.html_safe" teovibe/app/helpers/seo_helper.rb` → 0건 확인

    `bin/rails test test/helpers/seo_helper_test.rb` 실행 — 전부 PASS 확인 (GREEN).

    주의사항:
    - `organization_json_ld_hash`는 Hash만 반환하는 private 메서드이므로 직렬화 로직 수정 불필요. `organization_json_ld` public 메서드에서 `safe_json_ld(organization_json_ld_hash)` 호출로 처리.
    - `item_list_json_ld`와 `breadcrumb_json_ld`는 내부에 `.each_with_index.map` 블록이 있으므로, hash 전체를 변수에 담은 후 `safe_json_ld(data)` 호출하거나 직접 `safe_json_ld({...})` 형태로 감싸기.
    - `/` 이스케이프(`\u002f`)도 반드시 포함 — `</script>` 완전 차단을 위한 방어적 이스케이프.
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bin/rails test test/helpers/seo_helper_test.rb</automated>
  </verify>
  <done>8개 테스트 전부 PASS. seo_helper.rb에 `.to_json.html_safe` 패턴 0건. safe_json_ld private 메서드 존재.</done>
</task>

<task type="auto">
  <name>Task 2: Brakeman 정적 분석 0경고 확인</name>
  <files>teovibe/app/helpers/seo_helper.rb</files>
  <action>
    `bin/brakeman --no-pager` 실행하여 seo_helper.rb 관련 Cross-Site Scripting 경고가 0건인지 확인한다.

    만약 경고가 남아있다면:
    1. 경고 대상 메서드와 라인 번호 확인
    2. 해당 메서드가 `safe_json_ld`를 사용하도록 수정되었는지 검증
    3. Brakeman이 `safe_json_ld` 내부의 `.html_safe`를 여전히 경고할 수 있음 — 이 경우 `safe_json_ld` 메서드 내에서 `.html_safe` 대신 `ActiveSupport::SafeBuffer.new(...)` 패턴을 사용하거나, Brakeman 결과에서 seo_helper 관련 경고만 필터링하여 실제 XSS 위험이 있는 경고인지 판별
    4. 실제 XSS 위험이 없는 경고(이스케이프 후 html_safe)라면 Brakeman 경고 내용을 SUMMARY에 기록

    전체 테스트 스위트도 실행하여 기존 기능 회귀 없음 확인:
    `bin/rails test`
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bin/brakeman -q --no-pager 2>&1 | grep -i "seo_helper" ; echo "exit: $?"</automated>
  </verify>
  <done>Brakeman 실행 결과에서 seo_helper.rb 관련 XSS 경고 0건. 전체 테스트 스위트 PASS.</done>
</task>

</tasks>

<verification>
1. `cd teovibe && bin/rails test test/helpers/seo_helper_test.rb` — 전체 PASS
2. `cd teovibe && grep -c "\.to_json\.html_safe" app/helpers/seo_helper.rb` — 0 출력
3. `cd teovibe && bin/brakeman -q --no-pager` — seo_helper.rb 관련 경고 0건
4. `cd teovibe && bin/rails test` — 전체 스위트 회귀 없음
</verification>

<success_criteria>
- seo_helper.rb의 8개 public 메서드 모두 safe_json_ld 래퍼를 통해 JSON-LD를 반환한다
- `.to_json.html_safe` 패턴이 seo_helper.rb에서 완전히 제거되었다
- XSS 페이로드(`</script>`, `<img onerror>`, `&`)가 Unicode 이스케이프로 변환된다
- 이스케이프 후 JSON 출력이 JSON.parse로 유효하게 파싱된다
- Brakeman seo_helper.rb 관련 경고 0건
- 기존 테스트 스위트 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/09-xss-보안-패치/09-01-SUMMARY.md`
</output>
