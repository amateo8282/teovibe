---
phase: 07-scheduled-publishing
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - teovibe/test/controllers/admin/posts_controller_test.rb
autonomous: true
requirements: [SCHD-01, SCHD-02, SCHD-03]

must_haves:
  truths:
    - "Admin이 게시글 생성 시 예약 시각을 지정하면 draft 상태 + scheduled_at UTC 저장 + SolidQueue 잡 등록이 된다"
    - "Admin이 예약된 게시글의 시각을 변경하면 기존 잡 취소 + 새 잡 등록이 된다"
    - "Admin이 예약을 취소하면 scheduled_at nil + 기존 잡 삭제가 된다"
    - "예약 시각이 비어있는 게시글은 기존 로직대로 즉시 발행된다"
    - "PublishPostJob 실행 후 게시글이 published 상태로 전환된다"
    - "scheduled 게시글은 Post.published scope에 포함되지 않는다 (공개 피드 미노출)"
  artifacts:
    - path: "teovibe/test/controllers/admin/posts_controller_test.rb"
      provides: "예약 생성/수정/취소 통합 테스트"
      contains: "scheduled_at"
  key_links:
    - from: "teovibe/test/controllers/admin/posts_controller_test.rb"
      to: "teovibe/app/controllers/admin/posts_controller.rb"
      via: "ActionDispatch::IntegrationTest로 create/update 엔드포인트 호출"
      pattern: "post admin_posts_path"
---

<objective>
Admin 게시글 예약 발행의 전체 흐름(생성/수정/취소)을 통합 테스트로 검증하고, Phase 7 전체 요구사항(SCHD-01~03)을 최종 확인한다.

Purpose: 모든 요구사항이 자동 테스트로 보호되어 향후 변경 시 회귀를 방지한다.
Output: 통합 테스트 통과 + Phase 7 전체 요구사항 충족 확인
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scheduled-publishing/07-RESEARCH.md
@.planning/phases/07-scheduled-publishing/07-01-SUMMARY.md
@.planning/phases/07-scheduled-publishing/07-02-SUMMARY.md

<interfaces>
<!-- 07-02에서 수정된 Admin::PostsController 인터페이스 -->
```ruby
module Admin
  class PostsController < BaseController
    # create: handle_scheduling → save → enqueue_publish_job
    # update: cancel_existing_job → handle_scheduling → save → enqueue_publish_job

    private

    def handle_scheduling(post)
      # KST → UTC 변환, draft 강제, 빈 값이면 nil
    end

    def enqueue_publish_job(post)
      # PublishPostJob.set(wait_until:).perform_later → post.update_column(:job_id, ...)
    end

    def cancel_existing_job(post)
      # SolidQueue::Job.find_by(id:).destroy → post.update_column(:job_id, nil)
    end
  end
end
```

<!-- 기존 Admin 컨트롤러 테스트 패턴 참고 (categories_controller_test.rb) -->
```ruby
# sign_in_as(@admin) + ActionDispatch::IntegrationTest 패턴
class Admin::CategoriesControllerTest < ActionDispatch::IntegrationTest
  setup do
    @admin = users(:admin)
    sign_in_as(@admin)
  end

  test "should create category" do
    post admin_categories_path, params: { category: { name: "Test", ... } }
    assert_redirected_to admin_categories_path
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin 게시글 예약 발행 통합 테스트 작성</name>
  <files>teovibe/test/controllers/admin/posts_controller_test.rb</files>
  <behavior>
    - Test 1 (SCHD-01): 예약 시각 지정하여 게시글 생성 → draft 상태 + scheduled_at UTC 저장 + job_id 존재
    - Test 2 (SCHD-01): 예약 시각 없이 게시글 생성 → 기존대로 published 상태 + scheduled_at nil
    - Test 3 (SCHD-02): PublishPostJob 실행 → scheduled 게시글이 published 전환 + scheduled_at/job_id nil
    - Test 4 (SCHD-03): 예약된 게시글 시각 변경 → 기존 job_id 변경됨 + 새 scheduled_at 저장
    - Test 5 (SCHD-03): 예약된 게시글 예약 취소 → scheduled_at nil + job_id nil
    - Test 6 (SCHD-01): 예약 시각이 KST→UTC로 정확히 변환되었는지 확인 (9시간 차이)
    - Test 7 (공개 피드): scheduled 게시글은 Post.published scope에 포함되지 않음 (공개 피드 미노출)
  </behavior>
  <action>
    1. `test/controllers/admin/posts_controller_test.rb` 생성 (기존 파일 없음)
    2. 기존 프로젝트 테스트 패턴 따르기:
       - `ActionDispatch::IntegrationTest` 상속
       - `sign_in_as(@admin)` 헬퍼 사용
       - fixture에서 admin 유저 로드
    3. 테스트 setup:
       ```ruby
       setup do
         @admin = users(:admin)
         sign_in_as(@admin)
         @category = categories(:blog)  # 또는 적절한 fixture
       end
       ```
    4. 각 테스트에서:
       - `post admin_posts_path, params: { post: { title: "...", category_id: @category.id, body: "...", scheduled_at: "2026-04-01T14:00" } }`
       - `assert_redirected_to admin_posts_path`
       - `Post.last` 로 저장된 레코드 검증
       - KST→UTC 변환 검증: `"2026-04-01T14:00"` KST → `"2026-04-01 05:00:00 UTC"` 확인
    5. 예약 취소 테스트:
       - 먼저 예약된 게시글 생성 후 update에서 scheduled_at를 빈 문자열로 전송
    6. PublishPostJob 실행 테스트:
       - 예약된 게시글 직접 생성 후 `PublishPostJob.perform_now(post.id)` 호출하여 상태 변환 확인
    7. 공개 피드 scope 테스트:
       - scheduled 게시글(draft + scheduled_at 존재)을 생성하고 `Post.published`에 포함되지 않음을 assert
       - `assert_not_includes Post.published, scheduled_post`

    **주의사항:**
    - 기존 `test/controllers/admin/` 디렉토리의 테스트 패턴(sign_in_as, fixture) 참고하여 일관성 유지
    - `perform_enqueued_jobs` 블록이나 `assert_enqueued_with` 로 잡 큐잉 검증 가능
    - SolidQueue 잡 취소는 `SolidQueue::Job.count` 변화로 검증 가능
    - fixture에 적절한 카테고리/사용자가 없으면 테스트 내에서 직접 생성
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test test/controllers/admin/posts_controller_test.rb -v</automated>
  </verify>
  <done>
    - 7개 이상의 통합 테스트가 모두 통과
    - SCHD-01: 예약 시각 지정 생성 + KST→UTC 변환 검증
    - SCHD-02: PublishPostJob 실행 시 상태 전환 검증
    - SCHD-03: 예약 변경 + 예약 취소 검증
    - 기존 게시글 CRUD 동작(예약 없이 생성/수정/삭제)이 영향받지 않음
  </done>
</task>

<task type="auto">
  <name>Task 2: 전체 테스트 스위트 통과 + Phase 7 최종 검증</name>
  <files></files>
  <action>
    1. 전체 테스트 스위트 실행: `cd teovibe && bin/rails test`
    2. 실패하는 테스트가 있으면 원인 분석 후 수정
    3. Phase 7 요구사항 체크리스트 확인:
       - [SCHD-01] Admin 폼에 datetime-local 필드 존재, KST 표시, UTC 저장
       - [SCHD-02] PublishPostJob이 예약된 게시글을 자동 published 전환
       - [SCHD-03] 예약 취소 + 시각 변경 동작
    4. 회귀 확인: 기존 Phase 6 테스트(categories_controller_test.rb 등) 통과 여부
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test -v</automated>
  </verify>
  <done>
    - 전체 테스트 스위트 0 failures, 0 errors
    - Phase 7 요구사항 SCHD-01, SCHD-02, SCHD-03 모두 테스트로 커버
    - 기존 Phase 1-6 기능에 회귀 없음
  </done>
</task>

</tasks>

<verification>
```bash
cd teovibe

# 통합 테스트 실행
bin/rails test test/controllers/admin/posts_controller_test.rb -v

# 전체 테스트 스위트
bin/rails test -v

# 요구사항 커버리지 확인
echo "=== SCHD-01: 예약 시각 지정 ==="
grep -c "scheduled_at" test/controllers/admin/posts_controller_test.rb

echo "=== SCHD-02: 자동 발행 ==="
grep -c "PublishPostJob" test/controllers/admin/posts_controller_test.rb test/jobs/publish_post_job_test.rb

echo "=== SCHD-03: 예약 취소/변경 ==="
grep -c "cancel\|변경\|취소" test/controllers/admin/posts_controller_test.rb
```
</verification>

<success_criteria>
1. Admin 게시글 예약 생성/수정/취소 + 공개 피드 scope 통합 테스트 7개 이상 통과
2. 전체 테스트 스위트 0 failures, 0 errors
3. SCHD-01, SCHD-02, SCHD-03 모든 요구사항이 자동 테스트로 커버
4. Phase 6 이하 기존 기능에 회귀 없음
</success_criteria>

<output>
After completion, create `.planning/phases/07-scheduled-publishing/07-03-SUMMARY.md`
</output>
