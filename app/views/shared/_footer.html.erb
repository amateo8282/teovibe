<footer class="bg-tv-dark text-tv-light-gray py-16">
  <div class="max-w-[1200px] mx-auto px-5">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
      <%# 브랜드 %>
      <div>
        <h3 class="text-white text-2xl font-black mb-4">TeoVibe</h3>
        <p class="text-sm leading-relaxed">바이브코딩으로 사업화하는 과정을<br>기록하고 공유하는 커뮤니티</p>
      </div>

      <%# 링크 — visible_in_nav=true 카테고리 동적 표시 %>
      <div>
        <h4 class="text-white text-sm font-bold uppercase mb-4">Links</h4>
        <div class="space-y-2">
          <% Category.for_posts.visible_in_nav.ordered.each do |cat| %>
            <%= link_to cat.name, category_posts_path(category_slug: cat.slug), class: "block text-sm hover:text-white transition-colors" %>
          <% end %>
          <%= link_to "소개", about_path, class: "block text-sm hover:text-white transition-colors" %>
        </div>
      </div>

      <%# 문의 %>
      <div>
        <h4 class="text-white text-sm font-bold uppercase mb-4">Contact</h4>
        <p class="text-sm">문의: teovibe@example.com</p>
      </div>
    </div>

    <hr class="border-tv-gray/30 my-8">

    <div class="flex flex-col md:flex-row justify-between items-center gap-4 text-xs">
      <p>&copy; <%= Date.current.year %> TeoVibe. All rights reserved.</p>
      <div class="flex gap-4">
        <%= link_to "이용약관", "#", class: "hover:text-white transition-colors" %>
        <%= link_to "개인정보처리방침", "#", class: "hover:text-white transition-colors" %>
      </div>
    </div>
  </div>
</footer>
