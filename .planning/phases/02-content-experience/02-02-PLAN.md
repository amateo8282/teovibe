---
phase: 02-content-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/Gemfile
  - teovibe/Gemfile.lock
  - teovibe/db/migrate/YYYYMMDDHHMMSS_add_social_links_to_users.rb
  - teovibe/db/schema.rb
  - teovibe/app/models/user.rb
  - teovibe/app/models/concerns/badgeable.rb
  - teovibe/app/controllers/profiles_controller.rb
  - teovibe/app/views/profiles/show.html.erb
  - teovibe/app/views/profiles/edit.html.erb
autonomous: true
requirements: [PROF-01, PROF-02]

must_haves:
  truths:
    - "프로필 페이지에 아바타 이미지가 표시된다 (Active Storage 첨부 또는 기존 avatar_url 폴백)"
    - "프로필 편집에서 아바타 이미지를 업로드할 수 있다"
    - "프로필 페이지에 바이오가 표시된다"
    - "프로필 페이지에 소셜링크(GitHub, Twitter, 웹사이트)가 표시된다"
    - "프로필 편집에서 소셜링크를 입력할 수 있다"
    - "프로필 페이지에 포인트, 레벨, 뱃지가 시각적으로 표시된다"
    - "작성 글 목록이 프로필 페이지에 표시된다"
  artifacts:
    - path: "teovibe/app/models/user.rb"
      provides: "Active Storage avatar 첨부 + Badgeable concern include"
      contains: "has_one_attached :avatar"
    - path: "teovibe/app/models/concerns/badgeable.rb"
      provides: "뱃지 정의 및 earned_badges 메서드"
      contains: "module Badgeable"
    - path: "teovibe/app/views/profiles/show.html.erb"
      provides: "아바타, 소셜링크, 뱃지, 글 목록 표시"
      contains: "earned_badges"
    - path: "teovibe/app/views/profiles/edit.html.erb"
      provides: "아바타 업로드, 소셜링크 입력 폼"
      contains: "file_field :avatar"
  key_links:
    - from: "teovibe/app/models/user.rb"
      to: "Active Storage"
      via: "has_one_attached :avatar"
      pattern: "has_one_attached :avatar"
    - from: "teovibe/app/controllers/profiles_controller.rb"
      to: "teovibe/app/models/user.rb"
      via: "profile_params permit"
      pattern: "permit.*:avatar.*:github_url"
---

<objective>
작성자 프로필 페이지를 강화하여 아바타 업로드, 소셜링크, 게이미피케이션(포인트/레벨/뱃지) 표시를 완성한다.

Purpose: 작성자 정체성과 활동 성과를 시각화하여 커뮤니티 참여 동기 부여
Output: 아바타, 소셜링크, 뱃지가 포함된 프로필 페이지 + 프로필 편집 폼
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-experience/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: User 모델 확장 (Active Storage 아바타 + 소셜링크 컬럼 + Badgeable)</name>
  <files>teovibe/db/migrate/YYYYMMDDHHMMSS_add_social_links_to_users.rb, teovibe/db/schema.rb, teovibe/app/models/user.rb, teovibe/app/models/concerns/badgeable.rb</files>
  <action>
1. 소셜링크 마이그레이션 생성 (3개 고정 컬럼 -- JSON 파싱 복잡도 회피):
   ```bash
   cd teovibe && bin/rails generate migration AddSocialLinksToUsers github_url:string twitter_url:string website_url:string
   ```
   마이그레이션 실행: `bin/rails db:migrate`

2. app/models/user.rb 수정:
   - `has_one_attached :avatar` 추가
   - `include Badgeable` 추가
   - `display_avatar_url` 헬퍼 메서드 추가:
     ```ruby
     def display_avatar_url(size: 80)
       if avatar.attached?
         avatar.variant(resize_to_fill: [size, size])
       elsif avatar_url.present?
         avatar_url  # 기존 URL 문자열 폴백 (OAuth 아바타 등)
       end
     end
     ```
   - 기존 avatar_url 컬럼은 유지 (폴백 용도, 삭제하지 않음 -- RESEARCH Pitfall 4 참조)

3. app/models/concerns/badgeable.rb 생성:
   ```ruby
   module Badgeable
     extend ActiveSupport::Concern

     BADGES = [
       { id: :newcomer,   label: "뉴비",     icon: "seedling",  condition: ->(u) { u.posts_count >= 1 } },
       { id: :writer,     label: "작가",     icon: "pencil",    condition: ->(u) { u.posts_count >= 10 } },
       { id: :veteran,    label: "베테랑",   icon: "star",      condition: ->(u) { u.level >= 5 } },
       { id: :popular,    label: "인기인",   icon: "heart",     condition: ->(u) { u.points >= 500 } },
     ].freeze

     def earned_badges
       BADGES.select { |badge| badge[:condition].call(self) }
     end
   end
   ```
   - 별도 gem 불필요 (4개 뱃지에 gem은 과도 -- RESEARCH Don't Hand-Roll 참조)
   - icon 필드는 뷰에서 아이콘 매핑에 사용 (선택사항)
  </action>
  <verify>
- `cd teovibe && bin/rails db:migrate:status` -- 마이그레이션 up 확인
- `bin/rails runner "puts User.column_names.include?('github_url')"` -- true
- `bin/rails runner "puts User.new.respond_to?(:avatar)"` -- true
- `bin/rails runner "puts User.new.respond_to?(:earned_badges)"` -- true
- `bin/rails runner "puts User.new(posts_count: 1).earned_badges.length"` -- 1 (newcomer)
  </verify>
  <done>User 모델에 Active Storage avatar 첨부, social_links 3개 컬럼(github_url, twitter_url, website_url), Badgeable concern(earned_badges 메서드)이 추가되었다</done>
</task>

<task type="auto">
  <name>Task 2: 프로필 페이지 뷰 및 컨트롤러 업데이트</name>
  <files>teovibe/app/controllers/profiles_controller.rb, teovibe/app/views/profiles/show.html.erb, teovibe/app/views/profiles/edit.html.erb</files>
  <action>
1. app/controllers/profiles_controller.rb 수정:
   - profile_params에 `:avatar`, `:github_url`, `:twitter_url`, `:website_url` 추가:
     ```ruby
     def profile_params
       params.require(:user).permit(:nickname, :bio, :avatar_url, :avatar, :github_url, :twitter_url, :website_url)
     end
     ```

2. app/views/profiles/show.html.erb 수정 (기존 레이아웃 구조 보존하며 확장):
   - 아바타 영역: 기존 이니셜 원형 대신 Active Storage 이미지 또는 폴백 표시
     ```erb
     <% if @user.avatar.attached? %>
       <%= image_tag @user.display_avatar_url(size: 80), class: "w-20 h-20 rounded-full object-cover" %>
     <% elsif @user.avatar_url.present? %>
       <%= image_tag @user.avatar_url, class: "w-20 h-20 rounded-full object-cover" %>
     <% else %>
       <div class="w-20 h-20 rounded-full bg-tv-gold flex items-center justify-center text-3xl font-black text-tv-black">
         <%= @user.nickname.first.upcase %>
       </div>
     <% end %>
     ```
   - 레벨/포인트 영역 아래에 뱃지 표시 추가:
     ```erb
     <% if @user.earned_badges.any? %>
       <div class="flex flex-wrap gap-2 mt-3">
         <% @user.earned_badges.each do |badge| %>
           <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-bold bg-tv-gold/20 text-tv-black border border-tv-gold/40">
             <%= badge[:label] %>
           </span>
         <% end %>
       </div>
     <% end %>
     ```
   - 소셜링크 표시 (바이오 아래):
     ```erb
     <div class="flex gap-4 mt-3">
       <% if @user.github_url.present? %>
         <%= link_to "GitHub", @user.github_url, target: "_blank", class: "text-sm text-tv-orange hover:underline" %>
       <% end %>
       <% if @user.twitter_url.present? %>
         <%= link_to "Twitter", @user.twitter_url, target: "_blank", class: "text-sm text-tv-orange hover:underline" %>
       <% end %>
       <% if @user.website_url.present? %>
         <%= link_to "웹사이트", @user.website_url, target: "_blank", class: "text-sm text-tv-orange hover:underline" %>
       <% end %>
     </div>
     ```

3. app/views/profiles/edit.html.erb 수정:
   - 아바타 업로드 필드 추가 (file_field :avatar with preview):
     ```erb
     <div>
       <label class="block text-sm font-bold mb-1">프로필 사진</label>
       <% if @user.avatar.attached? %>
         <%= image_tag @user.display_avatar_url(size: 80), class: "w-20 h-20 rounded-full object-cover mb-2" %>
       <% elsif @user.avatar_url.present? %>
         <%= image_tag @user.avatar_url, class: "w-20 h-20 rounded-full object-cover mb-2" %>
       <% end %>
       <%= f.file_field :avatar, accept: "image/*", class: "text-sm" %>
     </div>
     ```
   - 소셜링크 입력 필드 3개 추가 (github_url, twitter_url, website_url):
     각각 `f.url_field` 또는 `f.text_field`로 구현, placeholder에 예시 URL 포함
   - 기존 nickname, bio 필드는 유지
  </action>
  <verify>
- `grep -c "earned_badges" app/views/profiles/show.html.erb` -- 1 이상
- `grep -c "github_url" app/views/profiles/show.html.erb` -- 1 이상
- `grep -c "file_field :avatar" app/views/profiles/edit.html.erb` -- 1
- `grep -c "github_url" app/views/profiles/edit.html.erb` -- 1 이상
- `grep -c ":avatar" app/controllers/profiles_controller.rb` -- 1 이상
- `RAILS_ENV=production bundle exec rails assets:precompile` 성공
  </verify>
  <done>프로필 페이지에 아바타(Active Storage + URL 폴백), 소셜링크(GitHub/Twitter/웹사이트), 뱃지(earned_badges)가 표시되고, 편집 폼에서 아바타 업로드와 소셜링크 입력이 가능하다</done>
</task>

</tasks>

<verification>
- User 모델에 has_one_attached :avatar, include Badgeable 존재
- social_links 마이그레이션 완료 (github_url, twitter_url, website_url 컬럼)
- 프로필 show 페이지에 아바타, 뱃지, 소셜링크 섹션 존재
- 프로필 edit 페이지에 아바타 업로드, 소셜링크 입력 필드 존재
- profiles_controller에서 새 파라미터 permit
- `RAILS_ENV=production bundle exec rails assets:precompile` 성공
</verification>

<success_criteria>
- 프로필 페이지에 아바타, 바이오, 소셜링크, 포인트/레벨/뱃지, 작성 글 목록이 모두 표시된다
- 프로필 편집에서 아바타 업로드와 소셜링크 입력이 가능하다
- 기존 avatar_url 문자열이 폴백으로 동작한다
- earned_badges가 포인트/레벨/글 수 기반으로 정확히 계산된다
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-experience/02-02-SUMMARY.md`
</output>
