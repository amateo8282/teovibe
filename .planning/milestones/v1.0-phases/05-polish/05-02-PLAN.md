---
phase: 05-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/config/application.rb
  - teovibe/config/routes.rb
  - teovibe/app/controllers/errors_controller.rb
  - teovibe/app/views/layouts/error.html.erb
  - teovibe/app/views/errors/not_found.html.erb
  - teovibe/app/views/errors/internal_server_error.html.erb
  - teovibe/app/views/errors/unprocessable_entity.html.erb
autonomous: true
requirements:
  - UIUX-02

must_haves:
  truths:
    - "존재하지 않는 URL 접근 시 브랜드에 맞는 한글 404 페이지가 표시된다"
    - "서버 오류 발생 시 브랜드에 맞는 한글 500 페이지가 표시된다 (DB 쿼리 없이 안전하게)"
    - "에러 페이지에서 홈으로 돌아가기 링크가 동작한다"
    - "404 페이지는 application 레이아웃(navbar 포함)으로 렌더링된다"
    - "500 페이지는 별도 error 레이아웃(navbar 없이 최소 의존성)으로 렌더링된다"
  artifacts:
    - path: "teovibe/app/controllers/errors_controller.rb"
      provides: "커스텀 에러 페이지 렌더링 컨트롤러"
      contains: "not_found"
    - path: "teovibe/app/views/errors/not_found.html.erb"
      provides: "커스텀 404 에러 뷰"
      contains: "404"
    - path: "teovibe/app/views/errors/internal_server_error.html.erb"
      provides: "커스텀 500 에러 뷰"
      contains: "500"
    - path: "teovibe/app/views/errors/unprocessable_entity.html.erb"
      provides: "커스텀 422 에러 뷰"
      contains: "422"
    - path: "teovibe/app/views/layouts/error.html.erb"
      provides: "500 에러 전용 최소 레이아웃 (DB 쿼리 방지)"
      contains: "vite_javascript_tag"
  key_links:
    - from: "teovibe/config/application.rb"
      to: "teovibe/config/routes.rb"
      via: "config.exceptions_app = self.routes"
      pattern: "exceptions_app.*routes"
    - from: "teovibe/config/routes.rb"
      to: "teovibe/app/controllers/errors_controller.rb"
      via: "match '/404' => errors#not_found"
      pattern: "errors#not_found"
    - from: "teovibe/app/controllers/errors_controller.rb"
      to: "teovibe/app/views/errors/"
      via: "render status:"
      pattern: "render status:"
---

<objective>
커스텀 404/500/422 에러 페이지를 구현한다: Rails exceptions_app 패턴으로 브랜드에 맞는 한글 에러 페이지를 제공한다. 500은 DB 의존 없는 별도 레이아웃으로 안전하게 렌더링한다.

Purpose: 기본 Rails 영문 에러 페이지를 브랜드 일관성 있는 한글 에러 페이지로 교체하여 프로덕션 완성도를 달성한다.
Output: ErrorsController, 에러 뷰 3개, error 레이아웃, config/routes 에러 라우트
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ErrorsController + 에러 라우트 + exceptions_app 설정</name>
  <files>teovibe/config/application.rb, teovibe/config/routes.rb, teovibe/app/controllers/errors_controller.rb, teovibe/app/views/layouts/error.html.erb</files>
  <action>
**1. config/application.rb 수정:**

`config.active_record.schema_format = :sql` 줄 아래에 다음 추가:
```ruby
# 커스텀 에러 페이지: Rails 라우터로 에러 처리
config.exceptions_app = self.routes
```

**2. config/routes.rb 수정:**

`root "pages#home"` 줄 바로 위에 에러 라우트 3개 추가:
```ruby
# 커스텀 에러 페이지
match "/404", to: "errors#not_found", via: :all
match "/500", to: "errors#internal_server_error", via: :all
match "/422", to: "errors#unprocessable_entity", via: :all
```

**3. ErrorsController 생성 (app/controllers/errors_controller.rb):**

```ruby
# 커스텀 에러 페이지 컨트롤러
# ActionController::Base 직접 상속 — DB 에러 시에도 안전하게 렌더링
class ErrorsController < ActionController::Base
  layout "application"

  def not_found
    render status: :not_found
  end

  def internal_server_error
    # 500은 DB가 다운된 상황일 수 있으므로 navbar DB 쿼리를 피하기 위해 별도 레이아웃 사용
    render status: :internal_server_error, layout: "error"
  end

  def unprocessable_entity
    render status: :unprocessable_entity
  end
end
```

핵심: ApplicationController 대신 ActionController::Base를 직접 상속한다. 이유:
- ApplicationController에 include Authentication이 있어 세션/DB 의존성 발생
- allow_browser 등 미들웨어가 에러 페이지에서 무한 루프 유발 가능
- 404/422는 application 레이아웃 사용 (navbar의 Current.user 조건부 렌더링은 nil-safe하므로 대부분 안전)
- 500은 error 레이아웃 사용 (navbar 없이 최소 렌더링)

**4. error 레이아웃 생성 (app/views/layouts/error.html.erb):**

500 에러 전용 최소 레이아웃. navbar/footer 없이 Vite CSS/JS만 로드:
```erb
<!DOCTYPE html>
<html lang="ko">
  <head>
    <title>TeoVibe</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css" />
    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application' %>
  </head>
  <body class="bg-tv-cream text-tv-black min-h-screen flex items-center justify-center">
    <%= yield %>
  </body>
</html>
```

주의: error 레이아웃에서 vite_react_refresh_tag는 생략한다 (에러 페이지에서 React 불필요). Pretendard 폰트 CDN 링크는 application 레이아웃과 동일하게 유지한다.
  </action>
  <verify>
`grep "exceptions_app" teovibe/config/application.rb` — 설정 존재 확인. `grep "errors#not_found" teovibe/config/routes.rb` — 에러 라우트 존재 확인. `test -f teovibe/app/controllers/errors_controller.rb` — 컨트롤러 파일 존재 확인. `test -f teovibe/app/views/layouts/error.html.erb` — error 레이아웃 존재 확인. `grep "ActionController::Base" teovibe/app/controllers/errors_controller.rb` — Base 직접 상속 확인.
  </verify>
  <done>config.exceptions_app이 설정되고, 에러 라우트 3개가 등록되고, ErrorsController가 ActionController::Base를 상속하며, 500용 error 레이아웃이 존재한다.</done>
</task>

<task type="auto">
  <name>Task 2: 커스텀 에러 뷰 3개 (404, 500, 422) 브랜드 스타일링</name>
  <files>teovibe/app/views/errors/not_found.html.erb, teovibe/app/views/errors/internal_server_error.html.erb, teovibe/app/views/errors/unprocessable_entity.html.erb</files>
  <action>
app/views/errors/ 디렉토리를 생성하고 에러 뷰 3개를 작성한다.

**1. not_found.html.erb (404):**

application 레이아웃으로 렌더링되므로 navbar가 표시됨. 메인 콘텐츠 영역에 배치:
```erb
<div class="max-w-[600px] mx-auto px-5 py-20 text-center" style="margin-top: 86px;">
  <div class="text-[120px] md:text-[160px] font-black text-tv-gold leading-none mb-6">404</div>
  <h1 class="text-2xl md:text-4xl font-black mb-4">페이지를 찾을 수 없습니다</h1>
  <p class="text-tv-gray text-base md:text-lg mb-10">
    요청하신 페이지가 존재하지 않거나 이동되었습니다.
  </p>
  <a href="/" class="inline-block bg-tv-gold text-tv-black rounded-pill px-8 py-3 text-lg font-bold hover:opacity-90 transition-opacity">
    홈으로 돌아가기
  </a>
</div>
```

주의: ErrorsController는 ActionController::Base를 상속하므로 link_to, root_path 등 Rails URL 헬퍼를 사용할 수 없을 수 있다. 안전하게 `<a href="/">` HTML 태그를 직접 사용한다. 단, `url_helpers`를 include하면 사용 가능하지만, 에러 페이지에서는 의존성을 최소화하는 것이 원칙이므로 직접 HTML 링크를 쓴다.

**2. internal_server_error.html.erb (500):**

error 레이아웃으로 렌더링되므로 navbar 없음. body가 flex center이므로 중앙 정렬:
```erb
<div class="max-w-[600px] mx-auto px-5 py-20 text-center">
  <div class="text-[120px] md:text-[160px] font-black text-tv-burgundy leading-none mb-6">500</div>
  <h1 class="text-2xl md:text-4xl font-black mb-4">서버 오류가 발생했습니다</h1>
  <p class="text-tv-gray text-base md:text-lg mb-10">
    일시적인 문제가 발생했습니다. 잠시 후 다시 시도해주세요.
  </p>
  <a href="/" class="inline-block bg-tv-gold text-tv-black rounded-pill px-8 py-3 text-lg font-bold hover:opacity-90 transition-opacity">
    홈으로 돌아가기
  </a>
</div>
```

**3. unprocessable_entity.html.erb (422):**

application 레이아웃으로 렌더링. CSRF 토큰 만료 등에서 발생:
```erb
<div class="max-w-[600px] mx-auto px-5 py-20 text-center" style="margin-top: 86px;">
  <div class="text-[120px] md:text-[160px] font-black text-tv-orange leading-none mb-6">422</div>
  <h1 class="text-2xl md:text-4xl font-black mb-4">요청을 처리할 수 없습니다</h1>
  <p class="text-tv-gray text-base md:text-lg mb-10">
    세션이 만료되었거나 잘못된 요청입니다. 페이지를 새로고침 후 다시 시도해주세요.
  </p>
  <a href="/" class="inline-block bg-tv-gold text-tv-black rounded-pill px-8 py-3 text-lg font-bold hover:opacity-90 transition-opacity">
    홈으로 돌아가기
  </a>
</div>
```

색상 체계: 404은 tv-gold (경고/안내), 500은 tv-burgundy (심각), 422는 tv-orange (주의). 프로젝트 기존 @theme 변수를 활용한다.

margin-top: 86px는 fixed navbar 높이(h-[86px])에 맞춘 것. 500은 error 레이아웃(navbar 없음)이므로 margin-top 불필요.
  </action>
  <verify>
`test -d teovibe/app/views/errors` — errors 뷰 디렉토리 존재 확인. `ls teovibe/app/views/errors/` — 3개 뷰 파일 존재 확인. 각 파일에 `href="/"` 홈 링크가 있는지 확인. `grep "tv-burgundy" teovibe/app/views/errors/internal_server_error.html.erb` — 500 고유 색상 확인.
  </verify>
  <done>404 (tv-gold), 500 (tv-burgundy), 422 (tv-orange) 에러 뷰가 각각 브랜드 색상으로 스타일링되어 존재한다. 404/422는 application 레이아웃(navbar 포함), 500은 error 레이아웃(navbar 없음)으로 렌더링된다.</done>
</task>

</tasks>

<verification>
1. `bin/rails routes | grep errors` — 에러 라우트 3개 등록 확인
2. 개발 환경에서 `config.consider_all_requests_local = false`로 임시 변경 후 존재하지 않는 URL 접근 — 커스텀 404 페이지 확인
3. ErrorsController가 ActionController::Base를 상속하는지 확인 (ApplicationController 아님)
4. 500 에러 뷰가 error 레이아웃을 사용하는지 확인 (navbar DB 쿼리 방지)
</verification>

<success_criteria>
- exceptions_app 설정으로 Rails가 커스텀 에러 페이지를 렌더링
- 404/422는 application 레이아웃(navbar 포함)으로 브랜드에 맞게 표시
- 500은 별도 error 레이아웃(navbar 없음)으로 DB 의존 없이 안전하게 표시
- 모든 에러 페이지에 홈 링크가 동작
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-02-SUMMARY.md`
</output>
