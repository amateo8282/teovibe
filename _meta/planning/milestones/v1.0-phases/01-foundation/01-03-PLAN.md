---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - Gemfile
  - app/components/application_component.rb
  - app/components/card_component.rb
  - app/components/card_component.html.erb
  - app/frontend/entrypoints/react-demo.jsx
  - app/frontend/components/ReactDemo.tsx
  - config/routes.rb
  - app/controllers/demo_controller.rb
  - app/views/demo/react.html.erb
autonomous: true
requirements:
  - INFRA-03
  - INFRA-01

must_haves:
  truths:
    - "ViewComponent로 만든 CardComponent가 ERB 뷰에서 렌더링된다"
    - "React 컴포넌트가 전용 페이지에서 마운트되어 브라우저에 표시된다"
    - "React 전용 페이지에서도 Turbo Drive 네비게이션이 유지된다"
    - "React 컴포넌트가 Turbo 페이지 전환 시 언마운트/재마운트된다"
  artifacts:
    - path: "app/components/application_component.rb"
      provides: "ViewComponent 베이스 클래스"
      contains: "ViewComponent::Base"
    - path: "app/components/card_component.rb"
      provides: "첫 ViewComponent (CardComponent)"
      contains: "CardComponent"
    - path: "app/frontend/components/ReactDemo.tsx"
      provides: "React 데모 컴포넌트"
      contains: "ReactDemo"
    - path: "app/frontend/entrypoints/react-demo.jsx"
      provides: "React 전용 페이지 진입점 (Turbo 이벤트 기반 마운트)"
      contains: "turbo:load"
  key_links:
    - from: "app/views/demo/react.html.erb"
      to: "app/frontend/entrypoints/react-demo.jsx"
      via: "vite_javascript_tag 'react-demo'"
      pattern: "vite_javascript_tag.*react-demo"
    - from: "app/frontend/entrypoints/react-demo.jsx"
      to: "app/frontend/components/ReactDemo.tsx"
      via: "import ReactDemo"
      pattern: "import.*ReactDemo"
    - from: "app/frontend/entrypoints/react-demo.jsx"
      to: "document turbo:load event"
      via: "addEventListener turbo:load + turbo:before-cache"
      pattern: "turbo:before-cache"
---

<objective>
ViewComponent gem을 도입하여 재사용 가능한 UI 컴포넌트 구조를 마련하고, React 데모 컴포넌트를 전용 페이지에 마운트하여 vite_ruby를 통한 JSX/React 동작을 검증한다.

Purpose: ViewComponent 기반 서버사이드 UI 컴포넌트 체계 확립 + React 전용 페이지 마운트 패턴 검증
Output: CardComponent(ViewComponent), ReactDemo(React) 동작 확인
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

사용자 결정 (CONTEXT.md):
- React 마운트 패턴: 전용 페이지 마운트 방식 (Island 방식 사용 안 함)
- 데이터 전달: JSON API 호출 방식 (data-* props 전달 안 함)
- Turbo Drive 네비게이션 유지 (data-turbo=false 사용 안 함)
- turbo:load + turbo:before-cache 이벤트 기반 마운트/언마운트
</context>

<tasks>

<task type="auto">
  <name>Task 1: ViewComponent 설치 및 첫 컴포넌트 생성</name>
  <files>
    Gemfile
    app/components/application_component.rb
    app/components/card_component.rb
    app/components/card_component.html.erb
  </files>
  <action>
1. ViewComponent gem 설치:
   - Gemfile에 `gem "view_component", "~> 4.4"` 추가
   - `bundle install` 실행

2. ApplicationComponent 베이스 클래스 생성:
   ```ruby
   # app/components/application_component.rb
   class ApplicationComponent < ViewComponent::Base
   end
   ```

3. CardComponent 생성 (제너레이터 또는 수동):
   ```ruby
   # app/components/card_component.rb
   class CardComponent < ApplicationComponent
     def initialize(title:, body: nil)
       @title = title
       @body = body
     end
   end
   ```

   ```erb
   <%# app/components/card_component.html.erb %>
   <div class="bg-white rounded-lg shadow-md p-6">
     <h2 class="font-bold text-lg"><%= @title %></h2>
     <% if @body %>
       <p class="mt-2 text-gray-600"><%= @body %></p>
     <% end %>
   </div>
   ```
   - 주의: Tailwind CSS 클래스는 프로젝트에서 사용 중인 커스텀 토큰(bg-tv-white 등)이 있으면 해당 클래스 사용. 없으면 기본 Tailwind 클래스 사용. 기존 뷰 파일의 클래스 네이밍을 참고할 것.

4. 기존 뷰에서 CardComponent 렌더링 확인:
   - 임시로 홈페이지(`app/views/pages/home.html.erb`) 또는 적절한 뷰에 삽입:
     ```erb
     <%= render(CardComponent.new(title: "ViewComponent 테스트", body: "첫 ViewComponent가 정상 렌더링됩니다.")) %>
     ```
   - 브라우저에서 확인 후, 검증이 끝나면 임시 삽입 코드는 제거하거나 유지 (Claude 재량)
  </action>
  <verify>
    - `bundle exec ruby -e "require 'view_component'; puts ViewComponent::VERSION::STRING"` 출력: 4.x.x
    - `ls app/components/card_component.rb app/components/card_component.html.erb` 파일 존재
    - `bin/rails runner "puts CardComponent.new(title: 'test').render_in(ActionController::Base.new.view_context)"` 실행 시 HTML 출력
  </verify>
  <done>
    ViewComponent 4.x가 설치되고, ApplicationComponent 베이스 클래스와 CardComponent가 생성되어 ERB에서 정상 렌더링된다.
  </done>
</task>

<task type="auto">
  <name>Task 2: React 데모 컴포넌트 전용 페이지 마운트 (Turbo 지원)</name>
  <files>
    app/frontend/entrypoints/react-demo.jsx
    app/frontend/components/ReactDemo.tsx
    config/routes.rb
    app/controllers/demo_controller.rb
    app/views/demo/react.html.erb
  </files>
  <action>
1. React 데모 컴포넌트 생성:
   ```tsx
   // app/frontend/components/ReactDemo.tsx
   import { useState, useEffect } from "react"

   export default function ReactDemo() {
     const [count, setCount] = useState(0)
     const [mounted, setMounted] = useState(false)

     useEffect(() => {
       setMounted(true)
       return () => setMounted(false)
     }, [])

     return (
       <div style={{ padding: "2rem", fontFamily: "sans-serif" }}>
         <h1>React 데모 컴포넌트</h1>
         <p>마운트 상태: {mounted ? "활성" : "비활성"}</p>
         <p>카운터: {count}</p>
         <button
           onClick={() => setCount(c => c + 1)}
           style={{
             padding: "0.5rem 1rem",
             backgroundColor: "#3b82f6",
             color: "white",
             borderRadius: "0.375rem",
             border: "none",
             cursor: "pointer",
           }}
         >
           증가
         </button>
       </div>
     )
   }
   ```

2. React 전용 페이지 진입점 생성 (Turbo 이벤트 기반, CONTEXT.md 결정 사항):
   ```jsx
   // app/frontend/entrypoints/react-demo.jsx
   import { createRoot } from "react-dom/client"
   import ReactDemo from "../components/ReactDemo"

   let root = null

   // Turbo Drive 페이지 진입 시 마운트
   document.addEventListener("turbo:load", () => {
     const el = document.getElementById("react-root")
     if (el && !root) {
       root = createRoot(el)
       root.render(<ReactDemo />)
     }
   })

   // Turbo Drive 페이지 이탈 전 언마운트 (캐시 오염 방지)
   document.addEventListener("turbo:before-cache", () => {
     if (root) {
       root.unmount()
       root = null
     }
   })
   ```
   - 주의: turbo:before-render가 아닌 turbo:before-cache를 사용 (RESEARCH.md 권장)
   - root 레퍼런스를 module 스코프에 저장하여 중복 마운트 방지

3. Rails 라우트 및 컨트롤러 생성:
   ```ruby
   # config/routes.rb에 추가
   get "demo/react", to: "demo#react"
   ```

   ```ruby
   # app/controllers/demo_controller.rb
   class DemoController < ApplicationController
     allow_unauthenticated_access
     def react; end
   end
   ```

4. ERB 뷰 생성:
   ```erb
   <%# app/views/demo/react.html.erb %>
   <div class="container mx-auto py-8">
     <h1 class="text-2xl font-bold mb-4">React 데모 페이지</h1>
     <p class="mb-4">이 페이지는 React 컴포넌트가 전용 페이지에 마운트되는 패턴을 검증합니다.</p>

     <%# ViewComponent 렌더링 확인 %>
     <%= render(CardComponent.new(title: "ViewComponent", body: "서버사이드 렌더링 컴포넌트")) %>

     <%# React 마운트 포인트 %>
     <div id="react-root" class="mt-8"></div>
   </div>

   <% content_for :head do %>
     <%= vite_javascript_tag 'react-demo' %>
   <% end %>
   ```
   - 주의: layout에 `<%= yield :head %>` 또는 `<%= content_for?(:head) ? yield(:head) : '' %>`가 head 태그 안에 있어야 함
   - 없으면 layout에 추가

5. Layout에 content_for :head 지원 확인:
   - `app/views/layouts/application.html.erb`의 `<head>` 안에 `<%= yield :head %>` 추가 (없는 경우)

6. 검증:
   - `bin/dev` 기동
   - http://localhost:3000/demo/react 접속
   - React 컴포넌트 렌더링 확인 (카운터, 버튼 동작)
   - ViewComponent CardComponent 렌더링 확인
   - 다른 페이지로 이동 후 뒤로가기하여 React 재마운트 확인
   - 브라우저 콘솔에 createRoot 경고 없음 확인
  </action>
  <verify>
    - http://localhost:3000/demo/react 접속 시 200 응답
    - React 컴포넌트 "React 데모 컴포넌트" 텍스트가 표시됨
    - "증가" 버튼 클릭 시 카운터 증가
    - ViewComponent "ViewComponent" 카드가 표시됨
    - 브라우저 콘솔에 에러 없음 (특히 createRoot 중복 경고 없음)
    - 다른 페이지 이동 후 뒤로가기 시 React 컴포넌트 재마운트 정상
  </verify>
  <done>
    React 데모 컴포넌트가 /demo/react 전용 페이지에서 Turbo Drive 네비게이션을 유지한 채 마운트/언마운트된다. ViewComponent CardComponent와 React 컴포넌트가 같은 페이지에서 공존한다. JSX 파일을 작성하고 브라우저에서 React 컴포넌트가 마운트됨을 확인할 수 있다.
  </done>
</task>

</tasks>

<verification>
1. ViewComponent CardComponent가 ERB에서 `render(CardComponent.new(title: "..."))` 호출로 렌더링됨
2. /demo/react 페이지에서 React 컴포넌트가 정상 마운트 (카운터, 버튼 동작)
3. Turbo Drive 네비게이션으로 /demo/react 진입/이탈 시 마운트/언마운트 정상
4. 브라우저 콘솔에 JavaScript 에러 없음
5. bin/dev 실행 시 Vite HMR과 함께 React Fast Refresh 동작
</verification>

<success_criteria>
- ViewComponent가 설치되어 CardComponent가 ERB 뷰에서 렌더링된다
- React 컴포넌트가 전용 페이지(/demo/react)에서 마운트되어 인터랙션이 동작한다
- turbo:load/turbo:before-cache 이벤트 기반으로 React가 Turbo Drive와 공존한다
- JSX/TSX 파일이 Vite를 통해 빌드되어 브라우저에서 실행된다
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
