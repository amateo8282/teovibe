---
phase: 11-소셜-색인-메타태그
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/app/controllers/posts_controller.rb
  - teovibe/app/controllers/pages_controller.rb
  - teovibe/test/integration/og_meta_tags_test.rb
autonomous: true
requirements: [SOCL-01, SOCL-02, SOCL-03, INDX-01]

must_haves:
  truths:
    - "게시글 URL을 소셜에 공유하면 og:title, og:description, og:image가 포함된 미리보기 카드가 표시된다"
    - "게시글 상세 페이지에 twitter:card, twitter:title, twitter:description 메타태그가 출력된다"
    - "홈/목록 페이지에 사이트 기본 OG 메타태그가 출력된다"
    - "게시글 상세 페이지에 쿼리 파라미터 없는 canonical URL이 출력된다"
  artifacts:
    - path: "teovibe/app/controllers/posts_controller.rb"
      provides: "show/index 액션에 set_meta_tags 호출"
      contains: "set_meta_tags"
    - path: "teovibe/app/controllers/pages_controller.rb"
      provides: "home 액션에 기본 OG set_meta_tags 호출"
      contains: "set_meta_tags"
    - path: "teovibe/test/integration/og_meta_tags_test.rb"
      provides: "OG/Twitter/canonical 메타태그 통합 테스트"
      min_lines: 30
  key_links:
    - from: "teovibe/app/controllers/posts_controller.rb"
      to: "display_meta_tags in application.html.erb"
      via: "set_meta_tags in show action"
      pattern: "set_meta_tags.*og:"
    - from: "teovibe/app/controllers/pages_controller.rb"
      to: "display_meta_tags in application.html.erb"
      via: "set_meta_tags in home action"
      pattern: "set_meta_tags.*og:"
---

<objective>
게시글/홈/목록 페이지에 Open Graph, Twitter Card, canonical URL 메타태그를 설정하여 소셜 공유 미리보기와 검색 색인 제어를 완성한다.

Purpose: 카카오톡/Discord 등에서 URL 공유 시 올바른 미리보기 카드가 표시되고, 검색엔진이 중복 URL을 canonical로 통합한다.
Output: PostsController, PagesController에 set_meta_tags 호출 추가 + 통합 테스트
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-소셜-색인-메타태그/11-CONTEXT.md
@.planning/phases/10-크롤링-기초/10-02-SUMMARY.md

<interfaces>
<!-- meta-tags gem API: 컨트롤러에서 set_meta_tags 호출하면 레이아웃의 display_meta_tags가 자동 렌더링 -->

From teovibe/app/views/layouts/application.html.erb (line 11):
```erb
<%= display_meta_tags site: "TeoVibe" %>
```

From teovibe/app/controllers/posts_controller.rb:
```ruby
class PostsController < ApplicationController
  allow_unauthenticated_access only: %i[index show]
  before_action :set_post, only: %i[show edit update destroy]
  # show 액션에서 @post 사용 가능
  # index 액션에서 @category 사용 가능
end
```

From teovibe/app/controllers/pages_controller.rb:
```ruby
class PagesController < ApplicationController
  allow_unauthenticated_access
  def home; end
end
```

OG 이미지 기본 파일: teovibe/public/icon.png (이미 존재)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 게시글 OG/Twitter/canonical 메타태그 (TDD)</name>
  <files>teovibe/app/controllers/posts_controller.rb, teovibe/test/integration/og_meta_tags_test.rb</files>
  <behavior>
    - Test 1: GET /posts/:slug 응답 HTML에 og:title이 게시글 제목과 일치
    - Test 2: GET /posts/:slug 응답 HTML에 og:description이 본문 앞 150자 이내 (태그 제거)
    - Test 3: GET /posts/:slug 응답 HTML에 og:url이 쿼리 파라미터 없는 절대 URL
    - Test 4: GET /posts/:slug 응답 HTML에 og:image가 존재 (기본 icon.png)
    - Test 5: GET /posts/:slug 응답 HTML에 twitter:card=summary, twitter:title 존재
    - Test 6: GET /posts/:slug 응답 HTML에 link rel="canonical"이 쿼리 파라미터 없는 절대 URL
    - Test 7: GET /posts/:category_slug 목록 응답 HTML에 og:title이 카테고리 이름 포함
  </behavior>
  <action>
    1. teovibe/test/integration/og_meta_tags_test.rb 작성:
       - OgMetaTagsTest 클래스, fixtures(:posts, :categories) 로드
       - 게시글 show 페이지 OG/Twitter/canonical 검증 (assert_select "meta[property='og:title']" 등)
       - 카테고리 목록 페이지 OG 검증
       - 테스트 실행하여 RED 확인

    2. PostsController#show에 set_meta_tags 추가:
       ```ruby
       set_meta_tags(
         title: @post.title,
         og: {
           title: @post.title,
           description: strip_tags(@post.body).truncate(150),
           url: post_url(@post),
           image: helpers.asset_url("icon.png"),
           type: "article"
         },
         twitter: {
           card: "summary",
           title: @post.title,
           description: strip_tags(@post.body).truncate(150)
         },
         canonical: post_url(@post)
       )
       ```

    3. PostsController#index에 set_meta_tags 추가:
       ```ruby
       set_meta_tags(
         title: @category.name,
         og: {
           title: "#{@category.name} - TeoVibe",
           description: "TeoVibe #{@category.name} 게시판",
           url: request.original_url.split("?").first,
           image: helpers.asset_url("icon.png"),
           type: "website"
         }
       )
       ```

    4. 테스트 실행하여 GREEN 확인

    주의: strip_tags는 ActionView::Helpers::SanitizeHelper에서 제공. 컨트롤러에서 사용하려면 helpers.strip_tags 또는 include ActionView::Helpers::SanitizeHelper. helpers.strip_tags를 사용할 것.
    주의: og:image URL은 절대 경로 필수. helpers.asset_url("icon.png") 또는 "#{request.base_url}/icon.png" 사용.
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test test/integration/og_meta_tags_test.rb</automated>
  </verify>
  <done>게시글 show에 og:title/description/url/image + twitter:card/title/description + canonical 출력, 목록에 카테고리 OG 출력, 통합 테스트 전부 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 홈페이지 기본 OG 메타태그 (TDD)</name>
  <files>teovibe/app/controllers/pages_controller.rb, teovibe/test/integration/og_meta_tags_test.rb</files>
  <behavior>
    - Test 1: GET / 응답 HTML에 og:title="TeoVibe - 바이브코딩 커뮤니티"
    - Test 2: GET / 응답 HTML에 og:description이 존재
    - Test 3: GET / 응답 HTML에 og:image가 존재
    - Test 4: GET / 응답 HTML에 og:type="website"
  </behavior>
  <action>
    1. og_meta_tags_test.rb에 홈페이지 테스트 추가 (같은 파일에 별도 테스트 메서드):
       - test_home_page_has_default_og_tags
       - RED 확인

    2. PagesController#home에 set_meta_tags 추가:
       ```ruby
       def home
         @sections = LandingSection.active.ordered.includes(:section_cards)
         set_meta_tags(
           og: {
             title: "TeoVibe - 바이브코딩 커뮤니티",
             description: "AI와 함께 만드는 바이브코딩 커뮤니티",
             url: root_url,
             image: "#{request.base_url}/icon.png",
             type: "website"
           }
         )
       end
       ```

    3. GREEN 확인
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test test/integration/og_meta_tags_test.rb</automated>
  </verify>
  <done>홈페이지에 사이트 기본 og:title/description/image/type 출력, 테스트 통과</done>
</task>

</tasks>

<verification>
cd teovibe && bin/rails test test/integration/og_meta_tags_test.rb -v
</verification>

<success_criteria>
- 게시글 상세 페이지 HTML에 og:title, og:description, og:url, og:image, twitter:card, twitter:title, canonical 메타태그가 모두 출력된다
- 카테고리 목록 페이지 HTML에 og:title, og:image가 출력된다
- 홈페이지 HTML에 og:title, og:description, og:image, og:type이 출력된다
- 통합 테스트 전부 통과
</success_criteria>

<output>
After completion, create `.planning/phases/11-소셜-색인-메타태그/11-01-SUMMARY.md`
</output>
