---
phase: 15-툴바-서식-확장
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/app/frontend/editor/admin_rhino_editor.js
  - teovibe/config/initializers/action_text.rb
  - teovibe/package.json
  - teovibe/pnpm-lock.yaml
autonomous: false
requirements: [MARK-01, MARK-02, MARK-03, MARK-04, MARK-05, MARK-06]

must_haves:
  truths:
    - "툴바에서 취소선 버튼을 클릭하면 선택 텍스트에 <del> 태그가 적용된다"
    - "툴바에서 밑줄 버튼을 클릭하면 선택 텍스트에 <u> 태그가 적용되고 저장 후에도 유지된다"
    - "툴바에서 인용구 버튼을 클릭하면 블록이 <blockquote>로 변환된다"
    - "툴바에서 구분선 버튼을 클릭하면 <hr> 태그가 삽입된다"
    - "툴바에서 코드블록 버튼을 클릭하면 블록이 <pre><code>로 변환된다"
    - "제목 드롭다운에서 H1/H2/H3을 선택하면 커서 블록이 해당 제목 레벨로 변환된다"
  artifacts:
    - path: "teovibe/app/frontend/editor/admin_rhino_editor.js"
      provides: "AdminRhinoEditor with Underline extension + renderToolbarEnd override"
      contains: "renderToolbarEnd"
    - path: "teovibe/config/initializers/action_text.rb"
      provides: "ActionText <u> tag allowlist"
      contains: "u"
  key_links:
    - from: "teovibe/app/frontend/editor/admin_rhino_editor.js"
      to: "@tiptap/extension-underline"
      via: "import + addExtensions()"
      pattern: "import Underline from.*extension-underline"
    - from: "teovibe/app/frontend/editor/admin_rhino_editor.js"
      to: "TipTap Editor commands"
      via: "this.editor?.chain().focus()"
      pattern: "this\\.editor\\?"
    - from: "teovibe/config/initializers/action_text.rb"
      to: "ActionText sanitizer"
      via: "allowed_tags += %w[u]"
      pattern: "allowed_tags.*\\bu\\b"
---

<objective>
Admin 에디터 툴바에 밑줄/구분선/제목 드롭다운 버튼을 추가하고, 기존 Strike/Blockquote/CodeBlock 버튼이 정상 동작하는지 확인한다.

Purpose: 작성자가 풍부한 서식(취소선, 밑줄, 인용구, 구분선, 코드블록, H1-H3)을 적용할 수 있도록 한다.
Output: AdminRhinoEditor에 renderToolbarEnd() 오버라이드 + Underline extension 등록 + ActionText <u> 허용목록 추가
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-에디터-기반-설정/14-01-SUMMARY.md
@.planning/phases/15-툴바-서식-확장/15-RESEARCH.md

<interfaces>
<!-- Phase 14에서 생성된 파일 — 이번 플랜에서 수정할 대상 -->

From teovibe/app/frontend/editor/admin_rhino_editor.js:
```javascript
import { TipTapEditor } from "rhino-editor/exports/elements/tip-tap-editor.js"

export class AdminRhinoEditor extends TipTapEditor {
  // Phase 14: 스캐폴드 단계
}

AdminRhinoEditor.define("admin-rhino-editor")
```

From teovibe/config/initializers/action_text.rb:
```ruby
Rails.application.config.after_initialize do
  sanitizer_class = ActionText::ContentHelper.sanitizer.class
  ActionText::ContentHelper.allowed_tags ||=
    sanitizer_class.allowed_tags + [ActionText::Attachment.tag_name, "figure", "figcaption"]
  ActionText::ContentHelper.allowed_attributes ||=
    sanitizer_class.allowed_attributes + ActionText::Attachment::ATTRIBUTES
  ActionText::ContentHelper.allowed_tags += %w[table thead tbody tfoot tr th td colgroup col caption]
  ActionText::ContentHelper.allowed_attributes += ["style", "colspan", "rowspan", "scope"]
end
```

<!-- TipTapEditor API (from rhino-editor d.ts 직접 확인) -->
- `addExtensions(...extensions)` — 중복 필터링 후 extension 추가
- `renderToolbarEnd()` — 기본 빈 TemplateResult, override하면 toolbar 끝에 버튼 추가
- `this.editor?.isActive("heading", { level: N })` — TipTap isActive API
- `this.editor?.chain().focus().setHorizontalRule().run()` — HR 삽입
- `this.editor?.chain().focus().toggleUnderline().run()` — 밑줄 토글
- `this.editor?.chain().focus().toggleHeading({ level }).run()` — 제목 토글
- `this.editor?.chain().focus().setParagraph().run()` — 단락으로 변환

<!-- 기존 toolbar에 이미 렌더링 중인 버튼 (코드 추가 불필요) -->
- Strike: renderStrikeButton() — rhinoStrike(CustomStrike) 기본 등록
- Blockquote: renderBlockquoteButton() — StarterKit 기본 포함
- CodeBlock: renderCodeBlockButton() — StarterKit 기본 포함
- Heading (단일 토글): renderHeadingButton() — 단일 레벨만 지원
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Underline extension 설치 + renderToolbarEnd 오버라이드 + ActionText 허용목록</name>
  <files>
    teovibe/app/frontend/editor/admin_rhino_editor.js,
    teovibe/config/initializers/action_text.rb,
    teovibe/package.json,
    teovibe/pnpm-lock.yaml
  </files>
  <action>
    1. `cd teovibe && pnpm add @tiptap/extension-underline@^2.27.2` 실행하여 Underline extension 설치. 반드시 ^2.27.2 버전 핀 (v3 설치 방지 — rhino-editor가 TipTap v2 기반).

    2. `teovibe/app/frontend/editor/admin_rhino_editor.js` 를 다음과 같이 수정:

    ```javascript
    import { TipTapEditor } from "rhino-editor/exports/elements/tip-tap-editor.js"
    import Underline from "@tiptap/extension-underline"
    import { html } from "lit"

    export class AdminRhinoEditor extends TipTapEditor {
      connectedCallback() {
        super.connectedCallback()
        this.addExtensions(Underline)
      }

      // MARK-06: 제목 드롭다운 (H1/H2/H3 + 단락)
      renderHeadingDropdown() {
        const isH1 = Boolean(this.editor?.isActive("heading", { level: 1 }))
        const isH2 = Boolean(this.editor?.isActive("heading", { level: 2 }))
        const isH3 = Boolean(this.editor?.isActive("heading", { level: 3 }))

        let currentValue = "0"
        if (isH1) currentValue = "1"
        else if (isH2) currentValue = "2"
        else if (isH3) currentValue = "3"

        return html`
          <select
            class="toolbar__button rhino-toolbar-button"
            aria-label="제목 레벨"
            data-role="toolbar-item"
            .value=${currentValue}
            @change=${(e) => {
              const level = parseInt(e.target.value)
              if (level === 0) {
                this.editor?.chain().focus().setParagraph().run()
              } else {
                this.editor?.chain().focus().toggleHeading({ level }).run()
              }
            }}
          >
            <option value="0">단락</option>
            <option value="1">H1</option>
            <option value="2">H2</option>
            <option value="3">H3</option>
          </select>
        `
      }

      // MARK-04: 구분선 버튼
      renderHorizontalRuleButton() {
        const isDisabled = this.editor == null || !this.editor.can().setHorizontalRule()
        return html`
          <button
            class="toolbar__button rhino-toolbar-button"
            type="button"
            tabindex="-1"
            part="toolbar__button toolbar__button--horizontal-rule"
            aria-disabled=${isDisabled}
            aria-label="구분선"
            data-role="toolbar-item"
            title="구분선"
            @click=${(e) => {
              if (isDisabled) return
              this.editor?.chain().focus().setHorizontalRule().run()
            }}
          >
            &#8212;
          </button>
        `
      }

      // MARK-02: 밑줄 버튼
      renderUnderlineButton() {
        const isActive = Boolean(this.editor?.isActive("underline"))
        const isDisabled = this.editor == null || !this.editor.can().toggleUnderline()
        return html`
          <button
            class="toolbar__button rhino-toolbar-button ${isActive ? 'toolbar__button--active' : ''}"
            type="button"
            tabindex="-1"
            part="toolbar__button toolbar__button--underline ${isActive ? 'toolbar__button--active' : ''}"
            aria-disabled=${isDisabled}
            aria-pressed=${isActive}
            aria-label="밑줄"
            data-role="toolbar-item"
            title="밑줄"
            @click=${(e) => {
              if (isDisabled) return
              this.editor?.chain().focus().toggleUnderline().run()
            }}
          >
            <u>U</u>
          </button>
        `
      }

      // 기존 toolbar 유지 + 새 버튼 3개 추가 (제목 드롭다운, 구분선, 밑줄)
      renderToolbarEnd() {
        return html`
          ${this.renderHeadingDropdown()}
          ${this.renderHorizontalRuleButton()}
          ${this.renderUnderlineButton()}
        `
      }
    }

    AdminRhinoEditor.define("admin-rhino-editor")
    ```

    주의사항:
    - `renderToolbar()` 전체를 재작성하지 말 것 — renderToolbarEnd()만 override하면 기존 Bold/Italic/Strike/Blockquote/CodeBlock 등 모든 기본 버튼이 그대로 유지됨
    - `this.extensions.push()` 직접 사용 금지 — `addExtensions()` 메서드를 통해 중복 필터링 처리
    - `@tiptap/extension-strike` 직접 등록 금지 — rhinoStrike(CustomStrike)와 충돌

    3. `teovibe/config/initializers/action_text.rb` 에서 기존 `allowed_tags +=` 라인 뒤에 `<u>` 태그 추가:

    ```ruby
    ActionText::ContentHelper.allowed_tags += %w[table thead tbody tfoot tr th td colgroup col caption u]
    ```

    기존 table 태그 라인에 `u`를 합쳐도 되고, 별도 라인으로 `ActionText::ContentHelper.allowed_tags += %w[u]` 추가해도 됨.
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bin/rails runner "puts ActionText::ContentHelper.allowed_tags.include?('u')" && node -e "require('@tiptap/extension-underline')" && grep -q "renderToolbarEnd" app/frontend/editor/admin_rhino_editor.js && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <done>
    - @tiptap/extension-underline@^2.27.2 가 package.json에 설치됨
    - admin_rhino_editor.js 에 Underline extension 등록 + renderToolbarEnd() 에 밑줄/구분선/제목 드롭다운 버튼 3개 구현
    - ActionText 허용목록에 u 태그 포함
    - 기존 toolbar 버튼(Bold, Italic, Strike, Blockquote, CodeBlock 등)이 그대로 유지됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 툴바 서식 버튼 전체 동작 확인</name>
  <files>teovibe/app/frontend/editor/admin_rhino_editor.js</files>
  <action>사용자가 브라우저에서 Admin 에디터 툴바의 모든 서식 버튼(Strike, Underline, Blockquote, HorizontalRule, CodeBlock, Heading 드롭다운)이 정상 동작하는지 확인한다.</action>
  <verify>사용자 승인</verify>
  <done>모든 MARK-01~06 서식 버튼이 에디터에서 동작하고, 저장 후 상세 페이지에서 서식이 유지됨을 사용자가 확인</done>
  <what-built>Admin 에디터 툴바에 밑줄/구분선/제목 드롭다운 버튼 추가 + 기존 Strike/Blockquote/CodeBlock 버튼 정상 동작 확인</what-built>
  <how-to-verify>
    1. `cd teovibe && bin/rails server` 실행 후 http://localhost:3000/admin/posts/new 접속
    2. 에디터 툴바 끝에 제목 드롭다운(단락/H1/H2/H3), 구분선(—) 버튼, 밑줄(U) 버튼이 보이는지 확인
    3. MARK-01: 텍스트 입력 후 선택 → Strike(S) 버튼 클릭 → 취소선 적용 확인
    4. MARK-02: 텍스트 선택 → 밑줄(U) 버튼 클릭 → 밑줄 적용 확인
    5. MARK-03: 텍스트 입력 후 → Blockquote 버튼 클릭 → 인용구 변환 확인
    6. MARK-04: 구분선(—) 버튼 클릭 → 구분선 삽입 확인
    7. MARK-05: CodeBlock 버튼 클릭 → 코드블록 변환 확인
    8. MARK-06: 제목 드롭다운에서 H2 선택 → 제목 변환 확인 → 단락 선택 → 원래대로 복귀 확인
    9. 제목/본문 입력 후 저장 → 게시글 상세 페이지에서 서식(특히 밑줄 <u> 태그)이 유지되는지 확인
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 문제점 설명</resume-signal>
</task>

</tasks>

<verification>
- ActionText 허용목록에 u 태그 포함 확인: `bin/rails runner "puts ActionText::ContentHelper.allowed_tags.include?('u')"`
- Underline 패키지 설치 확인: `grep "extension-underline" teovibe/package.json`
- renderToolbarEnd 메서드 존재 확인: `grep "renderToolbarEnd" teovibe/app/frontend/editor/admin_rhino_editor.js`
- Vite 빌드 정상 확인: `cd teovibe && bin/vite build` (에러 없음)
</verification>

<success_criteria>
1. 툴바에서 취소선/밑줄/인용구/구분선/코드블록 버튼을 클릭하면 선택 텍스트에 해당 서식이 즉시 적용된다
2. 제목 드롭다운에서 H1/H2/H3을 선택하면 커서가 위치한 블록이 해당 제목 레벨로 변환된다
3. 위 서식이 적용된 게시글을 저장하고 상세 페이지에서 조회하면 서식이 그대로 렌더링된다
4. 새 npm 패키지는 @tiptap/extension-underline 1개만 추가됨
</success_criteria>

<output>
After completion, create `.planning/phases/15-툴바-서식-확장/15-01-SUMMARY.md`
</output>
