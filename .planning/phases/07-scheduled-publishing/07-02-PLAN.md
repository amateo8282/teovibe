---
phase: 07-scheduled-publishing
plan: 02
type: tdd
wave: 2
depends_on: [07-01]
files_modified:
  - teovibe/app/controllers/admin/posts_controller.rb
  - teovibe/test/controllers/admin/posts_controller_scheduling_test.rb
  - teovibe/app/views/admin/posts/_form.html.erb
  - teovibe/app/views/admin/posts/index.html.erb
autonomous: true
requirements: [SCHD-01, SCHD-03]

must_haves:
  truths:
    - "Admin 게시글 폼에 datetime-local 필드로 예약 발행 시각(KST)을 입력할 수 있다"
    - "폼에서 입력한 KST 시각이 UTC로 변환되어 scheduled_at에 저장된다"
    - "예약 시각 지정 시 SolidQueue에 PublishPostJob이 등록되고 job_id가 Post에 저장된다"
    - "Admin이 예약 시각을 변경하면 기존 잡이 취소되고 새 잡이 등록된다"
    - "Admin이 예약을 취소(scheduled_at 비우기)하면 기존 잡이 삭제된다"
    - "Admin 게시글 목록에 예약된 게시글은 '예약됨' 배지와 예정 시각이 표시된다"
  artifacts:
    - path: "teovibe/test/controllers/admin/posts_controller_scheduling_test.rb"
      provides: "예약 컨트롤러 TDD 테스트 (생성/수정/취소)"
      contains: "posts_controller_scheduling_test"
    - path: "teovibe/app/controllers/admin/posts_controller.rb"
      provides: "예약 처리 로직 (handle_scheduling, enqueue_publish_job, cancel_existing_job)"
      contains: "handle_scheduling"
    - path: "teovibe/app/views/admin/posts/_form.html.erb"
      provides: "datetime-local 입력 필드 (KST 표시)"
      contains: "datetime-local"
    - path: "teovibe/app/views/admin/posts/index.html.erb"
      provides: "예약됨 배지 및 예정 시각 표시"
      contains: "scheduled?"
  key_links:
    - from: "teovibe/app/controllers/admin/posts_controller.rb"
      to: "teovibe/app/jobs/publish_post_job.rb"
      via: "PublishPostJob.set(wait_until:).perform_later"
      pattern: "PublishPostJob\\.set"
    - from: "teovibe/app/controllers/admin/posts_controller.rb"
      to: "SolidQueue::Job"
      via: "기존 잡 취소 시 SolidQueue::Job.find_by.destroy"
      pattern: "SolidQueue::Job"
    - from: "teovibe/app/views/admin/posts/_form.html.erb"
      to: "teovibe/app/controllers/admin/posts_controller.rb"
      via: "scheduled_at 파라미터 → handle_scheduling에서 KST→UTC 변환"
      pattern: "scheduled_at"
---

<objective>
Admin 게시글 컨트롤러에 예약 발행 로직을 추가하고, 폼에 datetime-local 입력 필드, 목록에 '예약됨' 배지를 구현한다.

Purpose: Admin이 게시글 저장 시 미래 발행 시각을 지정/변경/취소할 수 있는 완전한 UX를 제공한다.
Output: 컨트롤러 예약 처리 + 폼 UI + 목록 배지 완성
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scheduled-publishing/07-RESEARCH.md
@.planning/phases/07-scheduled-publishing/07-01-SUMMARY.md

<interfaces>
<!-- 07-01에서 추가된 Post 모델 인터페이스 -->
```ruby
# teovibe/app/models/post.rb (07-01 이후)
class Post < ApplicationRecord
  enum :status, { draft: 0, published: 1 }

  scope :scheduled, -> { where(status: :draft).where.not(scheduled_at: nil) }

  def scheduled?
    draft? && scheduled_at.present?
  end

  # scheduled_at: datetime (UTC 저장)
  # job_id: string (SolidQueue provider_job_id)
end
```

<!-- 07-01에서 구현된 PublishPostJob -->
```ruby
# teovibe/app/jobs/publish_post_job.rb
class PublishPostJob < ApplicationJob
  queue_as :default
  discard_on ActiveJob::DeserializationError

  def perform(post_id)
    post = Post.find_by(id: post_id)
    return unless post&.scheduled?
    post.update!(status: :published, scheduled_at: nil, job_id: nil)
  end
end
```

<!-- 현재 Admin::PostsController (teovibe/app/controllers/admin/posts_controller.rb) -->
```ruby
module Admin
  class PostsController < BaseController
    before_action :set_post, only: %i[show edit update destroy]

    def create
      @post = Current.user.posts.build(post_params)
      @post.status = :published if @post.status.blank?
      if @post.save
        redirect_to admin_posts_path, notice: "게시글이 작성되었습니다."
      else
        render :new, status: :unprocessable_entity
      end
    end

    def update
      if @post.update(post_params)
        redirect_to admin_posts_path, notice: "게시글이 수정되었습니다."
      else
        render :edit, status: :unprocessable_entity
      end
    end

    private

    def post_params
      params.require(:post).permit(:title, :body, :category_id, :status, :pinned, :seo_title, :seo_description)
    end
  end
end
```

<!-- 현재 _form.html.erb (teovibe/app/views/admin/posts/_form.html.erb) -->
<!-- 상태 select (draft/published), 제목, 카테고리, 본문(rhino-editor), SEO 필드 존재 -->
<!-- datetime-local 필드를 상태 select 아래에 추가해야 함 -->

<!-- 현재 index.html.erb 상태 표시 패턴 -->
```erb
<td class="px-4 py-3">
  <span class="<%= post.published? ? 'text-green-600' : 'text-tv-gray' %>"><%= post.status %></span>
</td>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin::PostsController 예약 발행 로직 TDD 구현</name>
  <files>
    teovibe/test/controllers/admin/posts_controller_scheduling_test.rb,
    teovibe/app/controllers/admin/posts_controller.rb
  </files>
  <behavior>
    - Test 1: 예약 시각 지정하여 게시글 생성 → draft 상태 + scheduled_at UTC 저장
    - Test 2: 예약 시각 없이 게시글 생성 → 기존대로 published 상태
    - Test 3: 예약된 게시글 시각 변경(update) → 새 scheduled_at 저장
    - Test 4: 예약된 게시글 예약 취소(update, 빈 문자열) → scheduled_at nil
  </behavior>
  <action>
    **RED 단계: 컨트롤러 테스트 먼저 작성**

    1. `test/controllers/admin/posts_controller_scheduling_test.rb` 생성:
       - `ActionDispatch::IntegrationTest` 상속
       - `sign_in_as(@admin)` 헬퍼 사용 (기존 프로젝트 패턴)
       - 4개 테스트 케이스 작성 (위 behavior 참고)
       - KST→UTC 변환 검증: `"2026-04-01T14:00"` KST → `"2026-04-01 05:00:00 UTC"` 확인

    **GREEN 단계: 컨트롤러 로직 구현**

    2. **handle_scheduling(post) 메서드 추가:**
       - `params[:post][:scheduled_at]`이 있으면 `ActiveSupport::TimeZone["Seoul"].parse(raw)`로 KST 해석 후 `.utc`로 변환하여 `post.scheduled_at`에 할당
       - 예약 시각이 지정되면 `post.status = :draft` 강제 (예약 중이면 draft 유지)
       - `params[:post][:scheduled_at]`이 빈 문자열이면 `post.scheduled_at = nil` (예약 취소)

    3. **enqueue_publish_job(post) 메서드 추가:**
       - `job = PublishPostJob.set(wait_until: post.scheduled_at).perform_later(post.id)`
       - `post.update_column(:job_id, job.provider_job_id)` 로 job_id 저장

    4. **cancel_existing_job(post) 메서드 추가:**
       - `post.job_id.present?`이면 `SolidQueue::Job.find_by(id: post.job_id)&.destroy`
       - `post.update_column(:job_id, nil)`

    5. **create 액션 수정:**
       ```ruby
       def create
         @post = Current.user.posts.build(post_params)
         handle_scheduling(@post)
         @post.status = :published if @post.status.blank? && !@post.scheduled?
         if @post.save
           enqueue_publish_job(@post) if @post.scheduled?
           redirect_to admin_posts_path, notice: "게시글이 작성되었습니다."
         else
           render :new, status: :unprocessable_entity
         end
       end
       ```

    6. **update 액션 수정:**
       ```ruby
       def update
         cancel_existing_job(@post)
         @post.assign_attributes(post_params)
         handle_scheduling(@post)
         if @post.save
           enqueue_publish_job(@post) if @post.scheduled?
           redirect_to admin_posts_path, notice: "게시글이 수정되었습니다."
         else
           render :edit, status: :unprocessable_entity
         end
       end
       ```
       - **중요:** `@post.assign_attributes(post_params)`를 `handle_scheduling` 이전에 호출하여 post_params가 반영되도록 할 것
       - update에서 `@post.update(post_params)` 대신 `@post.assign_attributes(post_params)` + `@post.save`로 변경 (handle_scheduling이 속성을 직접 설정하므로)

    **주의사항:**
    - `scheduled_at`은 `post_params`에 포함하지 말 것. handle_scheduling에서 timezone 변환 후 직접 할당
    - `Time.parse` 사용 금지 — 반드시 `ActiveSupport::TimeZone["Seoul"].parse()` 사용
    - `Time.now` 사용 금지 — `Time.current` 사용
    - 기존 destroy, show, edit, new 액션은 수정하지 말 것
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test test/controllers/admin/posts_controller_scheduling_test.rb -v</automated>
  </verify>
  <done>
    - 4개 컨트롤러 테스트가 모두 통과
    - create 액션에서 scheduled_at이 있으면 KST→UTC 변환 후 저장 + PublishPostJob 큐잉
    - update 액션에서 assign_attributes(post_params) 적용 후 기존 잡 취소 + 새 예약 처리
    - 예약 취소(scheduled_at 빈 값) 시 기존 잡 삭제 + scheduled_at nil 처리
    - post_params에 scheduled_at 미포함 (handle_scheduling에서 별도 처리)
  </done>
</task>

<task type="auto">
  <name>Task 2: 폼 datetime-local 필드 + 목록 예약됨 배지 추가</name>
  <files>
    teovibe/app/views/admin/posts/_form.html.erb,
    teovibe/app/views/admin/posts/index.html.erb
  </files>
  <action>
    1. **_form.html.erb에 datetime-local 필드 추가:**
       - 상태(status) select 아래, 고정글(pinned) 체크박스 위에 배치
       - KST 시간으로 value 표시: `post.scheduled_at&.in_time_zone("Seoul")&.strftime("%Y-%m-%dT%H:%M")`
       - min 속성으로 과거 시각 입력 방지: `Time.current.in_time_zone("Seoul").strftime("%Y-%m-%dT%H:%M")`
       - 기존 프로젝트 스타일 클래스 사용: `w-full px-4 py-3 rounded-2xl border border-gray-300 focus:outline-none`
       - 라벨: "예약 발행 시각 (KST)"
       - 힌트 텍스트: "비워두면 즉시 발행됩니다" (text-xs text-tv-gray)

       ```erb
       <div>
         <label class="block text-sm font-bold mb-1">예약 발행 시각 (KST)</label>
         <% scheduled_kst = post.scheduled_at&.in_time_zone("Seoul")&.strftime("%Y-%m-%dT%H:%M") %>
         <%= f.text_field :scheduled_at,
               type: "datetime-local",
               value: scheduled_kst,
               min: Time.current.in_time_zone("Seoul").strftime("%Y-%m-%dT%H:%M"),
               class: "w-full px-4 py-3 rounded-2xl border border-gray-300 focus:outline-none" %>
         <p class="text-xs text-tv-gray mt-1">비워두면 즉시 발행됩니다</p>
       </div>
       ```

    2. **index.html.erb 상태 컬럼 수정:**
       - 기존 published/draft 2분기 표시를 3분기로 변경 (scheduled? 추가)
       - 예약됨: 노란색 텍스트 + 예정 시각(KST) 표시

       ```erb
       <td class="px-4 py-3">
         <% if post.scheduled? %>
           <span class="text-yellow-600 font-bold">예약됨</span>
           <span class="text-xs text-tv-gray block">
             <%= post.scheduled_at.in_time_zone("Seoul").strftime("%m/%d %H:%M") %>
           </span>
         <% elsif post.published? %>
           <span class="text-green-600">published</span>
         <% else %>
           <span class="text-tv-gray">draft</span>
         <% end %>
       </td>
       ```

    **주의사항:**
    - 기존 폼 필드(제목, 카테고리, 본문, SEO 등)는 절대 수정하지 말 것
    - 기존 index 테이블 구조(컬럼 순서, 액션 버튼 등)는 유지
    - `Time.now` 사용 금지 — `Time.current` 사용
    - 시간 표시는 항상 `.in_time_zone("Seoul")` 사용
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test -v</automated>
  </verify>
  <done>
    - Admin 게시글 폼에 datetime-local 입력 필드가 상태 select 아래에 표시된다
    - 기존 예약 시각이 있으면 KST로 변환되어 필드에 표시된다
    - 게시글 목록에서 예약된 게시글은 노란색 '예약됨' 배지와 예정 시각이 표시된다
    - 발행된 게시글은 기존대로 녹색 'published', 초안은 회색 'draft'로 표시된다
  </done>
</task>

</tasks>

<verification>
```bash
cd teovibe
# 컨트롤러 예약 메서드 존재 확인
grep -n "handle_scheduling\|enqueue_publish_job\|cancel_existing_job" app/controllers/admin/posts_controller.rb

# 폼에 datetime-local 필드 존재 확인
grep -n "datetime-local" app/views/admin/posts/_form.html.erb

# 목록에 scheduled? 분기 존재 확인
grep -n "scheduled?" app/views/admin/posts/index.html.erb

# 전체 테스트 통과
bin/rails test
```
</verification>

<success_criteria>
1. Admin 게시글 폼에 예약 발행 datetime-local 필드가 KST로 표시된다
2. 예약 시각 입력 후 저장하면 KST→UTC 변환되어 DB에 저장된다
3. PublishPostJob이 SolidQueue에 wait_until로 등록되고 job_id가 Post에 저장된다
4. 예약 수정 시 기존 잡 취소 후 새 잡 등록된다
5. 예약 취소(필드 비움) 시 기존 잡 삭제된다
6. 게시글 목록에 '예약됨' 배지와 예정 시각이 표시된다
7. 기존 게시글 CRUD 동작이 정상 유지된다
</success_criteria>

<output>
After completion, create `.planning/phases/07-scheduled-publishing/07-02-SUMMARY.md`
</output>
