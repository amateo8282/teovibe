---
phase: 10-크롤링-기초
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - teovibe/app/views/layouts/application.html.erb
  - teovibe/test/integration/seo_tags_test.rb
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02

user_setup:
  - service: google-search-console
    why: "Google 소유권 인증 토큰 발급"
    env_vars:
      - name: seo.google_site_verification (Rails credentials)
        source: "Google Search Console -> 설정 -> 소유권 확인 -> HTML 태그 -> content 값 복사"
    dashboard_config:
      - task: "사이트 속성 추가 및 HTML 태그 인증 방식 선택"
        location: "https://search.google.com/search-console"
  - service: naver-search-advisor
    why: "네이버 소유권 인증 토큰 발급"
    env_vars:
      - name: seo.naver_site_verification (Rails credentials)
        source: "네이버 서치어드바이저 -> 웹마스터 도구 -> 사이트 추가 -> HTML 태그 -> content 값 복사"
    dashboard_config:
      - task: "사이트 등록 및 HTML 메타태그 인증 방식 선택"
        location: "https://searchadvisor.naver.com/"

must_haves:
  truths:
    - "모든 페이지 <head>에 google-site-verification 메타태그가 출력된다 (credentials에 토큰이 있을 때)"
    - "모든 페이지 <head>에 naver-site-verification 메타태그가 출력된다 (credentials에 토큰이 있을 때)"
    - "credentials에 토큰이 없으면 해당 메타태그가 출력되지 않는다"
    - "인증 태그가 yield :head 이전에 위치하여 모든 경로에서 일관 출력된다"
  artifacts:
    - path: "teovibe/app/views/layouts/application.html.erb"
      provides: "검색엔진 인증 메타태그 출력"
      contains: "google-site-verification"
    - path: "teovibe/test/integration/seo_tags_test.rb"
      provides: "인증 메타태그 통합 테스트"
      contains: "naver-site-verification"
  key_links:
    - from: "teovibe/app/views/layouts/application.html.erb"
      to: "Rails credentials"
      via: "Rails.application.credentials.dig(:seo, :google_site_verification)"
      pattern: "credentials\\.dig.*seo"
---

<objective>
Google Search Console / 네이버 서치어드바이저 소유권 인증 메타태그 삽입

Purpose: 양대 검색엔진에서 사이트 소유권을 인증하여 크롤링 상태 모니터링 및 색인 관리가 가능하도록 한다.
Output: application.html.erb에 인증 메타태그 추가, 통합 테스트 파일
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-크롤링-기초/10-RESEARCH.md

@teovibe/app/views/layouts/application.html.erb

<interfaces>
<!-- application.html.erb 현재 head 구조 -->
From teovibe/app/views/layouts/application.html.erb:
```erb
<head>
  <title>...</title>
  <meta name="viewport" ...>
  ...
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= display_meta_tags site: "TeoVibe" %>

  <%= yield :head %>
  <!-- 인증 태그는 display_meta_tags 이후, yield :head 이전에 삽입 -->

  <link rel="icon" ...>
  ...
</head>
```

<!-- Rails credentials 접근 패턴 -->
```ruby
Rails.application.credentials.dig(:seo, :google_site_verification)
Rails.application.credentials.dig(:seo, :naver_site_verification)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 검색엔진 인증 메타태그 삽입 (TDD)</name>
  <files>teovibe/app/views/layouts/application.html.erb, teovibe/test/integration/seo_tags_test.rb</files>
  <behavior>
    - Test 1: credentials에 google_site_verification 토큰이 있으면 모든 페이지 head에 `<meta name="google-site-verification" content="TOKEN">` 출력
    - Test 2: credentials에 naver_site_verification 토큰이 있으면 모든 페이지 head에 `<meta name="naver-site-verification" content="TOKEN">` 출력
    - Test 3: credentials에 토큰이 없으면(nil) 해당 메타태그가 출력되지 않음
    - Test 4: 루트 페이지(/)에서 두 인증 태그 모두 출력 확인
    - Test 5: 게시글 목록 페이지(/posts/blog)에서도 인증 태그 출력 확인 (경로 무관 일관 출력)
  </behavior>
  <action>
    1. `application.html.erb`의 `<head>` 섹션에서 `<%= display_meta_tags site: "TeoVibe" %>` 바로 아래, `<%= yield :head %>` 바로 위에 인증 메타태그 삽입:
       ```erb
       <%# 검색엔진 소유권 인증 메타태그 (yield :head 이전 — 모든 경로 일관 출력 보장) %>
       <% if (token = Rails.application.credentials.dig(:seo, :google_site_verification)).present? %>
         <meta name="google-site-verification" content="<%= token %>">
       <% end %>
       <% if (token = Rails.application.credentials.dig(:seo, :naver_site_verification)).present? %>
         <meta name="naver-site-verification" content="<%= token %>">
       <% end %>
       ```
    2. `set_meta_tags verification:` 방식 사용 금지 (RESEARCH.md 안티패턴 참고 — Naver 인증이 특정 뷰에 종속되면 루트 외 경로에서 소실)
    3. 테스트에서 credentials를 stub:
       ```ruby
       # credentials stub 패턴
       mock_creds = OpenStruct.new
       mock_creds.define_singleton_method(:dig) { |*keys|
         { seo: { google_site_verification: "test-google-token", naver_site_verification: "test-naver-token" } }.dig(*keys)
       }
       Rails.application.stub(:credentials, mock_creds) { ... }
       ```
       또는 test 환경 credentials에 실제 테스트 토큰을 설정하는 방법도 가능.
       가장 간단한 방법: `Rails.application.credentials.stub(:dig, ...)` 또는 테스트 fixtures credentials 사용.
    4. credentials에 실제 토큰 추가는 user_setup으로 사용자에게 위임. 코드는 토큰 존재 여부를 조건부로 처리하므로 토큰 없이도 에러 없음.
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test test/integration/seo_tags_test.rb</automated>
  </verify>
  <done>application.html.erb에 Google/Naver 인증 메타태그가 credentials 기반 조건부로 출력되고, 모든 경로에서 일관 출력됨. 테스트 통과.</done>
</task>

</tasks>

<verification>
- `cd teovibe && bin/rails test test/integration/seo_tags_test.rb` 통과
- `application.html.erb`에서 `google-site-verification`, `naver-site-verification` 메타태그가 `yield :head` 이전에 위치
- credentials에 토큰이 없어도 페이지 렌더링 에러 없음
</verification>

<success_criteria>
1. 모든 페이지 head에 Google/Naver 인증 메타태그가 credentials 기반으로 조건부 출력
2. yield :head 이전 위치에 삽입되어 경로 무관 일관 출력 보장
3. 테스트 통과, credentials 없이도 정상 동작
</success_criteria>

<output>
After completion, create `.planning/phases/10-크롤링-기초/10-02-SUMMARY.md`
</output>
