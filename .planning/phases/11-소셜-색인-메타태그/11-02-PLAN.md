---
phase: 11-소셜-색인-메타태그
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/app/views/layouts/admin.html.erb
  - teovibe/app/controllers/sessions_controller.rb
  - teovibe/app/controllers/registrations_controller.rb
  - teovibe/test/integration/noindex_test.rb
autonomous: true
requirements: [INDX-02, INDX-03]

must_haves:
  truths:
    - "Admin 하위 모든 페이지에 noindex 메타태그가 출력된다"
    - "로그인 페이지에 noindex 메타태그가 출력된다"
    - "회원가입 페이지에 noindex 메타태그가 출력된다"
  artifacts:
    - path: "teovibe/app/views/layouts/admin.html.erb"
      provides: "noindex 메타태그 삽입"
      contains: "noindex"
    - path: "teovibe/app/controllers/sessions_controller.rb"
      provides: "로그인 페이지 noindex"
      contains: "set_meta_tags"
    - path: "teovibe/app/controllers/registrations_controller.rb"
      provides: "회원가입 페이지 noindex"
      contains: "set_meta_tags"
    - path: "teovibe/test/integration/noindex_test.rb"
      provides: "noindex 메타태그 통합 테스트"
      min_lines: 20
  key_links:
    - from: "teovibe/app/views/layouts/admin.html.erb"
      to: "검색엔진 크롤러"
      via: "noindex 메타태그"
      pattern: "noindex"
    - from: "teovibe/app/controllers/sessions_controller.rb"
      to: "display_meta_tags in application.html.erb"
      via: "set_meta_tags noindex"
      pattern: "set_meta_tags.*noindex"
---

<objective>
Admin 페이지와 인증 페이지(로그인/회원가입)에 noindex 메타태그를 추가하여 검색엔진이 해당 페이지를 색인하지 않도록 한다.

Purpose: 관리자 페이지와 인증 폼이 검색 결과에 노출되는 것을 방지하여 SEO 크롤 버짓을 의미 있는 콘텐츠에 집중시킨다.
Output: admin 레이아웃에 noindex 고정 삽입 + SessionsController/RegistrationsController에 set_meta_tags noindex + 통합 테스트
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-소셜-색인-메타태그/11-CONTEXT.md

<interfaces>
<!-- Admin 레이아웃은 display_meta_tags를 사용하지 않음 — 직접 meta 태그 삽입 필요 -->

From teovibe/app/views/layouts/admin.html.erb (line 4-7):
```erb
<title>Admin - TeoVibe</title>
<meta name="viewport" content="width=device-width,initial-scale=1">
<%= csrf_meta_tags %>
<%= csp_meta_tag %>
```

From teovibe/app/controllers/sessions_controller.rb:
```ruby
class SessionsController < ApplicationController
  allow_unauthenticated_access only: %i[ new create ]
  # new 액션에서 set_meta_tags 호출 필요
end
```

From teovibe/app/controllers/registrations_controller.rb:
```ruby
class RegistrationsController < ApplicationController
  allow_unauthenticated_access only: %i[ new create ]
  # new 액션에서 set_meta_tags 호출 필요
end
```

<!-- application.html.erb에 display_meta_tags site: "TeoVibe" 이미 있어서
     SessionsController/RegistrationsController에서 set_meta_tags noindex: true 호출하면 자동 출력 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin/인증 페이지 noindex 메타태그 (TDD)</name>
  <files>teovibe/app/views/layouts/admin.html.erb, teovibe/app/controllers/sessions_controller.rb, teovibe/app/controllers/registrations_controller.rb, teovibe/test/integration/noindex_test.rb</files>
  <behavior>
    - Test 1: GET /admin 응답 HTML에 meta name="robots" content="noindex" 존재
    - Test 2: GET /session/new (로그인) 응답 HTML에 meta name="robots" content="noindex" 존재
    - Test 3: GET /registration/new (회원가입) 응답 HTML에 meta name="robots" content="noindex" 존재
  </behavior>
  <action>
    1. teovibe/test/integration/noindex_test.rb 작성:
       - NoindexTest 클래스
       - Admin 페이지 noindex 테스트: admin 로그인 후 GET /admin, assert_select "meta[name='robots'][content='noindex']"
       - 로그인 페이지 noindex 테스트: GET /session/new, assert_select 동일
       - 회원가입 페이지 noindex 테스트: GET /registration/new, assert_select 동일
       - RED 확인

    2. admin.html.erb head 섹션에 noindex 메타태그 직접 추가 (display_meta_tags 미사용 레이아웃이므로):
       ```erb
       <meta name="robots" content="noindex">
       ```
       csp_meta_tag 아래에 삽입.

    3. SessionsController에 before_action 추가:
       ```ruby
       before_action :set_noindex, only: %i[new]

       private
       def set_noindex
         set_meta_tags noindex: true
       end
       ```
       또는 간단히 new 액션 내부에 set_meta_tags noindex: true 호출.

    4. RegistrationsController에 동일하게 추가:
       ```ruby
       before_action :set_noindex, only: %i[new]

       private
       def set_noindex
         set_meta_tags noindex: true
       end
       ```

    5. GREEN 확인

    주의: Admin 테스트 시 admin 사용자로 로그인 필요. fixtures에 admin 사용자가 있는지 확인 후 sign_in 처리.
    주의: Admin 라우트 경로 확인 — /admin이 root이므로 admin_root_path 사용.
    주의: 로그인/회원가입 라우트 정확한 경로 확인 필요 (resource :session vs resources :sessions 등). routes.rb에서 확인할 것.
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test test/integration/noindex_test.rb</automated>
  </verify>
  <done>Admin 모든 페이지에 noindex 고정 출력, 로그인/회원가입 페이지에 noindex 출력, 통합 테스트 전부 통과</done>
</task>

</tasks>

<verification>
cd teovibe && bin/rails test test/integration/noindex_test.rb -v
</verification>

<success_criteria>
- Admin 레이아웃 HTML head에 `<meta name="robots" content="noindex">` 출력
- 로그인 페이지 HTML head에 noindex 메타태그 출력
- 회원가입 페이지 HTML head에 noindex 메타태그 출력
- 통합 테스트 전부 통과
</success_criteria>

<output>
After completion, create `.planning/phases/11-소셜-색인-메타태그/11-02-SUMMARY.md`
</output>
