<!DOCTYPE html>
<html lang="ko">
  <head>
    <title><%= content_for(:title) || "TeoVibe - 바이브코딩 커뮤니티" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="TeoVibe">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= display_meta_tags site: "TeoVibe" %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Pretendard 폰트 CDN %>
    <link rel="stylesheet" as="style" crossorigin href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css" />

    <%# Vite 빌드 파이프라인 (importmap 대체) %>
    <%= vite_client_tag %>
    <%= vite_react_refresh_tag %>
    <%= vite_javascript_tag 'application' %>
  </head>

  <body class="bg-tv-cream text-tv-black min-h-screen flex flex-col">
    <%= render "shared/navbar" %>
    <%= render "shared/flash" %>

    <main class="flex-1 pt-[86px]">
      <%= yield %>
    </main>

    <%= render "shared/footer" %>
  </body>
</html>
