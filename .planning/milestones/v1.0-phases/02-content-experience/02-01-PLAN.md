---
phase: 02-content-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/package.json
  - teovibe/pnpm-lock.yaml
  - teovibe/app/frontend/entrypoints/application.js
  - teovibe/app/views/posts/_form.html.erb
  - teovibe/app/views/admin/posts/_form.html.erb
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03]

must_haves:
  truths:
    - "새 글 작성 폼에서 rhino-editor가 로드되고 텍스트 입력이 가능하다"
    - "기존 Trix로 작성된 글을 편집할 때 콘텐츠가 깨지지 않고 표시된다"
    - "에디터에 이미지를 드래그앤드롭하면 Active Storage로 업로드되고 본문에 삽입된다"
    - "텍스트를 선택하면 버블 메뉴(볼드, 이탤릭, 링크)가 나타난다"
  artifacts:
    - path: "teovibe/app/frontend/entrypoints/application.js"
      provides: "rhino-editor import (trix/actiontext 제거)"
      contains: "import \"rhino-editor\""
    - path: "teovibe/app/views/posts/_form.html.erb"
      provides: "rhino-editor 웹 컴포넌트 폼 통합"
      contains: "<rhino-editor"
    - path: "teovibe/app/views/admin/posts/_form.html.erb"
      provides: "Admin 게시글 폼에 rhino-editor 적용"
      contains: "<rhino-editor"
  key_links:
    - from: "teovibe/app/frontend/entrypoints/application.js"
      to: "rhino-editor npm package"
      via: "import statement"
      pattern: "import \"rhino-editor\""
    - from: "teovibe/app/views/posts/_form.html.erb"
      to: "Active Storage direct uploads"
      via: "data-direct-upload-url attribute"
      pattern: "data-direct-upload-url"
---

<objective>
rhino-editor(TipTap 기반)를 도입하여 기존 Trix 에디터를 대체한다. 이미지 드래그앤드롭 업로드와 버블 메뉴는 rhino-editor 0.17.x에 내장된 기능이므로 별도 구현 없이 올바른 설정으로 활성화한다.

Purpose: 기존 ActionText/Trix 대비 이미지 리사이즈, 버블 메뉴, ProseMirror 플러그인 생태계 등 풍부한 편집 경험 제공
Output: rhino-editor가 적용된 게시글 작성/수정 폼 (사용자 폼 + Admin 폼)
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-experience/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: rhino-editor npm 패키지 설치 및 Vite 진입점 교체</name>
  <files>teovibe/package.json, teovibe/pnpm-lock.yaml, teovibe/app/frontend/entrypoints/application.js</files>
  <action>
1. rhino-editor 0.17.x 설치 (0.18.x는 rhinoImage/rhinoAttachment 제거로 이미지 업로드 불가 -- RESEARCH 참조):
   ```bash
   cd teovibe && pnpm add rhino-editor@">=0.17.0 <0.18.0"
   ```

2. app/frontend/entrypoints/application.js 수정:
   - `import "trix"` 제거
   - `import "@rails/actiontext"` 제거
   - 다음 import 추가:
     ```javascript
     import "rhino-editor"
     import "rhino-editor/exports/styles/trix.css"
     ```
   - 주의: trix + rhino-editor 동시 import 금지 (ActionText JS 이중 등록 발생)
   - @hotwired/turbo-rails, Stimulus 컨트롤러 import는 유지

3. trix, @rails/actiontext npm 패키지는 제거하지 않음 -- rhino-editor가 내부적으로 ActionText 호환 HTML을 생성하지만 actiontext CSS가 필요할 수 있으므로 안전하게 유지. 단, JS import만 제거.
  </action>
  <verify>
- `cd teovibe && pnpm ls rhino-editor` 실행 -- 0.17.x 버전 확인
- `grep -c "rhino-editor" app/frontend/entrypoints/application.js` -- 1 이상
- `grep -c "import \"trix\"" app/frontend/entrypoints/application.js` -- 0 (제거 확인)
- `RAILS_ENV=production bundle exec rails assets:precompile` 성공
  </verify>
  <done>rhino-editor 0.17.x가 설치되고, application.js에서 trix import가 rhino-editor로 교체되었으며, 프로덕션 빌드가 성공한다</done>
</task>

<task type="auto">
  <name>Task 2: 게시글 폼에 rhino-editor 웹 컴포넌트 적용</name>
  <files>teovibe/app/views/posts/_form.html.erb, teovibe/app/views/admin/posts/_form.html.erb</files>
  <action>
1. app/views/posts/_form.html.erb 수정:
   - 기존 `f.rich_text_area :body` 를 다음으로 교체:
     ```erb
     <%= f.hidden_field :body,
           id: f.field_id(:body),
           value: f.object.body.try(:to_trix_html) || f.object.body %>
     <rhino-editor
       input="<%= f.field_id(:body) %>"
       data-blob-url-template="<%= rails_service_blob_url(":signed_id", ":filename") %>"
       data-direct-upload-url="<%= rails_direct_uploads_url %>"
       class="w-full min-h-[300px] rounded-2xl border border-tv-light-gray/50 focus:within:border-tv-gold"
     ></rhino-editor>
     ```
   - `to_trix_html` 은 기존 ActionText 데이터를 rhino-editor가 이해하는 HTML로 변환 (필수 -- 누락 시 기존 글 편집 시 빈 에디터 표시)
   - `data-direct-upload-url`은 이미지 드래그앤드롭 업로드에 필수 (EDIT-02)
   - `data-blob-url-template`은 업로드된 이미지 URL 생성에 필수

2. app/views/admin/posts/_form.html.erb 수정:
   - 동일하게 `f.rich_text_area :body`를 hidden field + `<rhino-editor>` 패턴으로 교체
   - Admin 폼은 스타일링이 다를 수 있으므로 class는 Admin 레이아웃에 맞게 조정

3. 버블 메뉴 (EDIT-03): rhino-editor 0.17.x에 기본 내장. 텍스트 선택 시 자동으로 볼드/이탤릭/링크 등 서식 메뉴 표시. 별도 구현 불필요.

4. 이미지 드래그앤드롭 + 리사이즈 (EDIT-02): rhino-editor 0.17.x의 rhinoImage 확장에 기본 내장. data-direct-upload-url 속성만 있으면 Active Storage로 직접 업로드. 별도 구현 불필요.
  </action>
  <verify>
- `grep -c "rhino-editor" app/views/posts/_form.html.erb` -- 1 이상
- `grep -c "rhino-editor" app/views/admin/posts/_form.html.erb` -- 1 이상
- `grep -c "rich_text_area" app/views/posts/_form.html.erb` -- 0 (제거 확인)
- `grep -c "to_trix_html" app/views/posts/_form.html.erb` -- 1 (기존 데이터 호환)
- `grep -c "direct-upload-url" app/views/posts/_form.html.erb` -- 1 (이미지 업로드 지원)
- `RAILS_ENV=production bundle exec rails assets:precompile` 성공
  </verify>
  <done>사용자 게시글 폼과 Admin 게시글 폼 모두 rhino-editor 웹 컴포넌트를 사용하며, 기존 ActionText 콘텐츠 호환(to_trix_html), 이미지 업로드(data-direct-upload-url), 버블 메뉴(기본 내장)가 설정되었다</done>
</task>

</tasks>

<verification>
- `RAILS_ENV=production bundle exec rails assets:precompile` 성공
- application.js에 rhino-editor import 존재, trix import 없음
- posts/_form.html.erb와 admin/posts/_form.html.erb에 `<rhino-editor>` 태그 존재
- hidden field에 `to_trix_html` 호출 존재 (기존 데이터 호환)
- `data-direct-upload-url` 속성 존재 (이미지 업로드)
</verification>

<success_criteria>
- rhino-editor 0.17.x가 설치되고 Vite 빌드에 포함된다
- 게시글 작성/수정 폼에서 rhino-editor가 Trix를 대체한다
- 기존 ActionText 콘텐츠가 편집 시 정상 표시된다
- 이미지 드래그앤드롭 업로드와 버블 메뉴가 활성화된다
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-experience/02-01-SUMMARY.md`
</output>
