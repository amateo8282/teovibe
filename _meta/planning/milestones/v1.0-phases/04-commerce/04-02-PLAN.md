---
phase: 04-commerce
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - teovibe/app/frontend/entrypoints/checkout.jsx
  - teovibe/app/frontend/components/checkout/CheckoutWidget.tsx
  - teovibe/app/views/checkouts/show.html.erb
  - teovibe/app/views/checkouts/success.html.erb
  - teovibe/app/views/checkouts/fail.html.erb
  - teovibe/package.json
autonomous: true
requirements:
  - COMM-02
  - COMM-03

user_setup:
  - service: tosspayments
    why: "결제 위젯 테스트 모드 동작에 필요한 API 키 설정"
    env_vars:
      - name: toss_payments.client_key
        source: "토스페이먼츠 개발자센터 -> 내 개발정보 -> 테스트 클라이언트 키 (test_ck_...)"
      - name: toss_payments.secret_key
        source: "토스페이먼츠 개발자센터 -> 내 개발정보 -> 테스트 시크릿 키 (test_sk_...)"
    dashboard_config:
      - task: "테스트 계정 생성 (이미 있으면 스킵)"
        location: "https://developers.tosspayments.com 에서 회원가입/로그인"

must_haves:
  truths:
    - "체크아웃 페이지에서 상품명과 가격이 표시된다"
    - "토스페이먼츠 결제 위젯이 테스트 모드로 초기화되어 결제 수단 UI가 렌더링된다"
    - "결제하기 버튼 클릭 시 requestPayment가 호출된다"
    - "결제 완료 시 successUrl로, 실패 시 failUrl로 리다이렉트된다"
    - "Turbo Drive 이탈 시 React 컴포넌트가 정상 언마운트된다"
  artifacts:
    - path: "teovibe/app/frontend/components/checkout/CheckoutWidget.tsx"
      provides: "토스페이먼츠 결제위젯 React 컴포넌트"
      contains: "loadPaymentWidget"
    - path: "teovibe/app/frontend/entrypoints/checkout.jsx"
      provides: "체크아웃 React 진입점 (turbo:load/turbo:before-cache 패턴)"
      contains: "turbo:load"
    - path: "teovibe/app/views/checkouts/show.html.erb"
      provides: "체크아웃 페이지 (React 마운트 포인트 + data attributes)"
      contains: "checkout-root"
  key_links:
    - from: "teovibe/app/frontend/entrypoints/checkout.jsx"
      to: "teovibe/app/frontend/components/checkout/CheckoutWidget.tsx"
      via: "import CheckoutWidget"
      pattern: "import.*CheckoutWidget"
    - from: "teovibe/app/views/checkouts/show.html.erb"
      to: "teovibe/app/frontend/entrypoints/checkout.jsx"
      via: "content_for :head + vite_javascript_tag 'checkout'"
      pattern: "vite_javascript_tag.*checkout"
    - from: "teovibe/app/frontend/components/checkout/CheckoutWidget.tsx"
      to: "@tosspayments/payment-widget-sdk"
      via: "loadPaymentWidget import"
      pattern: "loadPaymentWidget"
---

<objective>
토스페이먼츠 결제 위젯을 React 컴포넌트로 구현하고, 체크아웃 페이지 뷰를 완성한다.

Purpose: 사용자가 스킬팩 체크아웃 페이지에서 토스페이먼츠 결제 UI를 보고 결제를 진행할 수 있는 프론트엔드를 완성한다.
Output: CheckoutWidget.tsx + checkout.jsx + 체크아웃 ERB 뷰 3개
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-commerce/04-RESEARCH.md
@.planning/phases/04-commerce/04-01-SUMMARY.md
@teovibe/app/frontend/entrypoints/landing.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: @tosspayments/payment-widget-sdk 설치 + CheckoutWidget.tsx + checkout.jsx 생성</name>
  <files>
    teovibe/package.json
    teovibe/app/frontend/components/checkout/CheckoutWidget.tsx
    teovibe/app/frontend/entrypoints/checkout.jsx
  </files>
  <action>
    1. `cd teovibe && pnpm add @tosspayments/payment-widget-sdk`

    2. `app/frontend/components/checkout/CheckoutWidget.tsx` 생성 (RESEARCH.md Pattern 2 기반):
       - Props: clientKey, customerKey, orderId, orderName, amount, successUrl, failUrl
       - useEffect 내에서 `loadPaymentWidget(clientKey, customerKey)` 비동기 호출
       - `paymentWidget.renderPaymentMethods("#payment-method", { value: amount, currency: "KRW", country: "KR" })` 호출
       - `paymentWidget.renderAgreement("#agreement", { variantKey: "AGREEMENT" })` 호출
       - paymentWidgetRef, paymentMethodsRef를 useRef로 관리 (React state가 아닌 DOM 참조)
       - useEffect cleanup에서 `cleanup = true` 플래그로 비동기 경쟁 조건 방지
       - handlePayment: `paymentWidgetRef.current?.requestPayment({ orderId, orderName, successUrl, failUrl })`
       - 상품 정보(orderName, amount) 표시 영역 포함
       - 결제하기 버튼은 Tailwind 스타일링 적용 (bg-tv-orange 등 프로젝트 컬러)
       - amount를 "15,000원" 형태로 포맷하여 표시 (toLocaleString 사용)

    3. `app/frontend/entrypoints/checkout.jsx` 생성 (landing.jsx 패턴 동일):
       - module 스코프에 `let root = null`
       - `turbo:load`에서 `#checkout-root` 엘리먼트 찾아 createRoot + render
       - `turbo:before-cache`에서 root.unmount() + root = null
       - `#checkout-root`의 data attributes에서 props 추출: data-client-key, data-customer-key, data-order-id, data-order-name, data-amount, data-success-url, data-fail-url
       - 추출한 값을 CheckoutWidget에 props로 전달 (amount는 parseInt 변환)
  </action>
  <verify>
    `cd teovibe && pnpm list @tosspayments/payment-widget-sdk`로 패키지 설치 확인.
    `cd teovibe && npx tsc --noEmit app/frontend/components/checkout/CheckoutWidget.tsx 2>&1 || echo "타입 체크 (tsconfig 없으면 스킵)"` -- 구문 오류 없음 확인.
    checkout.jsx에 turbo:load와 turbo:before-cache 리스너가 존재함을 파일 내용으로 확인.
  </verify>
  <done>
    CheckoutWidget.tsx가 loadPaymentWidget을 통해 결제 위젯을 초기화하고 requestPayment를 호출할 수 있는 완전한 React 컴포넌트로 구현됨.
    checkout.jsx가 landing.jsx와 동일한 Turbo Drive 패턴으로 마운트/언마운트를 처리함.
  </done>
</task>

<task type="auto">
  <name>Task 2: 체크아웃 ERB 뷰 3개 생성 (show/success/fail)</name>
  <files>
    teovibe/app/views/checkouts/show.html.erb
    teovibe/app/views/checkouts/success.html.erb
    teovibe/app/views/checkouts/fail.html.erb
  </files>
  <action>
    1. `app/views/checkouts/show.html.erb` 생성:
       - `content_for :head`로 `vite_javascript_tag 'checkout'` 로드 (페이지별 JS 로드 패턴, 01-03에서 확립)
       - 상품 정보 섹션: @skill_pack.title, @skill_pack.price를 number_to_currency(locale: :ko)로 표시
       - React 마운트 포인트: `<div id="checkout-root" data-client-key="<%= @client_key %>" data-customer-key="<%= @customer_key %>" data-order-id="<%= @order.toss_order_id %>" data-order-name="<%= @skill_pack.title %>" data-amount="<%= @order.amount %>" data-success-url="<%= checkout_success_skill_pack_url(@skill_pack) %>" data-fail-url="<%= checkout_fail_skill_pack_url(@skill_pack) %>"></div>`
       - 주의: successUrl과 failUrl은 `_url` 헬퍼 사용 (절대 URL 필수, RESEARCH.md Pitfall 4)
       - Tailwind 레이아웃: 가운데 정렬, max-w-lg, 카드 스타일

    2. `app/views/checkouts/success.html.erb` 생성:
       - 결제 완료 안내 메시지 (현재는 "결제 확인 중입니다..." 표시)
       - Plan 03에서 서버사이드 confirm 후 리다이렉트로 변경 예정이므로, 현재는 단순 안내 페이지
       - paymentKey, orderId, amount를 debug용으로 표시 (development 환경만)

    3. `app/views/checkouts/fail.html.erb` 생성:
       - 결제 실패 안내: params[:message] 또는 기본 "결제에 실패했습니다" 표시
       - 에러 코드: params[:code] 표시
       - 다시 시도 버튼: `link_to "다시 시도", checkout_skill_pack_path(@skill_pack)` (Tailwind 버튼 스타일)
       - 스킬팩 목록으로 돌아가기 링크
  </action>
  <verify>
    3개 뷰 파일이 존재하고 ERB 구문 오류 없음 확인:
    `ls teovibe/app/views/checkouts/{show,success,fail}.html.erb`
    `cd teovibe && ruby -e "require 'erb'; %w[show success fail].each { |v| ERB.new(File.read(\"app/views/checkouts/#{v}.html.erb\")); puts \"#{v}: OK\" }"`
  </verify>
  <done>
    체크아웃 페이지(show)에 React 마운트 포인트와 data attributes가 포함됨.
    success 페이지에 결제 완료 안내가 표시됨.
    fail 페이지에 에러 메시지와 재시도 링크가 표시됨.
  </done>
</task>

</tasks>

<verification>
- checkout.jsx에 turbo:load + turbo:before-cache 패턴이 존재한다
- CheckoutWidget.tsx에 loadPaymentWidget, renderPaymentMethods, requestPayment 호출이 존재한다
- show.html.erb에 vite_javascript_tag 'checkout'과 #checkout-root data attributes가 존재한다
- successUrl/failUrl이 _url 헬퍼를 사용한다 (절대 URL)
</verification>

<success_criteria>
체크아웃 페이지에서 토스페이먼츠 결제 위젯이 테스트 모드로 초기화되어 결제 수단 UI가 표시되고, 결제하기 버튼으로 결제 프로세스를 시작할 수 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/04-commerce/04-02-SUMMARY.md`
</output>
