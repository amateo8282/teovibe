---
phase: 08-ai-초안-작성
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/Gemfile
  - teovibe/Gemfile.lock
  - teovibe/app/services/ai_draft_service.rb
  - teovibe/app/controllers/admin/ai_drafts_controller.rb
  - teovibe/config/routes.rb
  - teovibe/test/services/ai_draft_service_test.rb
  - teovibe/test/controllers/admin/ai_drafts_controller_test.rb
autonomous: true
requirements:
  - AIDR-01
  - AIDR-02
  - AIDR-03
  - AIDR-04

must_haves:
  truths:
    - "POST /admin/ai_draft/outline 엔드포인트가 JSON { outline: string } 반환"
    - "POST /admin/ai_draft/body 엔드포인트가 JSON { body_html: string } 반환"
    - "AiDraftService#generate_outline은 SYSTEM_PROMPT를 system 파라미터로 포함"
    - "AiDraftService#generate_body는 HTML 형식 본문 반환 프롬프트를 포함"
    - "에러 발생 시 JSON { error: string } + 422 반환"
    - "Admin 인증이 없는 요청은 루트로 리다이렉트 (Admin::BaseController 상속)"
  artifacts:
    - path: "teovibe/app/services/ai_draft_service.rb"
      provides: "Anthropic API 래퍼, SEO/AEO 시스템 프롬프트, generate_outline/generate_body 메서드"
      exports: ["AiDraftService"]
    - path: "teovibe/app/controllers/admin/ai_drafts_controller.rb"
      provides: "outline/body JSON 엔드포인트"
      exports: ["Admin::AiDraftsController"]
    - path: "teovibe/test/services/ai_draft_service_test.rb"
      provides: "AIDR-04 시스템 프롬프트 포함 단위 테스트"
    - path: "teovibe/test/controllers/admin/ai_drafts_controller_test.rb"
      provides: "AIDR-01~03 컨트롤러 응답 테스트 (API stub)"
  key_links:
    - from: "teovibe/app/controllers/admin/ai_drafts_controller.rb"
      to: "teovibe/app/services/ai_draft_service.rb"
      via: "AiDraftService.new.generate_outline / generate_body"
      pattern: "AiDraftService\\.new"
    - from: "teovibe/config/routes.rb"
      to: "teovibe/app/controllers/admin/ai_drafts_controller.rb"
      via: "resource :ai_draft + collection post :outline, :body"
      pattern: "ai_draft"
---

<objective>
AI 초안 작성 백엔드를 구현한다: anthropic gem 설치, AiDraftService(SEO/AEO 프롬프트 + API 호출), Admin::AiDraftsController(JSON 엔드포인트), 라우트 추가, Minitest stub 테스트까지 완성한다.

Purpose: 프론트엔드(Plan 02)가 바로 연동할 수 있는 JSON API를 제공한다.
Output: /admin/ai_draft/outline, /admin/ai_draft/body 엔드포인트 + 테스트 통과
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-ai-초안-작성/08-RESEARCH.md

<interfaces>
<!-- 기존 Admin 컨트롤러 패턴 -->

From teovibe/app/controllers/admin/base_controller.rb:
```ruby
module Admin
  class BaseController < ApplicationController
    before_action :require_admin!
    layout "admin"

    private

    def require_admin!
      unless Current.user&.admin?
        redirect_to root_path, alert: "관리자만 접근할 수 있습니다."
      end
    end
  end
end
```

From teovibe/app/controllers/admin/posts_controller.rb (패턴 참조):
- `Admin::PostsController < BaseController` 상속
- `rescue_from` 없이 begin/rescue 블록 사용

From teovibe/config/routes.rb (admin namespace 기존 구조):
```ruby
namespace :admin do
  root to: "dashboard#index"
  resources :categories, ...
  resources :posts, only: %i[index show new create edit update destroy]
  # 여기에 ai_draft 라우트 추가
end
```

From 08-RESEARCH.md (검증된 라우트 패턴):
```ruby
namespace :admin do
  resource :ai_draft, only: [] do
    collection do
      post :outline
      post :body
    end
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: anthropic gem 설치 + AiDraftService 구현</name>
  <files>teovibe/Gemfile, teovibe/app/services/ai_draft_service.rb, teovibe/test/services/ai_draft_service_test.rb</files>
  <behavior>
    - AiDraftService::SYSTEM_PROMPT 상수가 "H2", "H3", "FAQ" 키워드를 포함한다
    - generate_outline(topic:)은 Anthropic::Client#messages.create를 max_tokens: 512로 호출하며, message.content.first.text를 반환한다
    - generate_body(outline:)은 max_tokens: 4096으로 호출하며, HTML 형식 프롬프트("HTML 형식으로")를 메시지에 포함한다
    - 두 메서드 모두 system: SYSTEM_PROMPT를 Anthropic API에 전달한다
    - model: "claude-opus-4-5-20250929" 사용
  </behavior>
  <action>
    1. `teovibe/Gemfile`에 `gem "anthropic", "~> 1.23.0"` 추가 (기존 gem 목록 확인 후 적절한 위치에)
    2. `cd teovibe && bundle install` 실행
    3. `teovibe/test/services/ai_draft_service_test.rb` 먼저 작성 (RED):
       - stub `Anthropic::Client` 인스턴스의 `messages` 메서드와 `create` 메서드를 Minitest::Mock으로 대체
       - SYSTEM_PROMPT에 "H2", "H3", "FAQ" 포함 여부 테스트
       - generate_outline이 topic을 메시지에 포함하여 호출하는지 테스트
       - generate_body가 "HTML 형식으로" 키워드를 포함하는지 테스트
    4. `teovibe/app/services/ai_draft_service.rb` 구현 (GREEN):
       ```ruby
       class AiDraftService
         SYSTEM_PROMPT = <<~PROMPT.freeze
           당신은 SEO와 AEO(Answer Engine Optimization)에 최적화된 한국어 블로그 콘텐츠 전문가입니다.
           콘텐츠 작성 규칙:
           - 제목은 H2/H3 계층 구조로 구성하며, 섹션 제목은 독자의 질문 형태로 작성
           - 각 H2 섹션은 200-400자의 본문을 포함
           - 글 마지막에 FAQ 섹션(H2)을 포함, 5개의 Q&A를 H3으로 구성
           - 답변은 40-60자의 명확한 직접 답변으로 시작
           - 읽기 쉬운 한국어로 작성
           - HTML 태그는 h2, h3, p, ul, li, strong, em만 사용 (div 중첩 금지)
         PROMPT

         def initialize
           @client = Anthropic::Client.new
         end

         def generate_outline(topic:)
           message = @client.messages.create(
             model: "claude-opus-4-5-20250929",
             max_tokens: 512,
             system: SYSTEM_PROMPT,
             messages: [
               {
                 role: "user",
                 content: "주제: #{topic}\n\nH2 섹션 제목 5-7개로 구성된 블로그 개요를 생성해주세요. " \
                          "각 제목만 줄바꿈으로 구분하여 반환하세요. FAQ 섹션도 포함하세요."
               }
             ]
           )
           message.content.first.text
         end

         def generate_body(outline:)
           message = @client.messages.create(
             model: "claude-opus-4-5-20250929",
             max_tokens: 4096,
             system: SYSTEM_PROMPT,
             messages: [
               {
                 role: "user",
                 content: "다음 개요를 기반으로 완성된 블로그 본문을 HTML 형식으로 작성해주세요.\n\n개요:\n#{outline}"
               }
             ]
           )
           message.content.first.text
         end
       end
       ```
    5. `ANTHROPIC_API_KEY` 환경변수 설정 없이도 테스트가 통과해야 하므로, 테스트에서 `Anthropic::Client.new`를 stub
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bin/rails test test/services/ai_draft_service_test.rb</automated>
  </verify>
  <done>SYSTEM_PROMPT에 H2/H3/FAQ 포함 확인, generate_outline/generate_body API 호출 파라미터 검증 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AiDraftsController + 라우트 + 컨트롤러 테스트</name>
  <files>teovibe/app/controllers/admin/ai_drafts_controller.rb, teovibe/config/routes.rb, teovibe/test/controllers/admin/ai_drafts_controller_test.rb</files>
  <behavior>
    - POST /admin/ai_draft/outline에 topic 파라미터 전송 시 { outline: "..." } JSON 반환 (200)
    - POST /admin/ai_draft/body에 outline 파라미터 전송 시 { body_html: "..." } JSON 반환 (200)
    - AiDraftService 에러 발생 시 { error: "..." } JSON + 422 반환
    - 비인증(비어드민) 요청 시 루트로 리다이렉트 (Admin::BaseController 상속)
    - 테스트에서 AiDraftService는 Minitest::Mock으로 stub 처리
  </behavior>
  <action>
    1. `teovibe/test/controllers/admin/ai_drafts_controller_test.rb` 먼저 작성 (RED):
       - 기존 `test/controllers/admin/posts_controller_test.rb`의 admin 로그인 패턴 참조
       - test_outline_returns_json: AiDraftService stub → POST outline → JSON { outline } 검증
       - test_body_returns_json: AiDraftService stub → POST body → JSON { body_html } 검증
       - test_outline_error_returns_422: AiDraftService stub raise RuntimeError → 422 검증
       - test_requires_admin: 비어드민으로 POST → redirect 검증
    2. `teovibe/app/controllers/admin/ai_drafts_controller.rb` 구현 (GREEN):
       ```ruby
       module Admin
         class AiDraftsController < BaseController
           def outline
             service = AiDraftService.new
             outline_text = service.generate_outline(topic: params[:topic])
             render json: { outline: outline_text }
           rescue => e
             render json: { error: e.message }, status: :unprocessable_entity
           end

           def body
             service = AiDraftService.new
             body_html = service.generate_body(outline: params[:outline])
             render json: { body_html: body_html }
           rescue => e
             render json: { error: e.message }, status: :unprocessable_entity
           end
         end
       end
       ```
    3. `teovibe/config/routes.rb` admin namespace 내부에 추가:
       ```ruby
       resource :ai_draft, only: [] do
         collection do
           post :outline
           post :body
         end
       end
       ```
       `resources :posts` 라인 아래에 추가
    4. 라우트 확인: `cd teovibe && bin/rails routes | grep ai_draft`
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bin/rails test test/controllers/admin/ai_drafts_controller_test.rb</automated>
  </verify>
  <done>outline/body JSON 응답, 422 에러 핸들링, Admin 인증 체크 테스트 모두 통과. bin/rails routes | grep ai_draft에 outline, body 경로 노출</done>
</task>

</tasks>

<verification>
전체 테스트 스위트가 기존 테스트를 깨지 않아야 한다:
cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bin/rails test
</verification>

<success_criteria>
1. `bundle show anthropic` → anthropic gem 설치 확인
2. `bin/rails routes | grep ai_draft` → outline, body 두 경로 노출
3. `bin/rails test test/services/ai_draft_service_test.rb` → 모든 테스트 통과
4. `bin/rails test test/controllers/admin/ai_drafts_controller_test.rb` → 모든 테스트 통과
5. `bin/rails test` → 기존 테스트 포함 전체 통과
</success_criteria>

<output>
완료 후 `.planning/phases/08-ai-초안-작성/08-01-SUMMARY.md` 생성
</output>
