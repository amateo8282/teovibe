---
phase: 03-interactive-landing
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - teovibe/app/frontend/components/landing/FeaturesSection.tsx
  - teovibe/app/frontend/components/landing/TestimonialsSection.tsx
  - teovibe/app/frontend/components/landing/StatsSection.tsx
  - teovibe/app/frontend/components/landing/CtaSection.tsx
  - teovibe/app/frontend/components/landing/FadeInSection.tsx
  - teovibe/app/frontend/components/landing/LandingPage.tsx
autonomous: true
requirements: [LAND-01, LAND-03]

must_haves:
  truths:
    - "features/testimonials/stats/cta 섹션이 각각 스크롤 시 whileInView 애니메이션으로 등장한다"
    - "모바일(375px)에서 모든 섹션이 1열 레이아웃으로 깨짐 없이 표시된다"
    - "Admin에서 활성화된 모든 섹션 타입이 React에서 렌더링된다"
    - "Turbo 네비게이션 후 React 컴포넌트가 메모리 누수 없이 언마운트된다"
  artifacts:
    - path: "teovibe/app/frontend/components/landing/FeaturesSection.tsx"
      provides: "기능 소개 3단 카드 섹션"
      contains: "section_cards"
    - path: "teovibe/app/frontend/components/landing/TestimonialsSection.tsx"
      provides: "후기 섹션 (어두운 배경)"
      contains: "bg-tv-black"
    - path: "teovibe/app/frontend/components/landing/StatsSection.tsx"
      provides: "통계 숫자 섹션"
      contains: "section_cards"
    - path: "teovibe/app/frontend/components/landing/CtaSection.tsx"
      provides: "CTA 마무리 섹션 (오렌지 배경)"
      contains: "bg-tv-orange"
    - path: "teovibe/app/frontend/components/landing/FadeInSection.tsx"
      provides: "재사용 스크롤 입장 래퍼"
      contains: "whileInView"
  key_links:
    - from: "teovibe/app/frontend/components/landing/LandingPage.tsx"
      to: "teovibe/app/frontend/components/landing/FeaturesSection.tsx"
      via: "SECTION_COMPONENTS map"
      pattern: "features.*FeaturesSection"
    - from: "teovibe/app/frontend/components/landing/FeaturesSection.tsx"
      to: "teovibe/app/frontend/components/landing/FadeInSection.tsx"
      via: "import FadeInSection wrapper"
      pattern: "import.*FadeInSection"
---

<objective>
나머지 랜딩 섹션 컴포넌트(Features, Testimonials, Stats, CTA)를 구현하고, 스크롤 애니메이션과 모바일 반응형을 완성한다.

Purpose: 기존 ERB 섹션 파셜 8종 중 핵심 5종(hero는 Plan 01에서 완료)을 React로 포팅하여 랜딩페이지를 완성한다. motion/react의 whileInView로 스크롤 시 섹션별 fade-in 애니메이션을 추가한다.
Output: 모든 섹션이 스크롤 애니메이션과 함께 동작하는 완성된 React 랜딩페이지
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactive-landing/03-RESEARCH.md
@.planning/phases/03-interactive-landing/03-01-SUMMARY.md

# 기존 ERB 섹션 참조 (React 포팅 시 디자인 유지)
@teovibe/app/views/pages/sections/_features.html.erb
@teovibe/app/views/pages/sections/_testimonials.html.erb
@teovibe/app/views/pages/sections/_stats.html.erb
@teovibe/app/views/pages/sections/_cta.html.erb
</context>

<tasks>

<task type="auto">
  <name>Task 1: FadeInSection 래퍼 + FeaturesSection + TestimonialsSection 구현</name>
  <files>
    teovibe/app/frontend/components/landing/FadeInSection.tsx
    teovibe/app/frontend/components/landing/FeaturesSection.tsx
    teovibe/app/frontend/components/landing/TestimonialsSection.tsx
  </files>
  <action>
    1. FadeInSection 재사용 래퍼 생성 — `FadeInSection.tsx`:
       - `import { motion } from "motion/react"` (framer-motion 아님!)
       - props: `{ children: React.ReactNode, className?: string }`
       - `motion.div`로 감싸기:
         - `initial={{ opacity: 0, y: 32 }}`
         - `whileInView={{ opacity: 1, y: 0 }}`
         - `viewport={{ once: true, amount: 0.1 }}`
         - `transition={{ duration: 0.7, ease: [0.22, 1, 0.36, 1] }}`

    2. FeaturesSection 생성 — `FeaturesSection.tsx`:
       - 기존 `_features.html.erb` 디자인 포팅:
         - `<section className="py-24 px-5">` 감싸기를 FadeInSection으로
         - `max-w-[1200px] mx-auto`
         - 섹션 라벨: `<span className="text-xs font-bold tracking-[0.2em] text-tv-gray uppercase mb-4 block">WHAT'S TEOVIBE</span>`
         - 제목: `text-subheading md:text-display font-black leading-tight mb-16`
         - 카드 그리드: `grid grid-cols-1 md:grid-cols-3 gap-8`
         - 각 카드: `bg-white rounded-card p-8` (FadeInSection으로 개별 감싸기)
           - card.icon이 있으면 `<div className="text-4xl mb-4">{card.icon}</div>`
           - card.title: `text-subheading font-extrabold mb-3`
           - card.description: `text-base text-tv-gray leading-relaxed`
       - props: `{ section: LandingSection }` (LandingPage.tsx에서 정의한 타입 import)
       - section_cards를 position 순 정렬: `[...section.section_cards].sort((a, b) => a.position - b.position)`

    3. TestimonialsSection 생성 — `TestimonialsSection.tsx`:
       - 기존 `_testimonials.html.erb` 디자인 포팅:
         - FadeInSection으로 감싸기
         - `bg-tv-black text-white` 배경
         - 카드 그리드: `grid grid-cols-1 md:grid-cols-3 gap-8`
         - 각 카드: `bg-tv-dark rounded-card p-8`
           - card.description: `text-base leading-relaxed mb-6 text-tv-light-gray`
           - card.title: `font-bold` (이름)
           - card.link_text: `text-sm text-tv-light-gray` (직함 등)
       - section_cards를 position 순 정렬

    주의: LandingSection/SectionCard 타입을 별도 파일로 분리하거나, Plan 01의 LandingPage.tsx에서 export하여 import. Plan 01에서 export하지 않았다면 이 태스크에서 타입을 별도 파일(`types.ts`)로 추출하거나 LandingPage.tsx에서 export 추가.
  </action>
  <verify>
    ```bash
    cd teovibe && ls app/frontend/components/landing/FadeInSection.tsx app/frontend/components/landing/FeaturesSection.tsx app/frontend/components/landing/TestimonialsSection.tsx
    ```
    3개 파일 존재 확인

    ```bash
    cd teovibe && grep "whileInView" app/frontend/components/landing/FadeInSection.tsx
    ```
    whileInView 패턴 사용 확인

    ```bash
    cd teovibe && grep "motion/react" app/frontend/components/landing/FadeInSection.tsx
    ```
    올바른 import path 확인
  </verify>
  <done>
    FadeInSection 스크롤 래퍼가 생성되고, FeaturesSection과 TestimonialsSection이 기존 ERB 디자인을 충실히 포팅하여 구현되었다. 각 카드에 개별 스크롤 애니메이션이 적용된다.
  </done>
</task>

<task type="auto">
  <name>Task 2: StatsSection + CtaSection 구현 + LandingPage SECTION_COMPONENTS 등록 + 모바일 반응형 검증</name>
  <files>
    teovibe/app/frontend/components/landing/StatsSection.tsx
    teovibe/app/frontend/components/landing/CtaSection.tsx
    teovibe/app/frontend/components/landing/LandingPage.tsx
  </files>
  <action>
    1. StatsSection 생성 — `StatsSection.tsx`:
       - 기존 `_stats.html.erb` 디자인 포팅:
         - FadeInSection으로 감싸기
         - `py-24 px-5`, `max-w-[1200px] mx-auto text-center`
         - 제목: `text-subheading md:text-display font-black leading-tight mb-16`
         - 카드 그리드: `grid grid-cols-1 md:grid-cols-3 gap-8`
         - 각 카드: `bg-white rounded-card p-8`
           - card.icon: `text-5xl font-black mb-2` (큰 숫자)
           - card.title: `text-2xl font-extrabold mb-2`
           - card.description: `text-tv-gray`
       - section_cards를 position 순 정렬

    2. CtaSection 생성 — `CtaSection.tsx`:
       - 기존 `_cta.html.erb` 디자인 포팅:
         - FadeInSection으로 감싸기
         - `bg-tv-orange text-white` 배경
         - `max-w-[800px] mx-auto text-center`
         - 제목: `text-subheading md:text-display font-black leading-tight mb-8`
         - 부제목(있으면): `text-lg mb-10 opacity-90`
         - CTA 버튼: `<a href="/registrations/new">시작하기</a>`
           - `inline-block bg-white text-tv-orange rounded-pill px-10 py-4 text-lg font-bold hover:opacity-90 transition-opacity`

    3. LandingPage.tsx의 SECTION_COMPONENTS 맵 업데이트:
       - 기존 `{ hero: HeroSection }`에 추가:
         ```typescript
         features: FeaturesSection,
         testimonials: TestimonialsSection,
         stats: StatsSection,
         cta: CtaSection,
         ```
       - 해당 컴포넌트들 import 추가
       - `pricing`, `faq`, `custom` 타입은 SECTION_COMPONENTS에 등록하지 않음 (null 반환으로 스킵 — 이미 Plan 01에서 처리됨)

    4. 모바일 반응형 확인 포인트 (375px):
       - 모든 그리드: `grid-cols-1 md:grid-cols-3` 패턴으로 모바일에서 1열
       - CTA 버튼: `flex-col sm:flex-row` 패턴으로 모바일에서 세로 배치
       - 히어로(Plan 01): `text-display md:text-hero`로 모바일에서 작은 폰트
       - padding: `px-5`로 양쪽 여백 유지
       - 각 섹션 컴포넌트에서 md: prefix가 올바르게 적용되었는지 확인
  </action>
  <verify>
    ```bash
    cd teovibe && ls app/frontend/components/landing/StatsSection.tsx app/frontend/components/landing/CtaSection.tsx
    ```
    파일 존재 확인

    ```bash
    cd teovibe && grep -c "import.*Section" app/frontend/components/landing/LandingPage.tsx
    ```
    5개 섹션 import 확인 (Hero + Features + Testimonials + Stats + Cta)

    ```bash
    cd teovibe && grep "md:grid-cols-3" app/frontend/components/landing/FeaturesSection.tsx app/frontend/components/landing/TestimonialsSection.tsx app/frontend/components/landing/StatsSection.tsx
    ```
    반응형 그리드 패턴 확인

    ```bash
    cd teovibe && bin/vite build 2>&1 | tail -10
    ```
    Vite 빌드 성공 확인
  </verify>
  <done>
    5개 섹션 컴포넌트(Hero, Features, Testimonials, Stats, CTA)가 모두 LandingPage에 등록되어 렌더링된다. 모든 섹션이 스크롤 시 whileInView 애니메이션으로 등장하고, 모바일(375px)에서 1열 레이아웃으로 깨짐 없이 표시된다. Vite 빌드가 성공한다.
  </done>
</task>

</tasks>

<verification>
1. 홈페이지에서 스크롤 시 Features, Testimonials, Stats, CTA 섹션이 순차적으로 fade-in 등장하는지 확인
2. 모바일(375px) 뷰포트에서 모든 섹션이 1열 레이아웃으로 깨짐 없이 표시되는지 확인
3. Admin에서 각 섹션 타입(features, testimonials, stats, cta)의 내용을 변경 후 홈페이지에 반영되는지 확인
4. `bin/vite build` 성공
5. Turbo 네비게이션으로 다른 페이지 이동 후 뒤로가기 시 모든 섹션이 정상 렌더링
</verification>

<success_criteria>
- 5개 섹션 타입(hero, features, testimonials, stats, cta)이 모두 React 컴포넌트로 렌더링된다
- 스크롤 시 whileInView 애니메이션이 동작한다
- 375px 뷰포트에서 모든 섹션이 깨짐 없이 표시된다
- Vite 빌드가 에러 없이 완료된다
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-landing/03-02-SUMMARY.md`
</output>
