# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# Kamal의 dotenv 파서는 bash의 ${VAR:-default} 기본값 문법을 지원하지 않는다
# (그걸 쓰면 ':-}'가 리터럴로 값에 붙어 키가 오염됨). 그래서 단순하게 파일에서 읽음.
# 로컬: config/master.key 그대로. CI: deploy 워크플로가 시크릿에서 이 파일을 생성.
RAILS_MASTER_KEY=$(cat config/master.key)

# Google OAuth (.env 파일에서 로드)
GOOGLE_CLIENT_ID=$GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=$GOOGLE_CLIENT_SECRET

# Kakao OAuth (.env 파일에서 로드)
KAKAO_CLIENT_ID=$KAKAO_CLIENT_ID
KAKAO_CLIENT_SECRET=$KAKAO_CLIENT_SECRET

# Anthropic API (.env 파일에서 로드)
ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY

# ghcr.io 인증 — CI에선 GITHUB_TOKEN, 로컬 수동 deploy 시엔 write:packages PAT.
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD
