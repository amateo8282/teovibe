---
phase: 06-category-management
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app/controllers/posts_controller.rb
  - app/views/posts/index.html.erb
  - app/views/posts/new.html.erb
  - app/views/posts/_form.html.erb
  - app/views/shared/_navbar.html.erb
  - config/routes.rb
autonomous: true
requirements: [CATM-05]

must_haves:
  truths:
    - "기존 /blogs, /tutorials 등 URL이 /posts/blog, /posts/tutorial 등으로 301 리다이렉트된다"
    - "/posts/:category_slug 로 카테고리별 게시글 목록이 표시된다"
    - "Navbar에 visible_in_nav=true인 카테고리만 동적으로 표시된다"
    - "일반 사용자 게시글 작성 시 admin_only=false인 카테고리만 선택지에 표시된다"
    - "Admin 사용자는 게시글 작성 시 모든 카테고리를 선택할 수 있다"
  artifacts:
    - path: "app/controllers/posts_controller.rb"
      provides: "통합 PostsController (category_slug 기반 필터)"
      contains: "class PostsController"
    - path: "app/views/shared/_navbar.html.erb"
      provides: "동적 카테고리 Navbar"
  key_links:
    - from: "app/controllers/posts_controller.rb"
      to: "app/models/category.rb"
      via: "Category.find_by!(slug: params[:category_slug])"
      pattern: "find_by.*slug.*category_slug"
    - from: "app/views/shared/_navbar.html.erb"
      to: "app/models/category.rb"
      via: "Category.for_posts.visible_in_nav.ordered"
      pattern: "visible_in_nav"
---

<objective>
컨트롤러 통합 (6개→1개) + 라우팅 리다이렉트 + Navbar 동적화 + 일반 사용자 카테고리 필터

Purpose: 기존 개별 컨트롤러/라우트를 통합 PostsController로 전환하고, SEO URL을 리다이렉트로 보존하며, Navbar를 동적 카테고리 기반으로 전환
Output: 통합 PostsController, 리다이렉트 라우트, 동적 Navbar, admin_only 카테고리 필터
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-category-management/06-RESEARCH.md
@.planning/phases/06-category-management/06-01-SUMMARY.md

@app/controllers/posts_base_controller.rb
@app/controllers/blogs_controller.rb
@app/controllers/tutorials_controller.rb
@config/routes.rb
@app/views/shared/_navbar.html.erb
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostsController 통합 + 라우팅 리다이렉트 설정</name>
  <files>
    app/controllers/posts_controller.rb,
    config/routes.rb
  </files>
  <action>
1. `config/routes.rb` 수정:
   - 기존 6개 개별 resources (blogs, tutorials, free_boards, qnas, portfolios, notices) 라우트 제거
   - 통합 라우트 추가:
     ```ruby
     # 카테고리별 게시글 목록
     get "posts/:category_slug", to: "posts#index", as: :category_posts
     # 게시글 CRUD (slug 기반)
     resources :posts, param: :slug, only: %i[show new create edit update destroy]
     ```
   - SEO 리다이렉트 (301 permanent):
     ```ruby
     get "/blogs", to: redirect("/posts/blog", status: 301)
     get "/blogs/:id", to: redirect { |params, req| "/posts/#{params[:id]}" }
     get "/tutorials", to: redirect("/posts/tutorial", status: 301)
     get "/tutorials/:id", to: redirect { |params, req| "/posts/#{params[:id]}" }
     get "/free-boards", to: redirect("/posts/free-board", status: 301)
     get "/free-boards/:id", to: redirect { |params, req| "/posts/#{params[:id]}" }
     get "/qnas", to: redirect("/posts/qna", status: 301)
     get "/qnas/:id", to: redirect { |params, req| "/posts/#{params[:id]}" }
     get "/portfolios", to: redirect("/posts/portfolio", status: 301)
     get "/portfolios/:id", to: redirect { |params, req| "/posts/#{params[:id]}" }
     get "/notices", to: redirect("/posts/notice", status: 301)
     get "/notices/:id", to: redirect { |params, req| "/posts/#{params[:id]}" }
     ```
   - 주의: 라우트 순서 — `/posts/:category_slug`와 `/posts/:slug` 충돌 방지. 카테고리 slugs는 하이픈 포함 단어(blog, tutorial 등), 포스트 slugs는 `{id}-{title}` 형식. 라우트 제약 조건(constraints) 사용 검토 또는 라우트 순서로 해결

2. `app/controllers/posts_controller.rb` 수정 (또는 신규 생성):
   - 기존 PostsBaseController 로직을 흡수
   - `index`: `@category = Category.find_by!(slug: params[:category_slug], record_type: :post)` + `@posts = @category.posts.published.ordered`
   - `new`: 카테고리 선택지를 `available_categories` 헬퍼로 제공 (admin_only 필터 적용)
   - `create`: `@post.category = Category.find(params[:post][:category_id])` — category_id FK로 저장
   - `show/edit/update/destroy`: 기존 로직 유지, category 참조를 FK 기반으로 변경

3. 기존 6개 컨트롤러 파일 삭제:
   - `app/controllers/blogs_controller.rb`
   - `app/controllers/tutorials_controller.rb`
   - `app/controllers/free_boards_controller.rb`
   - `app/controllers/qnas_controller.rb`
   - `app/controllers/portfolios_controller.rb`
   - `app/controllers/notices_controller.rb`
   - (실제 파일 존재 여부 먼저 확인 후 삭제)

4. PostsBaseController 제거 또는 최소화 (6개 컨트롤러 삭제 후 더 이상 필요 없으면 제거)

5. 뷰 파일 정리:
   - 기존 blogs/, tutorials/ 등 개별 뷰 디렉토리가 있으면 통합 posts/ 뷰로 병합
   - posts/index.html.erb: `@category.name` 표시, 해당 카테고리 게시글 목록
   - posts/_form.html.erb: 카테고리 선택 드롭다운 (admin_only 필터 적용)
  </action>
  <verify>
    <automated>cd teovibe && bin/rails routes | grep -E "posts|blog|tutorial" | head -20 && bin/rails runner "puts Category.for_posts.ordered.pluck(:slug).join(', ')"</automated>
  </verify>
  <done>
    - 기존 /blogs → /posts/blog 리다이렉트 동작
    - /posts/blog 접속 시 블로그 카테고리 게시글 목록 표시
    - 기존 6개 개별 컨트롤러 파일 삭제됨
    - PostsController에서 category_slug 기반 필터링 동작
  </done>
</task>

<task type="auto">
  <name>Task 2: Navbar 동적 카테고리 + 게시글 작성 폼 admin_only 필터</name>
  <files>
    app/views/shared/_navbar.html.erb,
    app/views/posts/_form.html.erb,
    app/helpers/application_helper.rb
  </files>
  <action>
1. `app/views/shared/_navbar.html.erb` 수정:
   - 하드코딩된 카테고리 링크 제거
   - `Category.for_posts.visible_in_nav.ordered` 루프로 동적 생성:
     ```erb
     <% Category.for_posts.visible_in_nav.ordered.each do |cat| %>
       <%= link_to cat.name, category_posts_path(category_slug: cat.slug), class: "..." %>
     <% end %>
     ```
   - 스킬팩 카테고리도 있으면 별도 섹션으로 동적 표시

2. `app/helpers/application_helper.rb`에 헬퍼 메서드 추가:
   ```ruby
   def available_post_categories
     if Current.user&.admin?
       Category.for_posts.ordered
     else
       Category.for_posts.where(admin_only: false).ordered
     end
   end
   ```

3. `app/views/posts/_form.html.erb` 수정 (또는 new.html.erb):
   - 카테고리 선택 드롭다운: `collection_select(:post, :category_id, available_post_categories, :id, :name)`
   - admin_only 카테고리는 일반 사용자에게 노출되지 않음 (CATM-05)

4. 스킬팩 폼도 동일하게 처리 (해당 뷰 확인 후 category_id 드롭다운으로 변경)
  </action>
  <verify>
    <automated>cd teovibe && bin/rails runner "admin = User.find_by(admin: true); puts 'Admin cats: ' + Category.for_posts.ordered.count.to_s; puts 'User cats: ' + Category.for_posts.where(admin_only: false).ordered.count.to_s" && grep -c "visible_in_nav" app/views/shared/_navbar.html.erb</automated>
  </verify>
  <done>
    - Navbar에 visible_in_nav=true인 카테고리만 동적으로 표시됨
    - 일반 사용자 게시글 작성 폼에 admin_only=false 카테고리만 표시됨 (CATM-05)
    - Admin 사용자는 모든 카테고리 선택 가능
    - 하드코딩된 카테고리 링크가 Navbar에서 제거됨
  </done>
</task>

</tasks>

<verification>
- `curl -I localhost:3000/blogs` — 301 리다이렉트 to /posts/blog
- `curl -I localhost:3000/tutorials` — 301 리다이렉트 to /posts/tutorial
- /posts/blog 접속 → 블로그 카테고리 게시글 목록
- Navbar에 visible_in_nav=true 카테고리만 표시
- 일반 사용자 /posts/new → admin_only 카테고리 숨김
- Admin /posts/new → 모든 카테고리 표시
</verification>

<success_criteria>
- 기존 6개 URL이 모두 통합 URL로 리다이렉트됨
- PostsController가 category_slug 기반으로 동작
- Navbar가 동적 카테고리로 전환됨
- admin_only 카테고리 필터가 일반 사용자에게 적용됨
</success_criteria>

<output>
After completion, create `.planning/phases/06-category-management/06-03-SUMMARY.md`
</output>
