---
phase: 09-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/post.rb
  - app/controllers/posts_controller.rb
  - app/helpers/seo_helper.rb
  - test/models/post_test.rb
  - test/integration/og_meta_tags_test.rb
  - test/fixtures/posts.yml
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04

must_haves:
  truths:
    - "seo_title이 비어있는 게시글을 저장하면 title 기반 seo_title이 자동 생성된다"
    - "seo_description이 비어있는 게시글을 저장하면 본문 기반 seo_description이 자동 생성된다"
    - "seo_title/seo_description이 이미 있는 게시글을 수정해도 기존 값이 유지된다"
    - "게시글 상세 페이지의 og:title/og:description에 seo_title/seo_description이 우선 사용된다"
    - "JSON-LD headline에 seo_title이 우선 사용된다"
  artifacts:
    - path: "app/models/post.rb"
      provides: "before_save :auto_generate_seo_fields 콜백"
      contains: "auto_generate_seo_fields"
    - path: "app/controllers/posts_controller.rb"
      provides: "show 액션에서 seo_title/seo_description 우선 사용"
      contains: "seo_title.presence"
    - path: "app/helpers/seo_helper.rb"
      provides: "article_json_ld headline에 seo_title 반영"
      contains: "seo_title.presence"
    - path: "test/models/post_test.rb"
      provides: "SEO 자동 생성 단위 테스트 4개"
      contains: "auto_generate"
    - path: "test/integration/og_meta_tags_test.rb"
      provides: "seo_title 우선 사용 통합 테스트"
      contains: "seo_title"
  key_links:
    - from: "app/models/post.rb"
      to: "ActionText body"
      via: "body.to_plain_text.squish.truncate(155)"
      pattern: "to_plain_text.*truncate"
    - from: "app/controllers/posts_controller.rb"
      to: "app/models/post.rb"
      via: "@post.seo_title.presence || @post.title"
      pattern: "seo_title\\.presence"
---

<objective>
게시글 저장 시 seo_title/seo_description을 자동 생성하고, 프론트엔드 메타태그에서 SEO 필드를 우선 사용하도록 연동한다.

Purpose: DB에 존재하지만 연결되지 않은 seo_title/seo_description 컬럼을 (1) 자동 생성 콜백과 (2) 메타태그 우선 사용으로 활성화
Output: Post 모델 콜백 + PostsController/SeoHelper 메타태그 수정 + 단위/통합 테스트
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-seo/09-RESEARCH.md

@app/models/post.rb
@app/controllers/posts_controller.rb
@app/helpers/seo_helper.rb
@test/models/post_test.rb
@test/integration/og_meta_tags_test.rb
@test/fixtures/posts.yml

<interfaces>
<!-- Post 모델 기존 콜백 패턴 -->
From app/models/post.rb:
```ruby
has_rich_text :body
before_save :generate_slug, if: -> { slug.blank? && title.present? }
```

<!-- PostsController show 기존 메타태그 패턴 -->
From app/controllers/posts_controller.rb:
```ruby
post_description = helpers.strip_tags(@post.body.to_s).squish.truncate(150)
set_meta_tags(
  title: @post.title,
  description: post_description,
  og: { title: @post.title, description: post_description, ... },
  twitter: { card: "summary", title: @post.title, description: post_description },
  canonical: post_url(@post)
)
```

<!-- SeoHelper JSON-LD 패턴 -->
From app/helpers/seo_helper.rb:
```ruby
def article_json_ld(post)
  safe_json_ld({ ..., "headline" => post.title, ... })
end
```

<!-- Fixture 구조 -->
From test/fixtures/posts.yml:
```yaml
blog_post:
  title: 테스트 블로그 게시글
  slug: 1-test-blog-post
  status: 1
  category: blog
  user: one
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Post 모델 SEO 자동 생성 콜백 + 단위 테스트 (SEO-01, SEO-02, SEO-04)</name>
  <files>app/models/post.rb, test/models/post_test.rb</files>
  <read_first>
    - app/models/post.rb (현재 콜백 패턴 확인: generate_slug 패턴 참조)
    - test/models/post_test.rb (기존 테스트 구조 확인)
    - test/fixtures/posts.yml (픽스처 구조 확인)
    - test/fixtures/categories.yml (테스트에서 사용할 카테고리 확인)
    - test/fixtures/users.yml (테스트에서 사용할 유저 확인)
  </read_first>
  <behavior>
    - Test 1: seo_title이 비어있는 게시글 저장 시 title.truncate(60) 값으로 자동 생성됨
    - Test 2: seo_description이 비어있는 게시글 저장 시 body.to_plain_text.squish.truncate(155) 값으로 자동 생성됨
    - Test 3: seo_title이 이미 있는 게시글 저장 시 기존 seo_title 값이 유지됨 (덮어쓰지 않음)
    - Test 4: seo_description이 이미 있는 게시글 저장 시 기존 seo_description 값이 유지됨
    - Test 5: body가 없는 게시글 저장 시 seo_description은 빈 상태 유지 (에러 없음)
  </behavior>
  <action>
    1. test/models/post_test.rb에 SEO 자동 생성 테스트 5개 추가 (기존 scheduled? 테스트 아래에):
       - "seo_title이 비어있으면 title 기반으로 자동 생성된다": Post.create!(title: "테스트 제목", body: "본문", category: categories(:blog), user: users(:one), status: :published) 후 assert_equal "테스트 제목", post.seo_title
       - "seo_description이 비어있으면 본문 기반으로 자동 생성된다": 같은 게시글에서 assert post.seo_description.present? && assert post.seo_description.length <= 155
       - "seo_title이 이미 있으면 덮어쓰지 않는다": seo_title: "수동 SEO 제목"으로 create 후 assert_equal "수동 SEO 제목", post.seo_title
       - "seo_description이 이미 있으면 덮어쓰지 않는다": seo_description: "수동 설명"으로 create 후 assert_equal "수동 설명", post.seo_description
       - "body가 없어도 에러 없이 저장된다": body 없이 create 후 assert post.seo_description.blank? (seo_title은 title 기반이므로 생성됨)

    2. 테스트 실행 → RED 확인: bundle exec rails test test/models/post_test.rb

    3. app/models/post.rb에 before_save 콜백 추가:
       - 기존 `before_save :generate_slug` 바로 아래에 추가:
         ```ruby
         before_save :auto_generate_seo_fields, if: -> { title.present? }
         ```
       - private 섹션에 메서드 추가 (generate_slug 메서드 위):
         ```ruby
         def auto_generate_seo_fields
           self.seo_title = title.truncate(60) if seo_title.blank?
           if seo_description.blank? && body.present?
             self.seo_description = body.to_plain_text.squish.truncate(155)
           end
         end
         ```

    4. 테스트 실행 → GREEN 확인: bundle exec rails test test/models/post_test.rb
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bundle exec rails test test/models/post_test.rb</automated>
  </verify>
  <acceptance_criteria>
    - app/models/post.rb에 `before_save :auto_generate_seo_fields` 라인 존재
    - app/models/post.rb에 `def auto_generate_seo_fields` 메서드 존재
    - app/models/post.rb에 `self.seo_title = title.truncate(60) if seo_title.blank?` 존재
    - app/models/post.rb에 `body.to_plain_text.squish.truncate(155)` 존재
    - test/models/post_test.rb에 "seo_title이 비어있으면" 테스트 존재
    - test/models/post_test.rb에 "seo_description이 비어있으면" 테스트 존재
    - test/models/post_test.rb에 "덮어쓰지 않는다" 테스트 2개 존재
    - bundle exec rails test test/models/post_test.rb 통과 (0 failures, 0 errors)
  </acceptance_criteria>
  <done>Post 모델에 before_save 콜백이 추가되어 seo_title/seo_description이 비어있을 때만 자동 생성되고, 이미 있으면 보존됨. 단위 테스트 5개 통과.</done>
</task>

<task type="auto">
  <name>Task 2: PostsController/SeoHelper 메타태그 SEO 필드 우선 사용 + 통합 테스트 (SEO-03)</name>
  <files>app/controllers/posts_controller.rb, app/helpers/seo_helper.rb, test/integration/og_meta_tags_test.rb, test/fixtures/posts.yml</files>
  <read_first>
    - app/controllers/posts_controller.rb (현재 show 액션 메타태그 코드)
    - app/helpers/seo_helper.rb (현재 article_json_ld 메서드)
    - test/integration/og_meta_tags_test.rb (기존 메타태그 테스트 구조)
    - test/fixtures/posts.yml (기존 픽스처 — seo 필드 추가 필요)
  </read_first>
  <action>
    1. test/fixtures/posts.yml 수정 — blog_post에 seo 필드 추가:
       ```yaml
       blog_post:
         title: 테스트 블로그 게시글
         slug: 1-test-blog-post
         status: 1
         seo_title: "SEO 최적화된 블로그 제목"
         seo_description: "SEO 최적화된 블로그 설명입니다"
         category: blog
         user: one
       ```
       notice_post는 seo 필드 없이 유지 (fallback 테스트용).

    2. test/integration/og_meta_tags_test.rb에 SEO 우선 사용 테스트 추가 (기존 Test 7 아래에):
       ```ruby
       # Test 8: seo_title이 있으면 og:title에 seo_title이 사용됨
       test "게시글에 seo_title이 있으면 og:title에 seo_title이 출력된다" do
         post = posts(:blog_post)
         get post_path(post)
         assert_response :success
         assert_select "meta[property='og:title'][content='SEO 최적화된 블로그 제목']"
       end

       # Test 9: seo_description이 있으면 og:description에 seo_description이 사용됨
       test "게시글에 seo_description이 있으면 og:description에 seo_description이 출력된다" do
         post = posts(:blog_post)
         get post_path(post)
         assert_response :success
         assert_select "meta[property='og:description'][content='SEO 최적화된 블로그 설명입니다']"
       end

       # Test 10: twitter:title에도 seo_title이 사용됨
       test "게시글에 seo_title이 있으면 twitter:title에 seo_title이 출력된다" do
         post = posts(:blog_post)
         get post_path(post)
         assert_response :success
         assert_select "meta[name='twitter:title'][content='SEO 최적화된 블로그 제목']"
       end
       ```

    3. 기존 Test 1 ("og:title이 게시글 제목으로 출력된다") 수정 — blog_post에 seo_title이 추가되었으므로:
       ```ruby
       test "게시글 상세 페이지에 og:title이 게시글 제목으로 출력된다" do
         post = posts(:blog_post)
         get post_path(post)
         assert_response :success
         expected_title = post.seo_title.presence || post.title
         assert_select "meta[property='og:title'][content='#{expected_title}']"
       end
       ```

    4. app/controllers/posts_controller.rb의 show 액션 수정 — 36~53행을 다음으로 교체:
       ```ruby
       # seo_title/seo_description 우선 사용, 없으면 기존 방식 fallback
       meta_title = @post.seo_title.presence || @post.title
       meta_description = @post.seo_description.presence ||
                          helpers.strip_tags(@post.body.to_s).squish.truncate(150)

       set_meta_tags(
         title: meta_title,
         description: meta_description,
         og: {
           title: meta_title,
           description: meta_description,
           url: post_url(@post),
           image: "#{request.base_url}/icon.png",
           type: "article"
         },
         twitter: {
           card: "summary",
           title: meta_title,
           description: meta_description
         },
         canonical: post_url(@post)
       )
       ```

    5. app/helpers/seo_helper.rb의 article_json_ld 메서드 수정 — 7행의 headline 변경:
       ```ruby
       "headline" => post.seo_title.presence || post.title,
       ```

    6. 통합 테스트 실행: bundle exec rails test test/integration/og_meta_tags_test.rb
    7. 전체 테스트 스위트 실행: bundle exec rails test
  </action>
  <verify>
    <automated>cd /Users/jaehohan/Desktop/keep-going/teo-vibe/teovibe && bundle exec rails test test/integration/og_meta_tags_test.rb && bundle exec rails test</automated>
  </verify>
  <acceptance_criteria>
    - app/controllers/posts_controller.rb show 액션에 `meta_title = @post.seo_title.presence || @post.title` 존재
    - app/controllers/posts_controller.rb show 액션에 `meta_description = @post.seo_description.presence` 존재
    - app/helpers/seo_helper.rb article_json_ld에 `post.seo_title.presence || post.title` 존재
    - test/fixtures/posts.yml의 blog_post에 `seo_title: "SEO 최적화된 블로그 제목"` 존재
    - test/integration/og_meta_tags_test.rb에 "seo_title이 있으면 og:title" 테스트 존재
    - test/integration/og_meta_tags_test.rb에 "seo_description이 있으면 og:description" 테스트 존재
    - bundle exec rails test 전체 통과 (0 failures, 0 errors)
  </acceptance_criteria>
  <done>PostsController#show와 SeoHelper#article_json_ld에서 seo_title/seo_description을 우선 사용하며, 없으면 기존 방식으로 fallback. 통합 테스트 3개 추가, 기존 테스트 1개 수정, 전체 테스트 스위트 통과.</done>
</task>

</tasks>

<verification>
1. bundle exec rails test test/models/post_test.rb — SEO 단위 테스트 통과
2. bundle exec rails test test/integration/og_meta_tags_test.rb — 메타태그 통합 테스트 통과
3. bundle exec rails test — 전체 테스트 스위트 통과 (기존 테스트 미깨짐)
4. grep -n "auto_generate_seo_fields" app/models/post.rb — 콜백 존재 확인
5. grep -n "seo_title.presence" app/controllers/posts_controller.rb app/helpers/seo_helper.rb — 우선 사용 패턴 확인
</verification>

<success_criteria>
- seo_title이 비어있는 게시글 저장 시 title.truncate(60)으로 자동 생성
- seo_description이 비어있는 게시글 저장 시 body.to_plain_text.squish.truncate(155)로 자동 생성
- seo_title/seo_description이 이미 있으면 재생성하지 않음 (수동 입력 우선)
- 게시글 상세 페이지 og:title, og:description, twitter:title, twitter:description에 SEO 필드 우선 사용
- JSON-LD headline에 seo_title 우선 사용
- 전체 테스트 스위트 0 failures, 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-seo/09-01-SUMMARY.md`
</output>
