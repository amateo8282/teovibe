#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# (--ensure-latest 제거: brakeman 신규 릴리스마다 CI가 outdated로 exit 5 실패하던 고마찰 플래그.
#  버전 신선도는 Gemfile.lock 갱신 + bundler-audit로 관리.)

load Gem.bin_path("brakeman", "brakeman")
