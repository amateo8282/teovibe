---
phase: 02-content-experience
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - teovibe/Gemfile
  - teovibe/Gemfile.lock
  - teovibe/app/frontend/entrypoints/application.js
  - teovibe/app/controllers/admin/dashboard_controller.rb
  - teovibe/app/views/admin/dashboard/index.html.erb
autonomous: true
requirements: [ADMN-01]

must_haves:
  truths:
    - "Admin 대시보드에 조회수 상위 게시글 차트가 표시된다"
    - "Admin 대시보드에 최근 30일 회원가입 추이 차트가 표시된다"
    - "차트가 실제 데이터를 반영한다 (하드코딩이 아님)"
  artifacts:
    - path: "teovibe/app/controllers/admin/dashboard_controller.rb"
      provides: "top_posts_data, registration_trend 쿼리"
      contains: "group_by_day"
    - path: "teovibe/app/views/admin/dashboard/index.html.erb"
      provides: "chartkick 차트 헬퍼 호출"
      contains: "bar_chart"
    - path: "teovibe/app/frontend/entrypoints/application.js"
      provides: "chartkick/chart.js import"
      contains: "chartkick"
  key_links:
    - from: "teovibe/app/views/admin/dashboard/index.html.erb"
      to: "teovibe/app/controllers/admin/dashboard_controller.rb"
      via: "instance variables @top_posts_data, @registration_trend"
      pattern: "@top_posts_data|@registration_trend"
    - from: "teovibe/app/frontend/entrypoints/application.js"
      to: "chart.js npm package"
      via: "chartkick/chart.js import"
      pattern: "chartkick/chart.js"
---

<objective>
Admin 대시보드에 콘텐츠 분석 차트를 추가한다. chartkick + groupdate gem으로 조회수 상위 게시글(막대 차트)과 회원가입 추이(라인 차트)를 표시한다.

Purpose: Admin이 콘텐츠 성과와 성장 추이를 한눈에 파악하여 운영 의사결정 지원
Output: 차트가 포함된 Admin 대시보드
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-experience/02-RESEARCH.md
@.planning/phases/02-content-experience/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: chartkick + groupdate gem 설치 및 Chart.js Vite 연동</name>
  <files>teovibe/Gemfile, teovibe/Gemfile.lock, teovibe/package.json, teovibe/pnpm-lock.yaml, teovibe/app/frontend/entrypoints/application.js</files>
  <action>
1. Gemfile에 chartkick + groupdate 추가:
   ```ruby
   gem "chartkick"
   gem "groupdate"
   ```
   `cd teovibe && bundle install` 실행

2. Chart.js npm 패키지 설치:
   ```bash
   cd teovibe && pnpm add chartkick chart.js
   ```

3. app/frontend/entrypoints/application.js에 chartkick + chart.js import 추가:
   ```javascript
   // 차트 라이브러리 (Admin 대시보드)
   import "chartkick/chart.js"
   ```
   - 이 import는 기존 rhino-editor import 아래에 추가 (Plan 02-01에서 수정된 application.js 기준)
   - chartkick gem의 `<%= line_chart %>` 헬퍼가 런타임에 Chart.js를 감지하여 렌더링
   - importmap 방식 아닌 Vite 번들 방식이므로 CDN 태그 불필요 (RESEARCH Pitfall 5 참조)

4. 주의: `chartkick:install` 제너레이터는 importmap 프로젝트용이므로 실행하지 않음. Vite 프로젝트에서는 npm import만으로 충분.
  </action>
  <verify>
- `cd teovibe && bundle list | grep chartkick` -- chartkick gem 확인
- `bundle list | grep groupdate` -- groupdate gem 확인
- `pnpm ls chartkick` -- chartkick npm 확인
- `pnpm ls chart.js` -- chart.js npm 확인
- `grep -c "chartkick" app/frontend/entrypoints/application.js` -- 1 이상
- `RAILS_ENV=production bundle exec rails assets:precompile` 성공
  </verify>
  <done>chartkick + groupdate gem이 설치되고, chart.js가 Vite 번들에 포함되어 chartkick 차트 헬퍼가 동작할 준비가 되었다</done>
</task>

<task type="auto">
  <name>Task 2: Admin 대시보드 컨트롤러 쿼리 및 차트 뷰 추가</name>
  <files>teovibe/app/controllers/admin/dashboard_controller.rb, teovibe/app/views/admin/dashboard/index.html.erb</files>
  <action>
1. app/controllers/admin/dashboard_controller.rb의 index 액션에 쿼리 추가 (기존 @total_users, @total_posts 등 유지):
   ```ruby
   def index
     @total_users = User.count
     @total_posts = Post.count
     @total_comments = Comment.count
     @recent_posts = Post.includes(:user).order(created_at: :desc).limit(5)
     @recent_users = User.order(created_at: :desc).limit(5)

     # ADMN-01: 조회수 상위 10개 게시글
     @top_posts = Post.where("views_count > 0").order(views_count: :desc).limit(10)
     @top_posts_data = @top_posts.map { |p| [p.title.truncate(30), p.views_count] }

     # ADMN-01: 최근 30일 회원가입 추이
     @registration_trend = User.group_by_day(:created_at, last: 30).count

     # ADMN-01: 좋아요 통계 (상위 10개)
     @top_liked_posts = Post.where("likes_count > 0").order(likes_count: :desc).limit(10)
     @top_liked_data = @top_liked_posts.map { |p| [p.title.truncate(30), p.likes_count] }
   end
   ```

2. app/views/admin/dashboard/index.html.erb에 차트 섹션 추가 (기존 통계 카드 + 최근 목록 아래에):
   - 기존 div 구조를 유지하고, 최근 게시글/가입 섹션 아래에 차트 섹션 추가:
   ```erb
   <%# 차트 섹션 %>
   <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mt-10">
     <div class="bg-white rounded-card p-6">
       <h2 class="text-lg font-bold mb-4">조회수 상위 게시글</h2>
       <% if @top_posts_data.any? %>
         <%= bar_chart @top_posts_data, height: "300px" %>
       <% else %>
         <p class="text-tv-gray text-sm">아직 조회 데이터가 없습니다</p>
       <% end %>
     </div>

     <div class="bg-white rounded-card p-6">
       <h2 class="text-lg font-bold mb-4">좋아요 상위 게시글</h2>
       <% if @top_liked_data.any? %>
         <%= bar_chart @top_liked_data, height: "300px" %>
       <% else %>
         <p class="text-tv-gray text-sm">아직 좋아요 데이터가 없습니다</p>
       <% end %>
     </div>
   </div>

   <div class="bg-white rounded-card p-6 mt-8">
     <h2 class="text-lg font-bold mb-4">최근 30일 회원가입 추이</h2>
     <%= line_chart @registration_trend, height: "200px" %>
   </div>
   ```
   - empty state 처리: 데이터가 없을 때 "아직 데이터가 없습니다" 표시
   - 기존 통계 카드(전체 사용자, 전체 게시글, 전체 댓글)와 최근 목록은 그대로 유지
  </action>
  <verify>
- `grep -c "bar_chart" app/views/admin/dashboard/index.html.erb` -- 2 (조회수 + 좋아요)
- `grep -c "line_chart" app/views/admin/dashboard/index.html.erb` -- 1 (가입 추이)
- `grep -c "group_by_day" app/controllers/admin/dashboard_controller.rb` -- 1
- `grep -c "top_posts_data" app/controllers/admin/dashboard_controller.rb` -- 1 이상
- `grep -c "@total_users" app/controllers/admin/dashboard_controller.rb` -- 1 (기존 유지 확인)
- `RAILS_ENV=production bundle exec rails assets:precompile` 성공
  </verify>
  <done>Admin 대시보드에 조회수 상위 게시글(막대 차트), 좋아요 상위 게시글(막대 차트), 최근 30일 회원가입 추이(라인 차트)가 표시되며, 기존 통계 카드와 최근 목록이 유지된다</done>
</task>

</tasks>

<verification>
- chartkick + groupdate gem이 Gemfile에 존재하고 bundle install 완료
- chart.js + chartkick npm 패키지가 설치되고 application.js에 import
- Admin 대시보드에 3개 차트 (조회수, 좋아요, 가입 추이) 표시
- 기존 대시보드 기능(통계 카드, 최근 목록) 유지
- `RAILS_ENV=production bundle exec rails assets:precompile` 성공
</verification>

<success_criteria>
- Admin 대시보드에 조회수 상위 게시글 막대 차트가 렌더링된다
- Admin 대시보드에 좋아요 상위 게시글 막대 차트가 렌더링된다
- Admin 대시보드에 최근 30일 회원가입 추이 라인 차트가 렌더링된다
- 차트가 실제 DB 데이터를 반영한다
- 기존 대시보드 콘텐츠가 깨지지 않는다
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-experience/02-03-SUMMARY.md`
</output>
