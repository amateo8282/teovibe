---
phase: 05-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/app/views/shared/_navbar.html.erb
  - teovibe/app/views/layouts/admin.html.erb
  - teovibe/app/frontend/controllers/admin_sidebar_controller.js
autonomous: true
requirements:
  - UIUX-01

must_haves:
  truths:
    - "모바일(375px)에서 햄버거 메뉴를 열면 알림 링크, 관리자 링크(admin인 경우), 글쓰기 버튼이 모두 표시된다"
    - "모바일(375px)에서 Admin 사이드바가 기본 숨김 상태이고 햄버거 버튼으로 열 수 있다"
    - "Admin 사이드바 열린 상태에서 오버레이 클릭 시 사이드바가 닫힌다"
    - "데스크톱(1024px+)에서 기존 네비게이션과 Admin 사이드바가 변경 없이 정상 표시된다"
  artifacts:
    - path: "teovibe/app/views/shared/_navbar.html.erb"
      provides: "모바일 메뉴에 알림/관리자/글쓰기 링크 포함"
      contains: "notifications_path"
    - path: "teovibe/app/views/layouts/admin.html.erb"
      provides: "반응형 Admin 레이아웃 (모바일 off-canvas 사이드바)"
      contains: "admin-sidebar"
    - path: "teovibe/app/frontend/controllers/admin_sidebar_controller.js"
      provides: "Admin 사이드바 모바일 토글 Stimulus 컨트롤러"
      exports: ["default"]
  key_links:
    - from: "teovibe/app/views/layouts/admin.html.erb"
      to: "teovibe/app/frontend/controllers/admin_sidebar_controller.js"
      via: "data-controller='admin-sidebar'"
      pattern: "data-controller.*admin-sidebar"
---

<objective>
전체 레이아웃의 모바일 반응형을 보완한다: 공개 Navbar 모바일 메뉴에 누락된 알림/관리자 링크를 추가하고, Admin 사이드바를 모바일에서 off-canvas 패턴으로 전환한다.

Purpose: 모바일(375px) 사용자가 모든 네비게이션 기능에 접근 가능하도록 하여 프로덕션 완성도를 달성한다.
Output: 반응형 보완된 _navbar.html.erb, admin.html.erb, admin_sidebar_controller.js
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공개 Navbar 모바일 메뉴 알림/관리자 링크 추가</name>
  <files>teovibe/app/views/shared/_navbar.html.erb</files>
  <action>
_navbar.html.erb의 모바일 메뉴 섹션(data-mobile-menu-target="menu" 내부)을 수정한다.

현재 로그인 사용자 분기에 "내 프로필", "글쓰기", "로그아웃"만 있는 상태에 다음 항목을 추가:

1. "알림" 링크 추가 (notifications_path). 읽지 않은 알림 수가 0보다 크면 배지 표시 (인라인 span으로 카운트). 알림 링크는 "내 프로필" 바로 아래에 배치.
2. Admin 링크 추가 (Current.user.admin? 조건부). admin_root_path로 이동. "로그아웃" 바로 위에 배치.
3. 기존 "내 프로필" 링크 유지, "글쓰기" 버튼 유지, "로그아웃" 버튼 유지.

알림 배지 패턴:
```erb
<%= link_to notifications_path, class: "block text-lg font-medium flex items-center justify-between" do %>
  알림
  <% unread = Current.user.notifications.unread.count %>
  <% if unread > 0 %>
    <span class="bg-tv-orange text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center">
      <%= unread > 9 ? "9+" : unread %>
    </span>
  <% end %>
<% end %>
```

주의: 데스크톱 네비게이션(hidden md:flex) 부분은 절대 수정하지 않는다. 모바일 메뉴(data-mobile-menu-target="menu") 내부만 변경한다.
  </action>
  <verify>
`grep -n "notifications_path" teovibe/app/views/shared/_navbar.html.erb`로 모바일 메뉴 섹션에 알림 링크 존재 확인. `grep -n "admin_root_path" teovibe/app/views/shared/_navbar.html.erb`로 모바일 메뉴에 관리자 링크 존재 확인. 데스크톱 메뉴의 기존 구조가 변경되지 않았는지 diff로 확인.
  </verify>
  <done>모바일 메뉴에 알림(배지 포함), 관리자(admin 조건부), 글쓰기, 내 프로필, 로그아웃이 모두 표시된다. 데스크톱 메뉴는 변경 없음.</done>
</task>

<task type="auto">
  <name>Task 2: Admin 사이드바 모바일 반응형 (off-canvas + Stimulus 컨트롤러)</name>
  <files>teovibe/app/views/layouts/admin.html.erb, teovibe/app/frontend/controllers/admin_sidebar_controller.js</files>
  <action>
**admin_sidebar_controller.js 생성:**

app/frontend/controllers/ 디렉토리에 admin_sidebar_controller.js를 생성한다 (Stimulus 자동 등록됨).

```javascript
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  static targets = ["sidebar", "overlay"]

  open() {
    this.sidebarTarget.classList.remove("-translate-x-full")
    this.overlayTarget.classList.remove("hidden")
  }

  close() {
    this.sidebarTarget.classList.add("-translate-x-full")
    this.overlayTarget.classList.add("hidden")
  }
}
```

**admin.html.erb 수정:**

1. `<body>` 태그 안에 `data-controller="admin-sidebar"` wrapper div 추가
2. 오버레이 div 추가: `hidden fixed inset-0 bg-black/50 z-40 md:hidden` (data-admin-sidebar-target="overlay", data-action="click->admin-sidebar#close")
3. 사이드바 `<aside>` 수정:
   - 기존: `class="w-60 bg-tv-dark text-white min-h-screen fixed"`
   - 변경: `class="fixed inset-y-0 left-0 z-50 w-60 bg-tv-dark text-white -translate-x-full md:translate-x-0 transition-transform"`
   - `data-admin-sidebar-target="sidebar"` 추가
4. 메인 `<main>` 수정:
   - 기존: `class="ml-60 flex-1 p-8"`
   - 변경: `class="md:ml-60 flex-1 p-4 md:p-8 min-h-screen"`
5. 메인 내부 최상단에 모바일 헤더 추가 (md:hidden):
   - 햄버거 버튼 (SVG 아이콘 3줄, data-action="click->admin-sidebar#open")
   - "TeoVibe Admin" 텍스트
   - 하단 border로 구분
   - 예시: `<div class="md:hidden flex items-center gap-4 mb-6 pb-4 border-b border-gray-200">`

주의: 사이드바 내부 네비게이션 링크 목록은 절대 변경하지 않는다. 외부 래퍼와 클래스만 수정한다.
  </action>
  <verify>
admin_sidebar_controller.js 파일이 app/frontend/controllers/에 존재하는지 확인. admin.html.erb에서 `data-controller="admin-sidebar"`가 있는지 확인. `md:ml-60` 클래스가 main 태그에 있는지 확인 (ml-60 단독이 아닌). `-translate-x-full md:translate-x-0`이 aside에 있는지 확인.
  </verify>
  <done>모바일(375px)에서 Admin 사이드바가 기본 숨김이고 햄버거 버튼으로 슬라이드 열기/오버레이 닫기가 동작한다. 데스크톱에서는 기존과 동일하게 사이드바가 항상 표시된다.</done>
</task>

</tasks>

<verification>
1. `bin/rails routes | grep -E "admin|notification"` — 기존 라우트 변경 없음 확인
2. 모바일 뷰포트(375px)에서 공개 사이트 네비게이션 햄버거 메뉴 열기 — 알림/관리자 링크 확인
3. 모바일 뷰포트(375px)에서 Admin 페이지 접근 — 사이드바 숨김 + 햄버거 버튼 확인
4. 데스크톱(1024px)에서 기존 레이아웃 깨짐 없음 확인
</verification>

<success_criteria>
- 모바일 메뉴에 알림(배지), 관리자(admin), 글쓰기, 프로필, 로그아웃 모두 접근 가능
- Admin 사이드바가 모바일에서 off-canvas로 동작 (기본 숨김, 토글 열기/닫기)
- 데스크톱에서 기존 레이아웃과 동일하게 동작
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish/05-01-SUMMARY.md`
</output>
