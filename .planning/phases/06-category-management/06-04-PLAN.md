---
phase: 06-category-management
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - test/controllers/admin/categories_controller_test.rb
  - test/integration/category_routing_test.rb
autonomous: false
requirements: [CATM-01, CATM-02, CATM-03, CATM-04, CATM-05, CATM-06]

must_haves:
  truths:
    - "Admin이 게시판 카테고리를 이름/슬러그/설명으로 생성하면 게시글 작성 폼에 즉시 나타난다"
    - "Admin이 카테고리를 수정/삭제할 수 있으며, 게시글이 있는 카테고리는 삭제가 거부된다"
    - "Admin이 카테고리 순서를 드래그앤드롭으로 변경하면 목록 순서가 즉시 반영된다"
    - "Admin이 카테고리를 '관리자 전용'으로 설정하면 일반 사용자 폼에서 숨겨진다"
    - "Admin이 스킬팩 카테고리를 CRUD + 순서 변경할 수 있다"
    - "기존 URL이 모두 정상 리다이렉트되며 404가 발생하지 않는다"
  artifacts:
    - path: "test/controllers/admin/categories_controller_test.rb"
      provides: "Admin 카테고리 컨트롤러 통합 테스트"
    - path: "test/integration/category_routing_test.rb"
      provides: "라우팅 리다이렉트 통합 테스트"
  key_links: []
---

<objective>
Phase 6 전체 통합 테스트 + 최종 검증

Purpose: 모든 요구사항(CATM-01~06)이 End-to-End로 동작하는지 검증하고, 기존 테스트가 모두 통과하는지 확인
Output: 통합 테스트 파일, 전체 테스트 통과 확인
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-category-management/06-01-SUMMARY.md
@.planning/phases/06-category-management/06-02-SUMMARY.md
@.planning/phases/06-category-management/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin 카테고리 컨트롤러 + 라우팅 통합 테스트 작성</name>
  <files>
    test/controllers/admin/categories_controller_test.rb,
    test/integration/category_routing_test.rb
  </files>
  <action>
1. `test/controllers/admin/categories_controller_test.rb`:
   - CATM-01: Admin이 카테고리 생성 (POST /admin/categories with name, slug, description, record_type) → 201/302
   - CATM-02: Admin이 카테고리 수정 (PATCH /admin/categories/:id) → 성공
   - CATM-02: Admin이 게시글 없는 카테고리 삭제 → 성공
   - CATM-02: Admin이 게시글 있는 카테고리 삭제 → 거부 + flash 에러
   - CATM-03: reorder 엔드포인트 (PATCH /admin/categories/reorder with positions) → position 업데이트
   - CATM-04: toggle_admin_only (PATCH /admin/categories/:id/toggle_admin_only) → admin_only 반전
   - CATM-06: 스킬팩 카테고리 CRUD (record_type: skill_pack으로 동일 테스트)
   - 비인증 사용자 접근 → 리다이렉트/403

2. `test/integration/category_routing_test.rb`:
   - GET /blogs → 301 /posts/blog
   - GET /tutorials → 301 /posts/tutorial
   - GET /posts/blog → 200 (카테고리 목록)
   - GET /posts/nonexistent-slug → 404
   - CATM-05: 일반 사용자로 GET /posts/new → admin_only 카테고리가 select에 없음
   - Admin으로 GET /posts/new → 모든 카테고리가 select에 있음

3. 기존 테스트 전체 실행하여 깨진 테스트 수정:
   - `bin/rails test` 실행
   - enum 의존 테스트가 있으면 Category FK 기반으로 수정
   - fixture 파일에 categories fixture 추가 (필요시)
  </action>
  <verify>
    <automated>cd teovibe && bin/rails test</automated>
  </verify>
  <done>
    - Admin 카테고리 컨트롤러 테스트 전체 통과
    - 라우팅 리다이렉트 테스트 전체 통과
    - 기존 테스트 전체 통과 (깨진 테스트 없음)
    - CATM-01~06 모든 요구사항이 테스트로 커버됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6 카테고리 동적 관리 전체 기능:
    - Category 모델 + enum→FK 마이그레이션
    - Admin 카테고리 CRUD + Sortable.js DnD + 인라인 토글
    - 컨트롤러 통합 + SEO 리다이렉트 + 동적 Navbar
    - admin_only 카테고리 필터
  </what-built>
  <how-to-verify>
    1. `bin/rails server` 실행
    2. Admin 로그인 후 /admin/categories 접속
       - 게시판 카테고리 6개 + 스킬팩 카테고리 4개 테이블 확인
       - 카테고리 하나를 새로 생성 (이름/슬러그/설명 입력)
       - 생성된 카테고리를 수정
       - 게시글 없는 카테고리 삭제 → 성공
       - 게시글 있는 카테고리 삭제 시도 → 에러 메시지 확인
       - 카테고리를 드래그앤드롭으로 순서 변경
       - admin_only 토글 클릭 → 인라인 전환 확인
       - visible_in_nav 토글 클릭 → 인라인 전환 확인
    3. 일반 사용자로 로그인
       - /posts/new 접속 → admin_only 카테고리가 드롭다운에 없는지 확인
    4. 기존 URL 확인
       - /blogs 접속 → /posts/blog 리다이렉트 확인
       - /tutorials 접속 → /posts/tutorial 리다이렉트 확인
    5. Navbar 확인
       - visible_in_nav=true 카테고리만 표시되는지 확인
       - Admin에서 visible_in_nav 토글 후 Navbar 반영 확인
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 수정 필요사항 기술</resume-signal>
</task>

</tasks>

<verification>
- `bin/rails test` — 전체 테스트 통과
- 모든 CATM-01~06 요구사항이 수동 + 자동 테스트로 검증됨
- 기존 기능 회귀 없음
</verification>

<success_criteria>
- 전체 테스트 스위트 통과
- Admin 카테고리 관리 UI가 정상 동작
- 기존 URL 리다이렉트 정상
- Navbar 동적 표시 정상
- admin_only 카테고리 필터 정상
- 사용자가 "approved" 확인
</success_criteria>

<output>
After completion, create `.planning/phases/06-category-management/06-04-SUMMARY.md`
</output>
