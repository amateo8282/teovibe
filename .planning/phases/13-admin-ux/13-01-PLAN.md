---
phase: 13-admin-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - teovibe/app/views/admin/posts/_form.html.erb
  - teovibe/app/views/admin/posts/new.html.erb
  - teovibe/app/views/admin/posts/edit.html.erb
  - teovibe/test/integration/admin_editor_layout_test.rb
autonomous: false
requirements:
  - ADMN-01
  - ADMN-02
  - ADMN-03

must_haves:
  truths:
    - "Admin 게시글 작성/수정 페이지에서 좌측에 본문 에디터, 우측에 메타 패널이 나란히 표시된다"
    - "본문 에디터를 스크롤해도 메타 패널이 뷰포트 상단에 고정되어 따라온다"
    - "모바일(768px 미만)에서 2단이 1단으로 전환되며 메타 패널이 본문 아래에 쌓인다"
  artifacts:
    - path: "teovibe/app/views/admin/posts/_form.html.erb"
      provides: "2단 flex 레이아웃 폼"
      contains: "flex flex-col md:flex-row"
    - path: "teovibe/app/views/admin/posts/new.html.erb"
      provides: "확장된 래퍼 너비"
      contains: "max-w-5xl"
    - path: "teovibe/app/views/admin/posts/edit.html.erb"
      provides: "확장된 래퍼 너비"
      contains: "max-w-5xl"
    - path: "teovibe/test/integration/admin_editor_layout_test.rb"
      provides: "레이아웃 검증 통합 테스트"
      min_lines: 30
  key_links:
    - from: "teovibe/app/views/admin/posts/new.html.erb"
      to: "teovibe/app/views/admin/posts/_form.html.erb"
      via: "render 'form' partial"
      pattern: "render.*form"
    - from: "teovibe/app/views/admin/posts/edit.html.erb"
      to: "teovibe/app/views/admin/posts/_form.html.erb"
      via: "render 'form' partial"
      pattern: "render.*form"
---

<objective>
Admin 게시글 에디터 폼을 2단 레이아웃으로 재구성한다. 좌측에 AI 초안 패널 + 본문 에디터, 우측에 메타 정보 패널(제목, 카테고리, 상태, 예약 발행, 고정글, SEO 필드, 액션 버튼)을 sticky로 고정 배치한다. 모바일에서는 1단 fallback을 제공한다.

Purpose: 긴 게시글 작성 시 메타 정보 변경을 위해 스크롤할 필요가 없어진다
Output: 수정된 폼 뷰 3개 + 통합 테스트 1개
</objective>

<execution_context>
@/Users/jaehohan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaehohan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-admin-ux/13-RESEARCH.md

@teovibe/app/views/admin/posts/_form.html.erb
@teovibe/app/views/admin/posts/new.html.erb
@teovibe/app/views/admin/posts/edit.html.erb

<interfaces>
<!-- 현재 폼 구조 — 모든 필드가 단일 컬럼(space-y-4)으로 배치됨 -->
<!-- form_with(model: [:admin, post]) 내부: -->
<!--   1. 에러 블록 -->
<!--   2. AI 초안 패널 (data-controller="ai-draft") -->
<!--   3. 제목 text_field -->
<!--   4. 카테고리 collection_select -->
<!--   5. 상태 select -->
<!--   6. 예약 발행 datetime-local -->
<!--   7. 고정글 check_box -->
<!--   8. 본문 rhino-editor -->
<!--   9. SEO 제목 text_field -->
<!--   10. SEO 설명 text_area -->
<!--   11. 취소/저장 버튼 -->

<!-- new.html.erb, edit.html.erb 모두 max-w-2xl 래퍼 안에 render "form" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 통합 테스트 작성 + 2단 레이아웃 구현</name>
  <files>
    teovibe/test/integration/admin_editor_layout_test.rb,
    teovibe/app/views/admin/posts/_form.html.erb,
    teovibe/app/views/admin/posts/new.html.erb,
    teovibe/app/views/admin/posts/edit.html.erb
  </files>
  <behavior>
    - Test 1 (ADMN-01): GET /admin/posts/new 응답 HTML에 `flex flex-col md:flex-row` 클래스를 가진 2단 컨테이너가 존재한다
    - Test 2 (ADMN-01): GET /admin/posts/:id/edit 응답 HTML에도 동일한 2단 컨테이너가 존재한다
    - Test 3 (ADMN-02): 메타 패널 div에 `md:sticky md:top-8 md:self-start` 클래스가 포함되어 있다
    - Test 4 (ADMN-03): 에디터 컬럼에 `flex-1 min-w-0`이 포함되고, 메타 패널에 `w-full md:w-80`이 포함되어 모바일 1단 / 데스크탑 2단 전환을 지원한다
    - Test 5: new/edit 래퍼가 max-w-2xl 대신 max-w-5xl 이상의 넓은 컨테이너를 사용한다
  </behavior>
  <action>
    **RED 단계:**
    `teovibe/test/integration/admin_editor_layout_test.rb` 생성. Admin 로그인 후 GET /admin/posts/new, GET /admin/posts/:id/edit 응답 HTML에서 assert_select 또는 assert_match로 2단 레이아웃 CSS 클래스 존재를 검증하는 테스트를 작성한다. 기존 `test/controllers/admin/posts_controller_test.rb`의 로그인 패턴(sign_in 또는 fixture 기반)을 참고하여 인증 처리.

    테스트 실행하여 FAIL 확인.

    **GREEN 단계:**
    1. `new.html.erb`, `edit.html.erb`: `max-w-2xl` -> `max-w-5xl`로 변경. `bg-white rounded-card shadow-sm p-6` 래퍼는 제거 (2단 레이아웃에서 폼 전체를 감싸는 카드 스타일은 부적절 — 메타 패널에 별도 카드 적용)

    2. `_form.html.erb` 재구성:
       - `form_with`의 `class: "space-y-4"` 유지하되 에러 블록 아래에 2단 컨테이너 삽입
       - 에러 블록: 전체 너비 유지 (2단 컨테이너 위)
       - 2단 컨테이너: `<div class="flex flex-col md:flex-row gap-6 items-start">`
       - 좌측 에디터 컬럼: `<div class="flex-1 min-w-0 space-y-4">`
         - AI 초안 패널 (기존 그대로 이동)
         - 본문 에디터 (rhino-editor, `min-h-[200px]` -> `min-h-[400px]`으로 확장)
       - 우측 메타 패널: `<div class="w-full md:w-80 md:sticky md:top-8 md:self-start">`
         - 내부 카드: `<div class="bg-white rounded-card border border-gray-200 p-5 space-y-4">`
         - 헤더: `<h2 class="text-sm font-black text-tv-gray uppercase tracking-wide">메타 정보</h2>`
         - 필드 순서: 제목 -> 카테고리 -> 상태 -> 예약 발행 -> 고정글
         - 구분선: `<hr class="border-gray-100">`
         - SEO 소제목: `<h3 class="text-xs font-bold text-tv-gray">SEO</h3>`
         - SEO 제목 -> SEO 설명
         - 액션 버튼 (취소/저장): `<div class="flex gap-3 pt-2">`

    3. 메타 패널에 max-height 방어 처리: `max-h-[calc(100vh-4rem)] overflow-y-auto`를 내부 카드 div에 추가

    주의사항:
    - sticky 부모(flex 컨테이너)에 overflow 속성 추가 금지
    - 에디터 컬럼에 반드시 `min-w-0` 포함 (rhino-editor 오버플로우 방지)
    - 모바일에서는 `md:sticky` 미적용이므로 자연스럽게 1단 세로 배치

    테스트 실행하여 PASS 확인.
  </action>
  <verify>
    <automated>cd teovibe && bundle exec rails test test/integration/admin_editor_layout_test.rb</automated>
  </verify>
  <done>
    - new/edit 페이지에서 좌측 에디터 + 우측 메타 패널 2단 레이아웃 렌더링
    - 메타 패널에 sticky 클래스 적용
    - 모바일 breakpoint에서 flex-col로 1단 전환
    - 통합 테스트 전체 통과
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 시각 검증 — 2단 레이아웃 + sticky + 모바일 fallback</name>
  <action>
    사용자가 로컬 서버에서 Admin 에디터 레이아웃을 시각적으로 확인한다.
    확인 항목: 데스크탑 2단 배치, sticky 동작, 모바일 1단 전환, 폼 제출 정상 동작.
  </action>
  <verify>
    1. 로컬 서버 실행: `cd teovibe && bin/dev`
    2. Admin 계정으로 로그인 후 `/admin/posts/new` 접속
    3. 데스크탑(1024px+)에서 확인:
       - 좌측에 AI 초안 패널 + 본문 에디터가 넓게 표시되는지
       - 우측에 메타 패널(제목, 카테고리, 상태 등)이 고정 너비로 표시되는지
    4. 본문 에디터에 긴 텍스트 입력 후 스크롤 시:
       - 우측 메타 패널이 뷰포트 상단에 sticky로 따라오는지
    5. 브라우저 너비를 768px 미만으로 줄여서:
       - 1단 레이아웃으로 전환되는지
       - 메타 패널이 본문 아래에 쌓이는지
    6. 기존 게시글 수정 페이지(`/admin/posts/:id/edit`)에서도 동일한 레이아웃 확인
    7. 폼 제출(작성/수정)이 정상 동작하는지 확인
  </verify>
  <done>사용자가 "approved" 입력 또는 수정 사항 피드백</done>
</task>

</tasks>

<verification>
- `cd teovibe && bundle exec rails test test/integration/admin_editor_layout_test.rb` — 레이아웃 CSS 클래스 검증
- `cd teovibe && bundle exec rails test test/controllers/admin/posts_controller_test.rb` — 기존 컨트롤러 테스트 유지 확인
- `cd teovibe && bundle exec rails test` — 전체 테스트 스위트 통과
</verification>

<success_criteria>
- Admin 게시글 new/edit 페이지에서 2단 레이아웃 렌더링
- 메타 패널이 스크롤 시 sticky 고정
- 모바일에서 1단 fallback
- 모든 기존 테스트 + 새 통합 테스트 통과
- 사용자 시각 검증 통과
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-ux/13-01-SUMMARY.md`
</output>
